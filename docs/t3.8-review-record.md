# T3.8 — review record

Branch `t3.8-housing` from main @ 33ebcf5 (base verified: 415/3-skip/418 + 127/127, Item -1).
Filing order: Q-F c6e384d → Item 0 Q-A..Q-E 168b7e8 (docs only) → spec + manifest 5a70ace →
before-column (below). All numbers TRANSCRIBED from /tmp/t38-before.txt (rig:
HousingBeforeColumnTests, Skip-gated with Q-F invalidation conditions in its header).

## ITEM 1 — the before-column, measured on main (never cited)

Replay of the director's gate order log (Fixtures/t38-director-orders.bin, decoded in the
spec) on TODAY's world — seed 42, canonical, endowmentJitter 0.69. PROVENANCE: the gate's own
figures came from the pre-T3.6b world (jitter 0.25; chronicle founding pops match it
exactly), so qualitative reproduction is the claim, and it holds:

| reading | gate (pre-T3.6b world) | measured on main |
| --- | --- | --- |
| Hikiavur @ t177, 100/0/0/0/0 — Sustenance | 0.88 | 0.8750 (peasants) / 0.9034 (artisans) |
| Hikiavur — SHELTER | 0.00 | **0.0000 (both classes)** |
| Hikiavur — COMFORT | 0.00 | **0.0000 (both classes)** |
| Hikiavur — grievance | 119.55 | **227.49** |
| Hikiavur — grain stock | 225,026 | 214,001 |
| Mothian @ t29, default mix — Shelter | 0.88 | 0.8800 |
| Mothian — Comfort | 0.74 | 0.7306 |
| Mothian — grievance | 16.49 | 33.07 |

**The flow-binding, decomposed (the packet's defect, measured):** Hikiavur t177 timber —
stock 0, produced 0, consumption demand 87, eaten 0; stone demand 15, eaten 0. Shelter = the
basket's fill ratio = 0 exactly. **Measured bonus fact:** Mothian t177 runs sectors
0.07/0/0/0/0.93 — construction 93% — and STILL reads Shelter 0.0000 (timber demand 6, eaten
0): under the stand-in, building LABOR is irrelevant to Shelter; only the extraction flow of
timber matters. The stand-in does not even measure building effort.

## ITEM 3 — Comfort: MEASURED, verdict below

Same run, same decomposition: Hikiavur t177 pottery demand 59 eaten 0, cloth demand 88 eaten
0 → Comfort 0.0000 both classes. **Comfort IS flow-bound in exactly the Shelter shape.**
VERDICT (per the packet's own rule): the honest fix is a HOUSEHOLD DURABLE-GOODS stock
(pots/cloth inventories with wear), which is a SECOND stock with its own maintenance model —
NOT the housing stock and not a cheap same-shape rider. Finding stated, scope NOT silently
widened: Comfort keeps the flow stand-in this packet, and the durable-goods stock is filed to
the queue (T3.9a close-out Q5 updated: measured confirmed, needs its own packet).

## FALLOUT — the true enumeration, measured BEFORE any repair

Two runs, two different facts:

1. **The wall (at 50d6127):** 229 failed / 186 passed / 4 skipped. Read as SUSPECT before
   being read as fallout — one cause: the pre-existing T3.5b bound-need-no-satisfier guard
   refused the canonical needs.json once Shelter's basket lines were gone. Guard taught the
   housingStock source in 61c423f; its red proofs are re-owed and tracked below.
2. **The enumeration (at 61c423f):** full Sim.Tests re-run on the worktree at 61c423f
   (c9f0627/9aff69a landed while the 29-minute run was in flight — tests-only and docs-only
   respectively, not in the enumerated binary). **Failed 13 / Passed 402 / Skipped 4 /
   Total 419** (base was 415/3/418; +1 is the Skip-gated before-column rig).

The thirteen, grouped by HYPOTHESIZED cause. Hypotheses are exactly that — each is
UNVERIFIED until its repair diagnoses it, and a failure that contradicts its group is a
finding, not fallout:

| # | failing test | hypothesis (unverified) |
| --- | --- | --- |
| 1 | Kernel.SnapshotTests.GoldenHash_Seed42Turn200_MatchesPinnedConstant | schema v19 + housing system → toy golden moves (re-pin ONCE, itemized) |
| 2 | Kernel.SnapshotTests.FoundedGolden_Seed42Turn300_MatchesPinnedConstant | same, founded golden |
| 3 | Systems.FirstReignTests.FirstReign_PostFix_HarvestDiesWithThePeople_NoFoodMountain | pinned-trajectory scenario; housing labor split + Shelter rebind move the trajectory |
| 4 | Kernel.PipelineLoaderTests.CanonicalPipelineFile_LoadsInConfiguredOrder | roster pin: "housing" (WellKnownId 15) joined the canonical pipeline |
| 5 | Kernel.PipelineLoaderTests.UnknownSystem_FailsNamingItAndListingKnown | the known-system list in the refusal message now includes housing |
| 6 | Systems.NeedsGrievanceTests.Satisfaction_ClampsAtBothEnds | fixture assumes basket-sourced Shelter; stock-sourced path changes setup needs |
| 7 | Systems.NeedsGrievanceTests.Plenty_DecaysGrievance_MeasuredHalfLifeDocumented | same family |
| 8 | Systems.NeedsGrievanceTests.CanonicalRegistry_TheEightNeeds_SustenanceShelterComfortBoundAtM3 | registry-shape pin sees Shelter's satisfier change form |
| 9 | Systems.NeedsGrievanceTests.GenerationalDecay_EqualGrief_HigherTurnoverDecaysStrictlyFaster | same family |
| 10 | Systems.NeedsBasketTests.HousingShortage_LowersShelterSatisfaction_AndRaisesGrievance | the T3.5-era test asserting the FLOW STAND-IN by name — semantic replacement against the stock, not a patch |
| 11 | Worldgen.PluralWorldTests.Partition_EqualsBruteForceWitness_On500SampledNodesAcross3Seeds | **SUSPECT, not assumed fallout**: the per-source-budget Partition refactor claims the scalar path is bit-identical — this failure either breaks that claim (regression in the refactor) or the witness now runs under different budgets. Diagnosed FIRST, before anything is re-pinned. |
| 12 | Systems.CatchmentTests.Catchment_Twin_Deterministic_AndEqualsDirectIsochrone | tier budgets vs the test's direct-isochrone witness (witness likely calls the scalar API at base budget while the system runs tier budgets) |
| 13 | Systems.CatchmentTests.Catchment_NodeCount_And_RecomputeMs_Reported_At1024 | tier budget moves node counts at 1024 |

Outside the suite: ci.yml's FOUNDED_GOLDEN pin moves with #2 (14th repair item, CI-side).

Repair order PRE-COMMITTED before diagnosis: #11 first (the bit-identity claim is at
stake), then #12/#13 (same subsystem), then the needs-side rewrites #6–#10, then roster
pins #4/#5, then trajectory/golden re-pins #1–#3 + ci.yml LAST and ONCE.

## GUARD RE-PROOF — the modified guard's reds, MEASURED (§7.4 in a new shape)

The T3.5b bound-need guard was WIDENED in 61c423f to clear the 229-wall — a modification
authored against a failing suite, which voids the guard's prior red proof (the queue line
carries the general rule; candidate ADR-015 section, owner T3.10). Three permanent tests
went in (HousingGuardTests, c9f0627); all three PASS against the intact guard (4/4 with the
v19 populated-table test, whose Conserved seeding was corrected to the lawful
Ledger.InitialEndowment path — the grep-gated FromSnapshot bypass refused to compile from a
test, which is that gate working). Then each arm was deleted ONE at a time and the red
measured:

1. **Original property** (no-satisfier arm deleted):
   BoundNeed_WithNoSatisfierOfAnyKind_StillRefusesLoad — FAILED 1/1. The original T3.5b
   red re-proven against the modified guard.
2. **Closed vocabulary** (typo arm deleted): SourceTypo_RefusesLoad… — FAILED, and the
   failure MODE is itself a finding: `Assert.Contains "source must be" — sub-string not
   found`; the load still refused, but via the NO-SATISFIER guard ("need 2 (Shelter) is
   bound but no basket e…"). The typo'd source silently became basket exactly as feared,
   and the other guard caught only the incidental emptiness. Assert.Throws alone would have
   PASSED — the message asserts are load-bearing; without them the surviving guard MASKS
   the vocabulary deletion. By the same code path (read, not measured): a typo on a need
   that HAS basket lines would load silently — the vocabulary arm is the only tooth for
   that case.
3. **Ambiguity** (double-source arm deleted): HousingSourcedNeed_WithBasketLines… —
   FAILED, "No exception was thrown": a double-sourced need loads silently without this
   arm.

All arms restored by git checkout after each measurement; no deletion was ever committed.

### #11 — pre-measurement reading, and the DISCRIMINATING observable (committed before running)

Reading the witness and the refactor surfaced TWO distinct candidate causes, not one:

- **(a) witness-budget mismatch (pure fallout).** The witness relaxes and filters at the
  single base budget; the system now runs tier budgets — and founding tiers are nonzero BY
  DESIGN (sizeDwellingsRef is founding-scale per the spec's committed shape, so settlements
  found near saturation; jitter 0.69 varies the tier per settlement). Signature: mismatches
  where the witness reads bestOwner = −1 but the claim table has an owner, at costs in
  (base, tierBudget].
- **(b) a semantic gap in the refactor itself (a finding, not fallout).** The single-label
  multi-source Dijkstra with relax-time budget refusal is NOT provably equal to per-source
  capped fields under UNEQUAL budgets: a cheaper, shorter-budget source can hold a node's
  only label, then exhaust at its budget — truncating a longer-budget source's field BEHIND
  it (label-stealing). Equal budgets cannot exhibit this (any refused tentative is refused
  for every source); unequal budgets can, in the annulus beyond a smaller settlement's
  budget but inside a larger one's, on the far side of the smaller. Signature: with the
  witness FIXED to per-settlement budgets, residual mismatches where the witness names an
  in-budget owner and the partition claims nobody (or somebody else).

Discriminating measurement, pre-committed: fix the witness to per-settlement budgets via
the system's own pure SizeTier/TierBudget, then compare over the FULL lattice (all nodes,
not 500 samples) on seeds 42/7/1234, printing each seed's tier vector. Outcomes: (a) alone
→ zero residual mismatches AND the tier vectors show unequal budgets were actually
exercised; (b) → residual mismatches with the (b) signature, and the repair goes to the
IMPLEMENTATION (exact per-source semantics), never to the witness — the witness is the
T2.3 spec of Partition. If every tier ties (budgets equal in all three worlds), the
diagnostic is VACUOUS for (b) and says so rather than reading green as exoneration.

### #11 — MEASURED OUTCOME: both causes real; the implementation was replaced

**(b) CONFIRMED — the refactor as shipped did not honor the witness semantics.** Rather
than wait for seeds to happen to exercise the steal zone, the case was constructed:
`PartitionUnequalBudgetTests` probes deterministic origin pairs on the dev world (origins
three columns apart; budgets = 25th percentile of the small source's own field vs 75th of
the large one's) and runs the OLD single-label formulation INLINE as a mutant against the
per-source witness. Measured: **14 pairs probed, 13 exhibit single-label divergence** —
the truncation was pervasive, not a corner case. The repair went to the implementation,
never the witness: `Pathfinder.Partition(budgets)` now runs one capped Dijkstra PER SOURCE
merged at settle time by the composite (cost, index) key — exact to the witness BY
CONSTRUCTION (measured: bit-exact owner+cost equality on every node of every probed pair).
The semantics ruling is stated at the implementation: a settlement's reach is its own
field; produce may TRANSIT a rival's claimed territory (M3 has no polity borders — walls
are D-037's business). Equal budgets reproduce T2.3's multi-source result exactly (a
refused tentative is refused for every source alike), so no pre-T3.8 semantics moved.

**(a) CONFIRMED and the exercise question answered by print:** founding tier vectors are
seed 42 [4,2,4,3], seed 7 [4,4,4,2], seed 1234 [1,4,4,4] — unequal budgets are genuinely
exercised in all three witness worlds (jitter 0.69 spreads founding dwellings across
tiers 1–4).

**A third defect found by the same diagnosis, in the refactor's own documentation:** the
shipped doc comment claimed equal-budget bit-identity "proven by the equal-budget
equivalence test" — grep found NO such test anywhere in Sim.Tests. A comment citing an
unmeasured proof is the §7.12 shape at code scale; the rewritten comment cites the real,
measured proof (the divergence rig) and nothing else.

**The residual red after the witness fix was the witness's own floor, not a mismatch:**
the full sweep passed node-for-node on seed 42 and then tripped `claimedTotal >= 50` — a
bar calibrated for the OLD instrument, which counted claimed DRAWS with duplicates over a
~40-node claimed set (measured: 44 distinct claimed nodes at seed 42 = 100% coverage).
Recalibrated to the completeness identity (witness claimed total == CatchmentNodes row
count, bidirectional) plus a ≥2×settlements non-triviality floor.

### #12/#13 — hypotheses CONFIRMED; #4–#10 — hypotheses CONFIRMED

- #12/#13: both witnesses called direct isochrones at the BASE budget while the system
  runs tier budgets; both now derive the settlement's actual budget through the system's
  own pure functions (`TierBudgetOf` helper). #13 measured on the canonical 1024² world:
  22 nodes, recompute 3.0 ms — the per-source implementation sits far inside the 2 s bound.
- #4/#5: roster pins — "housing" at pipeline index 6 (after trade, position-is-legibility
  comment added) and in the known-systems refusal list. Pipeline length 11 → 12.
- #6–#9: the rigs' "fully supplied" premise needed the stock — `HouseFully` seeds
  ceil(1000/ppd) dwellings via the lawful Ledger path on every arm whose accrual must be
  exactly zero; ClampsAtBothEnds' starved arm deliberately keeps NO housing row (the
  no-row path reads 0.0). GenerationalDecay houses BOTH arms identically so the contrast
  stays turnover alone.
- #10: semantic replacement, not a patch — the contrast variable is now the dwelling stock
  itself (167 vs 55 dwellings per 1000 people), with the satisfaction asserted BIT-EXACT
  against the same arithmetic the system runs (55×6/1000 = 0.33) and the §7.5
  interior-of-the-bound assert retained.

Measured after these repairs: the needs/pipeline/guard/v19 group runs 48/48; the
partition/catchment group runs green with the three tier prints and the transit pin's
13/14 divergence count. Remaining from the enumeration: #1/#2/#3 (goldens + FirstReign) —
LAST and ONCE, per the pre-committed order, after the housing mechanism tests land.

## MUTATION SWEEP — bounds and predicted killers, PRE-COMMITTED before any mutant runs (§7.1)

Stated before the sweep, in this commit, so the bounds cannot be fitted to the runs:

- **Instrument:** each mutant is applied to the source ONE at a time, killed by a TARGETED
  `dotnet test --filter` run over its predicted killer tests plus the full HousingSystemTests/
  guard/needs group (so an unexpected kill elsewhere is visible), then reverted by
  `git checkout`. Mutants are never committed.
- **Bound (§7.1):** the clean runtime of the sweep filter is measured ONCE before the first
  mutant; every mutant run is bounded at **5× that clean baseline**. A run exceeding the bound
  is killed and recorded as "NON-TERMINATION under this mutation" — a finding, never waited out.
- **Kill standard (§7.2, both questions):** a kill names the failing test and quotes its
  failure mode, and the test's asserted property is stated (teeth AND aim). **Golden-only
  kills are counted separately** and recorded as coverage gaps, not kills — no golden test is
  in the sweep filter, so any mutant that survives the filter gets one full-suite probe and,
  if only a golden catches it, that is the finding.
- **The one-turn-stop assertion** (manifest lens 4's required killer for the rebind mutant) is
  added as a PERMANENT test before the sweep: two stopped turns (no materials, no construction
  labor) must read Shelter at exactly the decayed stock's value — high and nonzero — where the
  flow stand-in read 0.0000 (the before-column's measured defect).

| # | mutant (manifest name) | applied as | PREDICTED killer (pre-committed) |
| --- | --- | --- | --- |
| M1 | τ_h → ∞ (shortfall never degrades) | degradation block condition `m < 1.0` → `false` | Unmaintained_DecaysByTheExactExponent (100 ≠ 78); OneTurnStop bits |
| M2 | rebind reverted (Shelter reads the flow) | NeedsGrievanceSystem's FromHousingStock branch removed — always basket path | **the one-turn-stop assertion**, per the manifest — NOT a golden; HousingShortage's row lookup as secondary |
| M3 | degradation ignores m (always full decay) | exponent's `(1.0 - m)` → `1.0` | Maintenance_IsLeontief (lost 22 ≠ 12 at m = 0.48) |
| M4 | labor split deleted (PathBuild unaffected) | PathBuild's Prev.Housing subtraction loop removed | LaborSplit twins (twin B bank == twin A bank → bit assert fails; floor arm accrues) |
| M5 | stock cap deleted | `deficit` term removed from the build min | Build_StopsExactlyAtTheSurplusTarget arm 1 (builds at target; 120 grows) |

### The sweep, MEASURED — five mutants, five kills, all by their pre-committed killers

Clean filter baseline (measured before the first mutant): 47 tests, ~11 s wall → bound 55 s
per mutant test phase (180 s hard timeout with rebuild). Every run finished far inside the
bound; no non-termination; the tree was git-restored after each mutant; nothing committed.

| # | result | killed by (failure transcribed from the run) |
| --- | --- | --- |
| M1 | **KILLED** | Unmaintained_DecaysByTheExactExponent + OneTurnStop (both predicted) + Leontief + ConservationIdentity + DtSensitivity — five semantic reds |
| M2 | **KILLED** | **OneTurnStop_ShelterFollowsTheStock_NeverCollapsesToZero — the manifest's required killer, not a golden** — plus HousingShortage as secondary |
| M3 | **KILLED** | Maintenance_IsLeontief_TheScarcerMaterialGoverns alone (the predicted single kill: lost 22 ≠ 12 at m = 0.48) |
| M4 | **KILLED** | LaborSplit_PathBuildDeductsPrevHousingLabor_FlooredAtZero alone (the founded twins' bit asserts) |
| M5 | **KILLED** | Build_StopsExactlyAtTheSurplusTarget_NeverAStockpile alone (arm 1: builds past the target) |

Golden-only kills: **zero** — no golden test is in the sweep filter and no mutant survived
the filter, so the golden probe was never needed. A first M1 formulation (`if (false)`) was
rejected by the compiler itself (CS0162 unreachable-code as error) and reformulated as a
runtime-opaque never-true condition; noted for sweep hygiene.

## AFTER-COLUMN — H1/H2 FIRED THE FAILURE BRANCH: the housing stock COLLAPSES on the
## canonical world, and the measured cause is one the pre-commitment did not enumerate

Instrument: the SAME rig as the before-column (Fixtures/t38-director-orders.bin replayed 190
turns, seed 42) extended with a strict SUPERSET of snapshot fields (dwellings,
maintFraction, laborUsed, catchment nodes/arable/tier) — shared fields stay comparable.
Suite context: full Sim.Tests green at ca29b85 (428/0/4-skip, 22 m 44 s) BEFORE this
measurement, so everything below is the shipped mechanism working exactly as its tests pin
it, on a world that cannot feed it.

**The after-column, transcribed (before-column values in brackets):**

| reading | after (fixed tree) | [before, flow stand-in] |
| --- | --- | --- |
| Hikiavur t177 — Sustenance | 0.8750 / — | [0.8750 / 0.9034] |
| Hikiavur t177 — SHELTER | **0.0000** | [0.0000] |
| Hikiavur t177 — Comfort | 0.0000 | [0.0000] |
| Hikiavur t177 — grievance | 119.02 | [227.49] |
| Hikiavur t177 — dwellings / maintFraction | **0 / 1.0000 (no-dwellings path)** | [n/a] |
| Mothian t29 — Shelter | **0.0000** | [0.8800] |
| Mothian t29 — grievance | 221.98 | [33.07] |
| Mothian t29 — dwellings | **0** | [n/a] |
| Mothian t177 — Shelter / grievance | 0.0000 / 243.15 | [0.0000 / —] |
| sizeTier, every snapshot | **0** | [n/a] |

H1's pre-committed discharge (0.78 / 0.29 / 0.007, "never 0") did NOT discharge; H2's
(Shelter ~0.5–0.6, grievance to tens) did NOT discharge. At t29 the after-world is
STRICTLY WORSE than the flow stand-in it replaces (Shelter 0.0000 vs 0.8800; grievance
221.98 vs 33.07): the stand-in at least rewarded a settlement actively obtaining materials.

**The cause, measured per-turn (one-off diagnostic, transcribed then deleted):** settlement
4, canonical founded world, no orders, first 20 turns:

```
t1:  dwellings=38 m=0.0000 clayEnd=0 clayProduced=120
t5:  dwellings=14 m=0.0000 clayEnd=0 clayProduced=155
t10: dwellings=4  m=0.0000 clayEnd=0 clayProduced=174
t17: dwellings=1  m=0.0000 clayEnd=0 clayProduced=177
t18: dwellings=0  m=0.0000 clayEnd=0 clayProduced=179
t19: dwellings=0  m=1.0000 (the no-dwellings publish path)
```

m = 0.0000 FROM TURN ONE. Clay ends every turn at zero while clay PRODUCTION runs 120–185
per turn — ample against housing's upkeep demand (~12/turn at the founding stock). H1's
branch asked "stock too small or maintenance too steep"; the measured answer is NEITHER —
the third cause: **intra-turn material allocation.** Crafting (pipeline position 2) consumes
the entire clay stock as pottery input every turn (input-capped-and-consumes, to labor
capacity, NOT demand-capped: t29 Mothian crafted 122 pottery against 97 demanded, eating
244 clay = all of it); housing (position 6) reads the residue, which is measured 0, every
turn, from turn 1. Founding seeds dwellings but no clay endowment; the Leontief's clay arm
is 0 from the start; the stock decays at the exact m = 0 exponent (the trajectory above IS
exp(−0.25) per turn, quantized) and rebuild is permanently clay-capped at zero.

**Consequences, stated:** Shelter reads 0 world-wide within ~2 turns of founding (worse than
before at every early reading); the Item 4 size-tier bonus is INERT on the canonical
trajectory (tier 0 everywhere once the stock is gone — the founding tiers 1–4 engage only
for the first few turns); the re-pinned founded golden and FirstReign v14 EMBED this
collapsed-housing world (their pins are reproducibility, not endorsement — stated so the
certification reads them correctly).

**A lens-1 finding against this packet's own derivation:** the build-mix provenance doc
checked the TIMBER arm against shipped production (~24/yr, recorded in sim.json's _doc) but
never checked the CLAY arm against shipped ALLOCATION. The missing check was not
production-vs-demand (production is ample) but residual-after-crafting-vs-demand — and the
residual is measured zero. A rate checked against the wrong denominator.

**Per the spec's own pre-commitment, this is REPORT, not repair:** the prohibitions stand
(no Shelter weight change, no Tier A demotion, no grievance weakening), and lens 1 forbids
adjusting any rate after a disappointing H-reading — so NO rate, no pipeline order, and no
draw-priority was touched. The design options belong to the director's ruling; enumerated
WITHOUT recommendation in the handback: (a) a maintenance-priority draw or clay reserve
share for housing; (b) demand-capping crafting's input consumption (B-2-adjacent — the same
unbounded-production shape); (c) re-deriving the maintenance material itself (wattle-and-
daub earth is not potter's clay — the reference class may simply name the wrong good);
(d) founding clay endowment + any of the above. Each changes measured behaviour and the
goldens; none is a tuning-only change in effect.

## ITEMS 3 + 4 — Sim.Ui.Tests and the bench, measured

**Sim.Ui.Tests: 127/127 green** after one repair. The suite first ran 126/127: the HUD
needs-block pin expected "Shelter: 0.00" and read "Shelter: 1.00" — a 14th fallout item the
Sim.Tests-only enumeration could not see. Diagnosed against the fixture: the rig founds the
dev world and steps ONE turn, so satisfaction reads the FOUNDING Prev — and founded
settlements now arrive HOUSED (round(400/6) = 67 dwellings × 6 = 402 capacity → clamps to
exactly 1.00). The old 0.00 was the flow stand-in's phantom (an empty timber store read as
homelessness on founding day). Pin updated with the reasoning; Comfort's 0.00 stands (flow
stand-in retained this packet). NOTE the count context: this branch forked from 33ebcf5,
before T3.9ab's 12 UI tests — 127 here vs 139 on merged main is the fork, not a loss; the
merge will reconcile.

**Bench (Release, seed 42), both trees, same box — the per-source Partition's cost
measured against main, not asserted:**

| phase | main @ d51327e | t3.8 tip | delta |
| --- | --- | --- | --- |
| catchment | 20,698 ms / 7.52 GB alloc | 21,095 ms / 7.91 GB | **+1.9% / +5.2%** |
| pathbuild | 18,934 ms | 19,011 ms | +0.4% |
| housing | — | **61 ms / 348 KB** | new |
| demographics | 959 ms | 1,038 ms | +8% (housing world diff) |
| TOTAL (300 founded turns) | 41,375 ms | 41,944 ms | **+1.4%** |

Catchment was already the heavy phase before this packet (pathbuild bumps the revision most
turns, so D-016 recomputes are frequent on this run); the per-source rewrite adds ~2% time
and ~5% allocation to it (the per-source dist/closed scratch). ~140 ms/turn average sits
far inside the Spine's ≤5 s/turn early-game budget; the toy bench runs 15.9 ms for 200
turns. The 1024² single-recompute measurement (3.0 ms, CatchmentTests print) stands.

## THE SIX-COLUMN LENS TABLE — reconciled BY NAME against docs/t3.8-lens-manifest.md

| lens | status | raised | confirmed | disposition |
| --- | --- | --- | --- | --- |
| `derivation-honesty` | RUN | 2 | 2 | Rates committed BEFORE measurement (5a70ace spec → 50d6127 code, commit order is the evidence) and NOT adjusted after the H-readings — proven the hard way: H1/H2 fired their failure branch and every rate still stands untouched. CONFIRMED FINDING against this packet's own derivation: the build-mix clay arm was checked against the wrong denominator (production, not the post-crafting residual — measured 0). Repair deferred to the director's ruling; options enumerated without recommendation. |
| `geometry-and-corridors` | RUN | 3 | 3 | (1) The #11 SUSPECT was real: single-label Partition truncates under unequal budgets — 13/14 probed pairs diverge; replaced with per-source-exact, pinned by PartitionUnequalBudgetTests, witnesses upgraded to full-lattice tier budgets. (2) Density: calibration corridor GREEN on the full suite — the band moved in NEITHER direction; H3's decomposition trigger did not fire at corridor scale. (3) The size-tier bonus is INERT on the canonical trajectory (tier 0 once the stock collapses) — folded into the after-column finding. No band edge moved. |
| `conservation-and-accounting` | RUN | 0 | 0 | Materials sunk exactly through Ledger (mechanism tests assert the draws to the unit); dwellings carry their own conserved id with the ledger-identity test (sourced − sunk == stock, exact, no epsilon); the labor split MEASURED by the founded twins' bit asserts, not assumed; the <1-unit/turn maintenance dust documented as deliberately unbanked at the draw site. FirstReign's per-turn ConservationAuditor asserts ran green through the re-pin. |
| `test-power` | RUN | 2 | 2 | Sweep: bounds pre-committed (f316cd1), 5/5 mutants KILLED by their pre-committed semantic killers, ZERO golden-only kills, all runs inside the bound. The rebind mutant's killer is the one-turn-stop assertion, as the manifest required — not a golden. Guards: three measured reds; red 2's failure MODE recorded as its own finding (the surviving guard masks the vocabulary deletion; the message asserts are load-bearing). |
| `scope-and-fence` | RUN | 0 | 0 | No tiers (Q-D filed), no store bounding, no transport/water/draught/route, no merchants, no trade change, no band edge. needs.json minimal-diff (25-line line-wise removal, T3.5b Q5 convention). Goldens re-pinned ONCE, itemized (ca29b85), provenance preserved. Comfort: finding-and-stop, stated (Item 3 verdict) — never a silent widening. The one out-of-enumeration repair (the HUD Shelter pin) is fallout of the rebind, test-only, documented above. |

Every manifest name has a result; none is blocking.

# FIX PASS (certification withheld — director ruling; the T3.3/T3.4/T3.4c review-fix precedent)

## The reframing, adopted and verified against the tree

T3.8 did not break the world — it DISCOVERED A DEFECT PRESENT SINCE T3.3. Verified:
ProductionSystem publishes recipe input demand "from labor alone, BEFORE any input cap
binds" (the comment stands verbatim at ProductionSystem.cs:354-359), and the production
semantics are produce-to-LABOR-CAPACITY capped by input stocks, never by demand — pinned by
T3.3's own tests (Crafting_OutputIsCappedByTheScarcestInput_AndConsumesIt;
Crafting_ScalesWithLabor_UntilInputsBind). Measured at t29: 122 pottery crafted against 97
demanded, consuming all 244 clay. That behaviour predates this packet by two milestones;
nothing noticed because nothing else wanted clay. Housing is the second consumer and the
first to expose it. THE FINDING IS AGAINST T3.3'S PRODUCTION SEMANTICS, NOT AGAINST T3.8'S
MECHANISM.

## ITEM 1 — which repair: MEASURED, not picked

The four options sorted by the separating criterion (derivation error vs mechanism change):
(a) housing-priority draw = NEW MECHANISM (allocation rule). (b) demand-capped crafting =
B-2-adjacent — produce-to-capacity is the same unbounded-accumulation root as the granary;
M4 material, out of scope here by the fence (and by reasoning: bounding crafting is store
bounding's twin — it changes every settlement's goods trajectory, exactly the class of
mechanism B-2 stages for M4). (c) re-derive the maintenance material = DERIVATION ERROR, if
the reference class says so. (d) founding clay endowment = masks the measured zero residual
without fixing it (the endowment drains and the collapse returns on a delay).

### (c)'s two readings, PRE-COMMITTED before the reference class is researched

- **R-YES (derivation error):** the reference class says neolithic dwelling maintenance
  and construction used locally dug earthen material (subsoil daub, mudbrick earth) that
  did NOT pass through the selected, processed material stream ceramics used — i.e. the
  build-mix's "clay" arm named the WRONG REGISTRY GOOD (the registry's clay is a
  deposit-extracted riverine raw, the pottery input). Then the fix is a CORRECTED
  DERIVATION: the earthen arm becomes a ubiquitous non-good (like water — present, not
  modeled, stated), the clay draw rates go to zero IN DATA with the corrected provenance
  doc, and NO mechanism changes (the Leontief's clay arm reads fill 1.0 at zero demand by
  the existing demandC > 0 branch). The mechanism keeps its full generality; only the
  canonical data stops naming the wrong good.
- **R-NO (not a derivation error):** the reference class says dwelling maintenance
  genuinely drew on the same selected clay stream as ceramics (shared pits, shared
  processing). Then (c) fails, the finding stands as a mechanism-level allocation question,
  and per the ruling this pass REPORTS AND STOPS — (a) is not reached for.

Decision rule, stated before the research: the class decides; the project's preference for
a corrected derivation over an added mechanism (law 2) breaks ties only if the class is
genuinely ambiguous, and ambiguity is reported as such.

### The reference class, researched AFTER the pre-commitment (commit order: c30378c → this)

Structural earth and ceramic clay are DISTINCT material streams in the neolithic reference
class. Daub (wattle-and-daub) and mudbrick use locally dug subsoil tempered with straw and
dung — borrow pits immediately beside the houses are a standard excavated feature of LBK
and Near Eastern settlements; the material is unselected, unprocessed, and effectively
ubiquitous at any settlement site. Potter's clay is a SELECTED material from specific beds
(riverine/lacustrine), commonly levigated, procured as a distinct activity — which is
exactly what the goods registry models: clay is `category: raw, depositChannel: water`, the
deposit-extracted pottery input. Dwelling maintenance did not compete with ceramics for the
same input. **R-YES FIRES: the build-mix's clay arm named the wrong registry good — a
derivation error, and the fix is a corrected derivation with NO mechanism change.**

Applied (c39fab5): sim.json housing.upkeepClayPerDwellingYear 0.025 → 0.0 and
buildClayPerDwelling 1.0 → 0.0, with the corrected provenance _doc (structural earth is a
non-good, like water — present, not modeled, not drawn; timber remains the only drawn
material: frame, rethatch, member replacement). The loader's RequireRate guard accepts 0
by its existing contract (finite, ≥ 0) — NO guard was modified, so no §7.17 red-proof
obligations arise. The mechanism keeps its full Leontief generality; the five mechanism
tests that exercise the clay arm moved to an explicit two-material rigged config
(data rigging, constitutional), and the canonical-behaviour tests stay on canonical data.
Repair-touched group: 70/70 green.

## ITEM 2 — H1/H2 RE-MEASURED on the repaired tree (same rig, same instrument)

| reading | repaired tree | [collapsed run] | [flow stand-in] |
| --- | --- | --- | --- |
| Mothian t29 — Shelter | **1.0000** | [0.0000] | [0.8800] |
| Mothian t29 — grievance | **10.85** | [221.98] | [33.07] |
| Mothian t29 — dwellings / m / tier | **116 / 1.0000 / TIER 4** | [0 / — / 0] | [n/a] |
| Mothian t29 — catchNodes / arable | **30 / 4769.2** | [24 / 3973.0] | [n/a] |
| Hikiavur t177 — Shelter | **1.0000** | [0.0000] | [0.0000] |
| Hikiavur t177 — grievance | **132.46** | [119.02] | [227.49] |
| Hikiavur t177 — dwellings / m / tier | 91 / 1.0000 / 4 | [0] | [n/a] |
| Hikiavur t177 — Comfort | 0.0000 | [0.0000] | [0.0000] |
| Mothian t177 — Shelter / dwellings | 0.0000 / 0 | [0.0000 / 0] | [0.0000] |

**Which branch fired, quoted against the pre-committed text:**

- **H1** ("after 1 turn ≈ 0.78 — never 0; 5 turns ≈ 0.29; 20 turns ≈ 0.007; discharge:
  gradual degradation on that curve"): DISCHARGED AT THE MECHANISM and SUPERSEDED AT THE
  WORLD. The curve itself is pinned bit-exact by the mechanism tests (0.78 measured at one
  stopped turn with zero materials — OneTurnStop). On the canonical world the curve's
  premise — that stopping construction stops maintenance — does not hold: maintenance
  draws from the timber STORE, so a farm-100% settlement keeps m = 1.0 for decades after
  extraction stops (Hikiavur: 30 years after the t174 order, dwellings 91, m 1.0, timber
  store 755 covering ~16 more turns). Degradation is gradual and engages exactly when the
  store genuinely runs out — Mothian's t177 state (pop 65, timber 0, dwellings 0) is the
  session's engineered starvation read honestly: a settlement that starved for 150 years
  has ruins.
- **H2** ("Shelter ~0.5–0.6 instead of 0.00; grievance falls to tens, within ~2× the
  Mothian-style reading"): PARTIALLY DISCHARGED, both misses explained by measurement.
  Shelter OVERSHOT the reading (1.0000, not 0.5–0.6) for H1's reason — the pre-commitment
  assumed 30 years unmaintained; the store maintained. Grievance fell 227.49 → 132.46
  (−42%) but not to tens: the residual accruer is COMFORT (0.0000 both classes — pottery
  and cloth flows dead under farm 100%), which kept the flow stand-in BY THIS PACKET'S OWN
  ITEM 3 VERDICT. The healthy-mix settlement DID land in tens (Mothian t29: 10.85). The
  Tier A gate no longer fires on Shelter anywhere housing stands. Prohibitions untouched.
- **H3** ("if density moves, DECOMPOSE population vs arable before interpreting; the band
  moves in NEITHER direction"): THE TRIGGER NOW FIRES and the decomposition is done before
  interpretation — the FIFTH instance of the pattern, as the fix-pass ruling predicted.
  Mothian t29, collapsed run vs repaired run (same world, same orders, only the clay rates
  differ): population 472 vs 477 (−1%, rounding-scale demographic drift); arable 3973.0 →
  4769.2 (+20%), PURE DENOMINATOR — the tier-4 budget (base × 1.10) grows the catchment
  24 → 30 nodes. The corridor question is answered by the full-suite calibration battery
  on this tree (counts in the verification section); no band edge was touched either way.

## ITEM 3 — the SECOND golden re-pin, authorized, itemized, cause loud

- **Toy golden: DID NOT MOVE** (b4d523f6… unchanged — config values are not state; the toy
  preset runs no housing). No re-pin.
- **Founded golden → b9f93d4a…** with the history entry stating plainly that the defective
  v19 pin (8473c021…) captured the collapsed-housing world and must not be read as a good
  state. ci.yml FOUNDED_GOLDEN updated in the same commit.
- **FirstReign → v15 (144d7e5d…)** with the same defective-v14 statement; shape asserts
  re-verified against the new trajectory (extinction inside (5, 25], dead world frozen, no
  food mountain).

## FIX-PASS VERIFICATION + the ONE red, diagnosed and ESCALATED per H3 (not repaired into)

Full suite on the repaired tree: **Sim.Tests 428 / 1 failed / 4 skipped of 433 (23 m 34 s);
Sim.Ui.Tests 127/127.** The single red, transcribed:

> `Canonical_FedCorridors_AllInBand(seed: 1)`:
> `canonical.densityPerArableKm2: 1.53756 has fallen below the recorded deviation window -
> if it is back inside the corridor [0.15, 0.6], CR-002/CR-003 are resolved: delete this
> quarantine and restore the plain AssertInBand.`

Diagnosis, complete and decomposed (the H3 pre-commitment executed): this is the CR-002/
CR-003 DENSITY QUARANTINE — the known deviation pinned per-seed ABOVE the corridor
[0.15, 0.6] (T3.4c protocol: pinned values, loud drift, deliberate re-pin under a ruling,
never a tolerance widen). The tier-4 catchment bonus grows seed 1's arable denominator by
the measured +20%, so the quarantined density falls 1.537 × 1.2 ≈ 1.84 → 1.53756 — BELOW
the seed's recorded window floor but still ABOVE the true corridor's 0.6 ceiling. The
movement is TOWARD the corridor, population-side unchanged (−1%), pure denominator — the
fifth-instance decomposition already recorded above. The other canonical seed held.
CORRECTION (transcription, caught at the re-pin): the canonical corridor seeds are 1 and
2, not "seed 42" as this paragraph first said — the passing case was SEED 2 (measured
1.60184 at the re-pin, inside the old floor by 0.05), and the deviation window is ONE
SHARED envelope, not per-seed pins. The handback prose carried the same error; this is the
corrected truth, and the re-pin below is sized to BOTH seeds' measured values.

RE-PIN EXECUTED under the certification ruling: window [1.55, 1.95] → [1.40, 1.80] around
the measured pair 1.53756 / 1.60184 with the predecessor's modest margins; the three-part
history (decomposition-as-the-point, direction + CR-002 Option B cross-reference in both
directions, lift condition unmet) lives at the pin site in CalibrationBatteryTests, in
cr-002.md §6, and B-2's third-costume instance + the extended Q-B one-test-three-
predictions hypothesis are filed in m4-blocking-material.md and queue.md.

Per H3's pre-committed rule ("outside-the-band is reported and ESCALATED as CR-002 did")
and T3.4c's re-pin-under-a-ruling protocol, this pass REPORTS AND STOPS: the quarantine
pin was not touched, no tolerance moved, no band edge moved. The decision offered for the
ruling: a deliberate per-seed re-pin of the quarantine window under the fix pass's cause
(the Item 4 bonus moving density toward the CR-002/CR-003 target — arguably progress on
the quarantine's own resolution condition), versus any other reading the director prefers.
Everything else in both suites is green.

### #1/#2/#3 — re-pinned ONCE, itemized (the enumeration closes)

Mechanism tests first (HousingSystemTests, 8/8 green: exact upkeep draw, exact-exponent
decay with banked remainder, Leontief scarcer-material rule scaling the exponent,
surplus-target cap from both sides, per-cap binding, PathBuild labor-split twins with bit
asserts and the floor, ledger conservation identity, bounded dt drift) — then the pins:

- **Toy golden → v19** (SCHEMA-ONLY for the preset: Housing's zero count prefix; no
  CatchmentSummary rows to widen; toy pipeline runs no housing):
  b4d523f6f1e0cc1a66d94393cf86368db7063acde27ca24bf17555e772883525.
- **Founded golden → v19** (schema + behavior — every count moves):
  8473c02161ff4051457d13521e7dc0edbaf7078240cc2da3cad33d38476544c4. ci.yml's
  FOUNDED_GOLDEN updated in the SAME commit (the update-together rule in ci.yml's own
  comment).
- **FirstReign → v14** (the N = 1 famine world founds housed, maintenance draws, labor
  splits): 2d6f9f45f9ec5097efbca0063284f0325352dbea40dd3df4b246a0c316f7b4e5. The SHAPE
  asserts ran against the new trajectory and passed — extinction inside (5, 25], dead
  world frozen, no food mountain — which is what separates this re-mint from a blind
  re-pin. Verification run: 4/4 green (both FirstReign tests + both goldens), 2 m 37 s.
