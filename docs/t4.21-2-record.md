# T4.21-2 — BOUNDED MIGRATION: implementation record (measured on branch `t4.21-2-migration`)

Packet: `docs/t4.21-architecture.md` §3.4, §3.4.4, §3.5, §3.11 (the `Plan` static), §6.3, §4 row T4.21-2;
ADR-025. Base: `1735d41` (integration branch `claude/civdemo-work-b1z2y4` + T4.21-1b). Every number
below was measured by the agent writing this record, on this tree, with `dotnet test -c Release`.

## 1. What shipped, commit by commit

| commit | step | content |
| --- | --- | --- |
| `517cfdb` | 1 | `MigrationSystem.Plan(prev, cfg, dt)` — pure planner; `Step = Plan + transfer loop`; BIT-IDENTICAL (all four goldens and the whole suite unchanged: 824 / 2 inherited reds / 6 skipped) |
| `0fbab3f` | 2–5 | ω, Z, shares, exact hazard φ on nominal d, readout value (ω := 1), basin caps both ends, vacancy bound, `BasketBook` in the ctor, §6.3 tests, dispositions, `MigrationExplanation` text, header doc |
| `134747f` | 7 | goldens re-pinned by measurement, measured-turn pins re-derived, `IntegratedPinAttribution` constants re-measured |
| `6b2f155` | — | two `queue.md` lines (measured findings) |

## 2. Goldens (behaviour), measured old → new

| pin | old | new |
| --- | --- | --- |
| `FoundedGolden_Seed42Turn300` (= `ci.yml FOUNDED_GOLDEN`) | `008aa28c…` | `d45f14d3b83601ec1a3507762a621f6a599a2926ecd7221998e7254150f018a1` — derived twice (in-test harness and `sim run --founded --seed 42 --turns 300`) |
| `DrivenGolden_Seed42Turn300` | `73009964…` | `297b432ccd4eff0257c86a89913bd9c2b0996e5fb3ff3c0aeba5ae30072b4839` |
| FirstReign turn 40 | `125e0186…` | `86adfd85673b27b74901162425c3404263ca3f485c7f4f9ebc200081b3e063f8` |
| `GoldenHash_Seed42Turn200` | `b6df7edd…` | **UNMOVED** (terrain-less: no distances ⇒ no migration) — the no-unrelated-movement control; its v22/v24 strips `0f94b4ad…` / `eec82711…` unmoved |

`IntegratedPinAttributionTests` constants re-measured (T4.10/T4.19 precedent; the T4.21-1 "layout alone"
claim is historical from `134747f`, the strips prove separability): founded v22 `f886efbd…→05eb271c…`,
v23 `e48d9bcd…→f49815f0…`, v24 `917993b2…→7ee73714…`; FirstReign v22 `69d6cf17…→7cb35cbd…`, v23
`4e7d2e69…→e877c796…`, v24 `5ee8119e…→8e23b4b3…`; driven `CapacityFloorFixAtSchemaV22`
`60bd5b20…→789585be…`, v23 `cf93e0fe…→bdf29a88…`, v24 `76f82629…→cee0c2c0…`.

## 3. Measured on the founded seed-42 world (Plan diagnostics over 300 turns)

- Basin caps (§3.5b) bite in 8–12 of 12 settlements per early turn (t3–t12), 4–11 mid-run, 1–5 late;
  pair-capped gap inflow 14 863 vs 5 064 planned / 4 941 moved over 300 turns (≈ 3× reduction).
- Vacancy bound (§3.5c): bites in ALL 12 settlements on turn 2 (turn 1 is the zero-harvest endowment
  turn ⇒ `N_lim = 0` ⇒ `V = 0` for that one turn — migration's first turn moves 2 → 3 in the founded
  and the driven world) and on one settlement-turn afterwards (t9). Elsewhere `vacScale = 1.0` literal.
- Famine flight: `d > 0` on one settlement-turn (t6) in 300 turns; planned flight 0.1 heads.
- Downstream measured-turn moves: first starvation 55 → 57 (founded); first dwelling decay 48 → 64
  (driven); first merchant latch at t124 (2 towns), receded by 650 (episodic by design);
  `ExplainRigs.Starved` deficit at turn 3: 0.7786416647610336 → 0.76919291338582674, grain demand
  3937 → 3657 (eaten 531 unchanged); `Dev_MalthusCorridors_AllInBand(seed 42)` now PASSES (0
  starvation deaths in the dev horizon); seed 7 stays red (inherited).

## 4. Test dispositions (spec §6.3 and the survey notes §(c))

New file `Sim.Tests/Systems/MigrationBoundedTests.cs` — every §6.3 row: `M_Flight_BoundedPerTurn`,
`M_DestinationCount_DoesNotMultiplyQuantity`, `M_EmptyRuin_StillRefused`,
`M_VacantViableDestination_AcceptsUpToCap`, `M_Basin_FanIn_Flight_BoundedByVacancy`,
`M_Basin_FanIn_AggregateInflowBounded` (+ `M_Basin_FanIn_SingleSource_BitIdenticalToPairCap`),
`M_Basin_FanOut_AggregateOutflowBounded`, `M_NoViableExit_NobodyLeaves`,
`M_SmallSettlement_FlooringAndRemainders` (3/7/20), `M_Readout_ValueTurnExact` (0.40 / 0.15),
`M_Flight_DtComposes`, `M_Recovery_FlowsStopWhenDeficitClears`, `M_Plan_EqualsStep`.
Re-rigged by measurement, aim kept: `Overdraw_ScalesProportionally_NoFirstDestinationGrab` (shares ∝
damping × viability, moved == floor(1000 φ)); `FamineFlight_FiresWithZeroGap` (+ bounded assertion);
`Colonization_PartyNeverEmptiesSource` (new, turn-exact); `ClassSystemTests.ArtisanPresence_…` (seed
16 → 3 after re-measuring dev seeds 1–24 — table in the test); `MerchantTests.MerchantsEmerge…`
("ever latched within 650", first latch t124); `WorldReconciliationTests` (migration first on turn 3;
starvation 57; decay 64); `ExplainGrievanceTests` / `ExplainRecomputedFunctionTests` value re-pins.
SKIPPED and left skipped (T4.21-4 lifts them): `MigrationTests.cs` `FamineAtOneOfTwelve_…` and
`MagnitudeCorridor_FedPhaseDrift_WithTeeth`. `MigrationStabilityTests` (Bifurcation / Occupancy),
`CollapseStabilityTests`, `MigrationConcentrationTests`, `HappinessMigrationTests`,
`DemographyRetuneTests`, `ExplainHappinessMigrationTests` (PushDeficit == prev DeficitRatio,
"gap-INDEPENDENT") all green unchanged.

## 5. Mutant kill record (full Release suite per mutant, reverted after each; clean run 421 s ⇒ bound 1260 s)

Each mutant is a one-line change to `MigrationSystem.cs`, applied, built, run through the FULL Release
suite (bound 1260 s = 3× the 421 s clean run of `134747f`), reverted. The machine was shared with
another lane building and testing concurrently (load average 20–30 on 4 CPUs during the FANIN/FANOUT
runs, 1–13 during the later ones), so two full runs were cut by the bound while visibly progressing
(results were still arriving — NOT a non-termination); for those the semantic kill is recorded from
(a) the tests that completed before the cut and (b) a TARGETED run of the migration classes
(`MigrationBoundedTests`, `MigrationTests`, `MigrationStabilityTests`, `ColonizationTests`,
`CollapseStabilityTests`, `HappinessMigrationTests`) against the same mutant binaries. Every mutant
is killed by at least one SEMANTIC test (goldens listed for completeness only), and every killing
test asserts a property the system ought to have (ADR-015 §7.2, stated per row).

| mutant | change | semantic killers (aim) | full-suite record |
| --- | --- | --- | --- |
| **M-MIG-CAP** | `FlightFractionOf` returns the Euler `profile·K·ω·d·dt` | `M_Flight_BoundedPerTurn` (a decade cannot empty a bucket — 909 of 1000, scaler at 1.0), `M_Readout_ValueTurnExact` ×2 (readout is the bounded hazard), `M_Flight_DtComposes` (exact integration), `M_SmallSettlement_FlooringAndRemainders` (7, 20), `Overdraw_ScalesProportionally` re-rig (moved == floor(1000 φ)), `Colonization_PartyNeverEmptiesSource` | completed, 412 s: 21 failed (the 8 above + goldens/attribution/measured-turn pins) |
| **M-MIG-SUMNOTMAX** | `omega[src] = z` (Σ instead of max) | `M_DestinationCount_DoesNotMultiplyQuantity` (HOW MANY never depends on the number of exits), `Overdraw_ScalesProportionally` re-rig (φ on the best exit) | completed, 504 s: 12 failed |
| **M-MIG-FANIN** | `destScale[dst] = 1.0` | `M_Basin_FanIn_AggregateInflowBounded` (four sources may not refill a destination past f·M*_j^in), `M_Plan_EqualsStep` (planner totals ≠ executed) | run 1 cut at 1260 s under load ~28 with 11 results (both killers among them); run 2 completed, 765 s: 13 failed |
| **M-MIG-FANOUT** | `srcScale[src] = 1.0` | `M_Basin_FanOut_AggregateOutflowBounded` (a source may not drain past f·M*_i^out into many destinations) | targeted run (104 s): 1 failed = the killer; full run cut at 1260 s under load ~29 with 10 results (goldens/merchant/pins) — progressing, not hung |
| **M-MIG-VACANCY** | `vacScale[dst] = 1.0` | `M_VacantViableDestination_AcceptsUpToCap` (35 of 909 at V = 70), `M_Basin_FanIn_Flight_BoundedByVacancy` (four famine sources bounded by one destination's vacancy), `M_NoViableExit_NobodyLeaves` (V = 0 ⇒ nobody moves, nobody founds), `M_Recovery_FlowsStopWhenDeficitClears`, `M_Plan_EqualsStep` | completed, 621 s: 18 failed |

Records: `scratchpad/t421/mutant-*.txt` (+ `.full`, `.targeted`; not committed — the numbers above are
what they say).

## 6. Bench (hot path touched: `MigrationSystem.Step`)

`sim bench --seed 42 --turns 300 --founded`, same machine, base `1735d41` vs this tree: migration phase
95.6 ms → 158.5 ms over 300 turns (+0.2 ms per turn, 0.6 % of a 25–27 s run), allocations 47.3 MB →
55.6 MB (the per-step `MigrationPlan` arrays: n×n doubles and per-bucket arrays); total run 25.5 s vs
27.0 s (within the load noise of a shared machine). No hot-path concern.

## 7. Gates on the final tree

`check-banned-constructs.sh` OK · `check-read-isolation.sh` OK · `check-readonly-proof.sh` OK ·
`dotnet build -c Release` 0 warnings · full suite on `134747f`: 843 passed / 1 failed
(`Dev_MalthusCorridors_AllInBand(seed 7)`, inherited) / 6 skipped, 6 m 21 s.

## 8. Deviations from the spec text, stated

1. **Source-basin outflow is the DEST-SCALED outflow** (`Σ_j gapScale_ij × destScale_j × gapDesire_ij`),
   i.e. the scale chain is sequential (pair → destination basin → source basin → vacancy → overdraw as
   the spec pins the ORDER); the spec's `outflow_i` is not written out. A source already held back by its
   destinations' basins is not limited twice for the same people. `M_Basin_FanOut_…` has single-source
   destinations (destScale 1.0), so it reads the same under either form.
2. **`M_VacantViableDestination_AcceptsUpToCap` second turn**: the spec's clause "a second turn accepts more
   (labour grew)" needs Production; in the migration-only rig N_lim is HELD and the second turn accepts
   what the shrunken vacancy allows (V = 80 − 45 = 35 ⇒ cap 17.5 ⇒ 17), which is what is asserted.
3. **Flight weight association**: `flight = φ_b × count_b × (w_ij × vacScale_j)` (the vacancy factor
   folded into the pair weight, multiplied only when < 1) rather than `φ × count × w × vac`; no pinned
   number depends on the association and the test expectations multiply the plan's own factors.
4. **"Green" now means ONE inherited red**: `Dev_MalthusCorridors_AllInBand(seed 42)` passes on this tree
   (no starvation deaths in the dev horizon under bounded migration); the ruling's quarantine is
   untouched. Reported, not acted on.

---

## 9. T4.21-4 rule applications on the merged tree (§3 findings (A) and (B), ruled)

The two measured findings §3 recorded and did not act on were put to the orchestrator's
pre-registered rules. Both were re-measured on the merged tree by the applying agent (ADR-015 §6:
a number written here was measured by whoever wrote it).

### 9.1 Finding (A) — the source-basin (fan-out) cap binds in the fed world: **RULE 1 ⇒ STAYS**

Instrument: `sim autoplay --seeds 1 --seed-base 42 --turns 300 --metrics OUT.json`; gross migration
per decade = `Σ_{t=2..300} migrationGross / Σ_{t=2..300} population × dtYears × 10`
(`CalibrationAnalysis.MigrationGrossPerDecade` restricted to turns 2..300 — turn 1 moves nobody in
every arm, so this differs from the JSON's own `derived.migrationGrossPerDecade` only in the 4th
significant digit).

| arm | commit | gross/decade | first migration turn | world hash |
| --- | --- | --- | --- | --- |
| (a) pre-packet | `1735d41` | 0.001509 | 2 | `008aa28ceeb73659…` |
| (b) merged | `a621c86` ≡ `8a945f3` | 0.001021 | 3 | `db7c7a0907ad4335…` |

RULE 1's test: `0.001021 ≥ 0.4 × 0.001509 = 0.000604` (ratio **0.677**) **and**
`0.001021 ∈ [0.001, 0.01]`. Both hold ⇒ **the source basin cap STAYS as shipped.** No mechanism
changed; `M_Basin_FanOut_AggregateOutflowBounded`, `M_Basin_FanIn_AggregateInflowBounded`,
`M_Basin_FanIn_Flight_BoundedByVacancy` and the **M-MIG-FANOUT** / **M-MIG-FANIN** mutants are
untouched and remain meaningful. §3's fed-world magnitude is the specified physics, **measured and
accepted**; ADR-025 §2.3a carries the record.

Recorded from the T4.21-4 diagnostic lane's arms (its measurement, not this agent's — four divergent
trajectories, not a counterfactual on one world): disabling the source cap moves canonical gross
migration to 0.000922 (*down* 9.7 %) and disabling the destination cap to 0.000939, so **neither
basin cap is what moved (a) → (b)**; the source cap's evidence is per-turn, not 300-turn (driven
seed 42: max inflow fraction 0.1754 → 0.1987 with it off, settlement 9's t3 outflow fraction
0.0965 → 0.1834); and §3's ~3× headline is mostly the **destination** stage (source binds 13.8 % of
settlement-turns, destination 37.4 %). Where the residual (a) → (b) cut actually sits — vacancy
bound, bounded flight, the exit-openness/exact-hazard rewrite — RULE 1 does not decide, and it is
escalated to the director rather than settled here.

### 9.2 Finding (B) — the turn-2 vacancy refusal: **RULE 2 applied, and it does NOT close it**

`FoodHeadroom.Limit`'s null arm now includes **catchment row absence** (ADR-026 §2.1a), keyed on row
absence only, with `H_NullArm_NoCatchmentRow_IsPositiveInfinity_ButAbandonedWithARowIsZero` pinning
both arms. Measured on the merged tree, the change is a **bit-exact no-op**:

- founded seed 42, 300 turns: `db7c7a0907ad43353b1a44f1a957a407c0ce89ecbb2bf105b170b4b316cc82d9`
  before and after (the `ci.yml` `FOUNDED_GOLDEN`, reproduced via the ci step's own
  `sim run --founded --seed 42 --turns 300 --hash-log`); gross/decade 0.001021 both ways;
  `migrationGross[1..6] = [0, 0, 252, 215, 149, 110]` both ways; **first migration turn 3 both ways**;
- driven world (ci order corpus, 300 turns): hash log **byte-identical every turn**.

RULE 2's stated consequence — "the first migration turn returns to 2 on founded and driven seed 42" —
**does not follow from the key it specifies**. Catchment is pipeline entry 1 and writes into NEXT, so
the turn-1 world (PREV on turn 2) already carries a row for every founded settlement; the turn-2
refusal comes from that row being PRESENT alongside the turn-1 zero staple harvest, which the rule
explicitly forbids keying on ("never on `production == 0`"). The canonical world founds no colonies
over 300 turns, so the arm never fires there at all. The rule's mechanism was applied as written and
its prediction was measured false; no substitute key was designed in its place. The queue line stays
OPEN with this measurement attached — see ADR-025 §2.4a for the residual and the two ways to close it.
