# T4.12 — MIGRATION WEIGHT / REFERENCE CLASS: DERIVATION RECORD

**STOPPED AT PHASE 3. No production code, test, config or golden was changed; tree verified
byte-identical to `40afe7b`.** Baseline for this work is T4.10 Option A (`t4.10-migrated-work`
@ `40afe7b`, pushed, NOT merged, PENDING REVIEW). Current `main` is `ba96b1c`.

**READ IN ORDER — this record has two parts.** (1) The derivation investigation below, which stopped
because no unique `LandWeight` is derivable. (2) **The DIRECTOR RULING section at the end**, which
rules `LandWeight = 1.0` as a semantic normalization and reports the measurements taken against it —
including the finding that the naive form of that ruling is **not** behaviour-neutral. Both parts
changed nothing in production; the second is the current state.

The packet asked: what should `AttractivenessLandWeight` (currently `0.078125 = 5/64`) MEAN, now that
T4.10 has removed the food term and `R_i = LandWeight × arableKm2_i` is the whole of attractiveness?

The answer this packet reached is that **no unique value is derivable**, for a reason that is
structural rather than a gap in the evidence, and that reason is itself the finding. Four independent
lines converge on it. Three STOP conditions named in the packet are met (1, 2, 8), and a fourth (9)
was already on file before this packet opened.

---

## 1. THE PRESCRIBED METHOD'S DENOMINATOR NO LONGER EXISTS

`docs/t3.4c-remeasurement.md:162` heads the prescription **"What a sound design point would fix
instead — PROPOSED, NOT APPLIED"**. Two things follow from reading it exactly:

- It **rules out** the method that produced the current value: *"A dispersion balance cannot be the
  design point, because dispersion is not a model constant"* (`:164`). That prohibition is standing.
- It **prescribes** (`:165-170`): *"name a reference fed state (a stated food-per-capita and
  arable-per-capita, from a reference class rather than from a measurement of the sim) and set
  `landWeight / foodWeight` so the two terms are equal there."*

**The prescription is a RATIO, and T4.10 deleted its denominator.** `foodWeight` no longer exists,
so "set `landWeight / foodWeight` so the two terms are equal" has no second term to equalise against.
The method is not merely unapplied here — as written it is **inapplicable** to the post-T4.10 model.
It also names no reference class; the class is left open.

This is not a reason to improvise a substitute. `docs/t4.1f-review-record.md:62-71` records what
happened the last time a landWeight was derived without a ratified basis: computed 0.088286, shipped
0.0859375, then **REVERTED** by director ruling — *"The derivation is sound but it is a TUNE change
carried by a packet whose purpose it does not serve, and a +10 % weight landing on main unexplained
is a constant nobody can attribute later."* It had also been fitted from a sim measurement, which is
precisely what T3.4c forbids.

## 2. MEASURED — THE LAND SIGNAL IS NEARLY UNIFORM

Canonical world, seeds 1/2/3, turns 100/300/650, all 12 settlements (temporary probe, deleted; tree
verified clean afterwards):

| seed / turn | arable per capita: mean · median · min–max | spread | max smoothed-attractiveness gap |
| --- | --- | --- | --- |
| 1 / 100 | 9.5942 · 9.5489 · 9.4449–9.9464 | **1.053×** | 0.0451 |
| 1 / 300 | 2.6411 · 2.6102 · 2.5983–2.7215 | **1.047×** | 0.00977 |
| 1 / 650 | 0.7036 · 0.7012 · 0.6995–0.7115 | **1.017×** | 0.000958 |
| 2 / 650 | 0.6819 · 0.6819 · 0.6817–0.6823 | **1.001×** | **0.0000297** |
| 3 / 100 | 12.5905 · 12.6190 · 12.4456–12.7997 | **1.028×** | 0.0331 |
| 3 / 650 | 0.9407 · 0.9416 · 0.9337–0.9458 | **1.013×** | 0.00102 |

Population-weighted and unweighted means agree to four figures throughout, i.e. the distribution is
tight, not skewed.

**T3.4b derived the current weight against a 26× arable-per-capita spread** (`sim.json`'s
`_docLandWeight`: *"arable/capita runs 0.162–4.698 (a 26x spread)"*, dev world, turn 120). The
canonical world's spread is **1.001×–1.053×**. The land signal is now as flat as the food signal that
same derivation condemned — *"the land signal was effectively dead and migration was reading noise on
a saturated stock"*. The condemnation now applies to land.

This is not a new hypothesis: `docs/queue.md:807-810` already carries it as **REAL** — *"packing
shrinks partitioned catchments, the land term shrinks, attractiveness gaps narrow, T2.8's gap-closing
cap binds, and the migration rate lever loses its teeth."* The measurement above is that item,
quantified.

## 3. MEASURED — THE WEIGHT IS FALSIFIABLE, SO STOP CONDITIONS 3 AND 4 DO **NOT** FIRE

Stated plainly because the packet asked for it specifically, and because the answer is the opposite
of T4.10's food finding. Canonical, 650 turns, scaling `AttractivenessLandWeight` alone:

| seed | ×0.25 | ×1 | ×4 |
| --- | --- | --- | --- |
| 1 | 0.00014170 | 0.00019344 | 0.00023326 |
| 2 | 0.00018971 | 0.00023008 | (not run — see below) |

Monotone and material. **`LandWeight` is falsifiable and does not cancel from every observable.**
Cap-binding is a minority regime, which is why: of 132 ordered pairs, ~66 carry a positive gap at
all, and of those the cap binds on 2–30 (3–45 % of live pairs, varying by seed and turn). The
majority of live pairs are sub-cap, and in the sub-cap regime flow is linear in the weight. This
also agrees with the historical instrumentation (`docs/t3.4b-review-record.md:186`: 63 % of pairs at
the shipped rate; `docs/t4.1g-review-record.md:11-14`: 36.4 % at ×1).

## 4. THE FINDING — `LandWeight` IS REDUNDANT WITH `BaseRatePerYear`

This is the reason a unique value cannot be derived, and it is a consequence of T4.10 rather than a
pre-existing defect.

**Algebraically.** Write `K = Σ_buckets BaseRate × profile × count × dt × damping × viability`. Then
`gapDesire = K × gap`, `gap ∝ lw`, and `cap = f × m*` is **independent of `lw`** (T4.10 established
that `lw` cancels exactly from `m*`). Delivered gap-driven flow is
`perCount × damping × viability × gapScale × gap` with `gapScale = min(1, cap/gapDesire)`:

- **Sub-cap** (`gapScale = 1`): flow `∝ perCount × gap ∝ BaseRate × lw`. Scaling `lw` by `f` and
  scaling `BaseRate` by `f` produce **algebraically identical** flow — the first multiplies `gap`,
  the second multiplies `perCount`, and only the product appears.
- **Cap-bound**: `gapScale × gap = (cap/gapDesire) × gap = cap/K`, in which **both** constants cancel
  — the same structural cancellation `corridors.json` already records for `BaseRatePerYear`
  (*"the rate cancels out entirely"*) and `docs/t3.4b-migration-evidence.md:93-99` explains.
- **Overdraw-bound**: `scale = prevCount/desiredTotal` with `desiredTotal ∝ BaseRate × lw` — both
  cancel again.

So in **no** regime does `lw` carry information that `BaseRatePerYear` does not. Only the **product**
is identifiable.

**Measured, as a check.** Scaling `lw` by `f` against scaling `BaseRate` by the same `f`:

| seed | f | LandWeight arm | BaseRate arm | divergence |
| --- | --- | --- | --- | --- |
| 1 | 0.25 | 0.00014170 | 0.00013481 | 5 % |
| 1 | 1 | 0.00019344 | 0.00019344 | 0 (identical — the control) |
| 1 | 4 | 0.00023326 | 0.00026366 | 13 % |
| 2 | 0.25 | 0.00018971 | 0.00016460 | 15 % |

The `f = 1` arms are bit-identical, which is the sanity control. The other arms agree to within
5–15 % rather than exactly. **What is and is not claimed:** the algebra above shows exact
redundancy *in the gap channel*, and famine flight — the one channel `BaseRate` scales that `lw` does
not — is inert here, since T4.10 measured `DeficitRatio` identically 0 across the canonical world.
The residual 5–15 % is therefore attributable to integer flooring (`ConservedMath.WholeUnits`), the
per-bucket migration remainder, and 650 turns of feedback through population into arable-per-capita.
**I did not isolate that residual with a dedicated experiment, so it is reported as unattributed
rather than explained.** It does not restore a second degree of freedom: no reading of a 5–15 %
discretisation divergence identifies a *level* for `lw`.

**Consequence:** any `(lw, BaseRatePerYear)` pair with the same product is observationally
near-equivalent. There is no unique `lw` to derive, and pinning one would require an external anchor.

## 5. THE ONLY AVAILABLE ANCHOR IS THE CORRIDOR, AND USING IT WOULD BE FITTING

With the ratio method's denominator gone (§1) and the coefficient redundant (§4), the sole remaining
way to pin `lw` is to choose the value that puts `migrationGrossPerDecade` inside `[0.001, 0.01]`.
That is **STOP condition 2** verbatim ("the derivation requires fitting to the old migration
corridor") and **STOP condition 8**, and it is the exact move `docs/queue.md:718` forbids: *"Do not
re-tune the floor to silence it — that would be fitting the instrument to the artifact."*

It is also worth recording that the corridor is a poor anchor on its own terms, for two reasons that
have nothing to do with this packet's preferences:

- **Scope mismatch.** The band's derivation (`corridors.json`, `canonical.migrationGrossPerDecade`)
  defines what it measures as *"LONG-DISTANCE PERMANENT INTER-SETTLEMENT RELOCATION between sites
  spaced >=480 km apart"*, and reasons its bounds from a frontier-agrarian reference class (LBK
  Neolithic, Bantu expansion, the Russian/Siberian and North American frontiers). **T4.1b moved
  `minSpacingKm` from 480 to 95.2.** The shipped world's settlements are ~5× closer than the
  phenomenon the band describes.
- **Possibly the wrong observable.** `docs/t4.1g-review-record.md:43-49` already records that
  *"gap-driven migration DOES respond to the rate lever; gross migration does not… something
  uncapped and rate-insensitive dominates gross flow"* — the corridor measures gross migration.

**Note, against the obvious inference:** the band itself is NOT invalidated by T4.10's model change.
It was derived from a reference class about the society, not from the attractiveness formula's shape,
so re-shaping `R` does not touch it. Its problem is the spacing scope above, which predates T4.10.

## 6. PHASE 5 — WHERE THE DEGREE OF FREEDOM SHOULD GO (recommendation, not applied)

The model carries one parameter too many in the gap channel. Two coherent resolutions, neither taken:

- **(i) Fix `lw` by convention and let `BaseRatePerYear` carry the rate.** Set `LandWeight = 1` so
  `R` is literally fertility-weighted arable km² and `instant` is literally arable km² per capita —
  a quantity with units and meaning, which a reference class can actually speak about. Then derive
  `BaseRatePerYear` against the migration reference class, which is what its name already claims it
  is: a per-year propensity. This puts the free parameter on the constant that has a physical
  interpretation and removes the one that does not. **Recommended.**
- **(ii) The converse** — keep `lw`, drop `BaseRatePerYear`. Rejected: `BaseRatePerYear` is the
  parameter with a stateable reference class (per-year relocation propensity); `lw` is a bare
  reconciler, and `docs/spine-s8-governance-freeze.md:97` only ever licensed such weights as
  *"dimensionless reconciler between X and Y"* — a justification that lapses when there is no Y.

Either is a TUNE change that moves behaviour and goldens, and (i) requires a fresh reference-class
derivation of `BaseRatePerYear`. That is a directed decision, not something to fold into a packet
whose ruling was "derive `landWeight`" — which is the T4.1f lesson exactly.

## 7. WHAT WAS NOT DONE, DELIBERATELY

`AttractivenessLandWeight` was **not** changed and keeps its T3.4b value of `0.078125`. No corridor
was widened, narrowed, re-pinned or retired. No golden moved. No production file was touched. T4.2's
four certified reds were not approached. No new constant was invented to resolve any STOP condition.

---

## FOUR-STATE

**DOCUMENTED: YES** — this record.
**IMPLEMENTED: NO** — stopped at Phase 3 on STOP conditions 1, 2 and 8, before any production edit.
**TESTED: N/A** — no code changed; the suite is unchanged from `40afe7b`
(455 passed / 4 certified reds / 6 skipped; `Sim.Ui.Tests` 151/151).
**MEASURED: YES** — arable-per-capita distribution and smoothed-attractiveness gaps (3 seeds ×
3 checkpoints × 12 settlements); cap-binding shares per pair; a `LandWeight` sweep at ×0.25/×1/×4
against a matched `BaseRatePerYear` sweep. Both probes deleted and the tree verified byte-identical.
One arm (seed 2, ×4) was not run — the sweep exceeded its time box and the remaining five arms
already settle both questions.

**T4.12: STOPPED / PENDING REVIEW.** The missing decision is §6: where the redundant degree of
freedom should live, and whether `BaseRatePerYear` is to be re-derived against the migration
reference class. That decision is the director's; it cannot be derived from inside this packet
without fitting.

---

# DIRECTOR RULING — SEMANTIC NORMALIZATION, AND WHAT MEASUREMENT SAYS ABOUT IT

**RULING: `LandWeight = 1.0`,** so that `R_i` is fertility-weighted arable land and `instant_i` is
fertility-weighted arable land **per capita** — a quantity with a unit and a meaning, rather than a
bare scale coefficient — with `BaseRatePerYear` left as the parameter responsible for migration
response rate instead of the two compensating for one another. **The ruling states explicitly that
this is a normalization/design decision, NOT a claim that 1.0 was empirically discovered, and asks
that the distinction be documented. It is documented here, and it matters more than it first looks:
the ruling is about SEMANTICS, and the measurements below show the naive implementation of it would
have been a BEHAVIOUR change.**

**STOPPED AGAIN, at Phase 1 and again at Phase 4 — two independent triggers, both named in the
packet. No production file, config, test, corridor or golden was changed; tree verified
byte-identical to `abbb6fa`.**

## PHASE 1 — THE NEUTRALITY PRECONDITION FAILS

The packet's Phase 1 requires: *"Verify that setting LandWeight = 1 changes only the scale
representation of the attractiveness signal and does not introduce a new behavioral degree of freedom
by itself. If this is not mathematically true in the actual code, STOP and report."*

**It is not true, and here is exactly why.** Verified against the shipped code:

| quantity | line | dependence on `lw` |
| --- | --- | --- |
| `resources[s] = lw × arableKm2` | `MigrationSystem.cs:188` | linear |
| `instant[s] = resources[s] / max(pop,1)` | `:189` | linear |
| `gap = max(0, smoothed[dst] − smoothed[src])` | `:265` | **linear** |
| `gapDesire += BaseRate × profile × count × dt × damping × viability × gap` | `:274` | **linear (via gap)** |
| `denom = resources[src] + resources[dst]` | `:280` | linear |
| `equalizing = (R_dst·P_src − R_src·P_dst)/denom` | `:281-283` | **CANCELS** |
| `cap = GapClosingFraction × equalizing` | `:284` | **CANCELS** |
| `gapScale = gapDesire > cap ? cap/gapDesire : 1` | `:285` | **asymmetric** |

`lw` scales the **desire** but not the **cap**. Raising it 0.078125 → 1.0 multiplies the gap channel
by **12.8×** against a fixed ceiling, which is a behaviour change, not a re-labelling.

**Measured, canonical, 650 turns:**

| arm | `lw` | `BaseRate` | seed 1 migration | seed 2 migration |
| --- | --- | --- | --- | --- |
| CONTROL (today) | 0.078125 | 0.03 | 0.00019344 | 0.00023008 |
| **`lw`=1 ALONE** (the literal ruling) | 1.0 | 0.03 | **0.00032391 (+67 %)** | **0.00046388 (+102 %)** |
| **`lw`=1, product preserved** | 1.0 | 0.00234375 | 0.00018064 (−6.6 %) | 0.00019406 (−15.7 %) |

So `lw = 1` **alone is a +67 % to +102 % behavioural change**, not a normalization. (It is not the
full 12.8× because the fixed cap absorbs most of the amplification — more pairs simply become
cap-bound, exactly as the algebra predicts.)

**A true normalization exists and is arithmetically forced, not invented.** Because only the product
`BaseRate × lw` is identifiable (§4 above), the unique behaviour-preserving companion to `lw = 1` is
`BaseRatePerYear = 0.03 × 0.078125 = 0.00234375` — exact in binary. That arm measures −6.6 % / −15.7 %
rather than exactly 0; the residual is the same integer-flooring and 650-turn-feedback class already
reported in §4, **and it was not isolated by a dedicated experiment, so it is reported as
unattributed rather than explained.**

**This is the distinction the ruling asked to have documented, sharpened by measurement:** `lw = 1`
is a semantic choice, and it is only a *pure* semantic choice when the product is carried across. On
its own it silently doubles migration.

## PHASE 3 — `BaseRatePerYear` IS IDENTIFIABLE

At `lw = 1`, sweeping `BaseRatePerYear` about the product-preserving value (seed 1, canonical, 650
turns):

| ×0.25 | ×0.5 | ×1 | ×2 | ×4 |
| --- | --- | --- | --- | --- |
| 0.00013314 | 0.00015491 | 0.00018064 | 0.00020154 | 0.00023509 |

Monotone and material, saturating upward as the cap absorbs more pairs. So **outcome C is refuted**:
`BaseRatePerYear` is not redundant once `lw` is fixed by convention — fixing `lw` is precisely what
removes the redundancy and leaves one identifiable rate parameter, which is what the ruling intended.

## PHASE 4 — OUTCOME **B**: IDENTIFIABLE, BUT WITH NO DERIVATION AT ALL

Searched exhaustively. **`baseRatePerYear = 0.03` has no derivation anywhere in the repository:**

- `Sim.Data/content/sim.json:111` carries **no `_doc` string** — conspicuous, since the same migration
  block documents `_docDamping` and gives `_docLandWeight` a full derivation record.
- `Sim.Core/Systems/SimConfig.cs:421` has **no per-field comment**, unlike its neighbours. The only
  enclosing statement is `SimConfig.cs:394`: *"Migration tuning (T2.5, all TUNE, per-sim-year)"* —
  classified as tuning, with no basis given.
- No reference class was ever stated for it, in pointed contrast to the corridor band, whose class
  (LBK Neolithic, Bantu expansion, the Russian/Siberian and North American frontiers) is written out
  in full in `corridors.json`.
- `corridors.json` denies its name is even operationally accurate: *"It is NOT a migration
  propensity… the rate cancels out entirely"* whenever the cap binds.
- `docs/spine-s8-governance-freeze.md` contains **no mention of migration or of this constant at
  all**, so it holds neither a provenance requirement nor an exemption — the S8 audit never covered
  it. It is also not exemption-eligible on the `attractivenessFoodWeight` precedent: that pass-state
  licensed a *"dimensionless reconciler between X and Y"*, which is not what a per-year rate claims
  to be.

Per the packet's own decision tree this is **outcome B — "identifiable but has no defensible existing
derivation. STOP and return a narrowly scoped director decision."** Phase 4 also forbids inventing a
value, and none was invented.

**Bearing on the normalization: there is no provenance to protect.** Moving the scale from `lw` to
`BaseRatePerYear` cannot damage a derivation that does not exist. But neither does it *create* one —
after normalization the model still carries exactly one underived scale constant. What changes is
that it then sits on the parameter that is *supposed* to carry rate and can be derived against a
reference class, instead of on a coefficient that cannot.

## PHASE 5 — CORRIDOR: UNRESOLVED / REQUIRES SEPARATE DERIVATION

Unchanged: `[0.001, 0.01]` was not re-pinned, widened, narrowed, or optimized toward. Nothing in this
phase changes the §5 assessment above: the band is **not** invalidated by T4.10's food removal or by
this normalization, because it derives from a reference class about the society rather than from
`R`'s shape — but its stated scope (*">=480 km apart"*) does not match the shipped world since T4.1b
moved `minSpacingKm` to 95.2, and `t4.1g` separately flagged gross migration as possibly the wrong
observable. **UNRESOLVED / REQUIRES SEPARATE DERIVATION.**

## PHASES 6–7 — NOT REACHED

No 20-seed post-normalization sweep and no golden work were performed, because nothing was
implemented. The pre-normalization states A–D stand exactly as recorded at T4.10; there is no E.

## THE DECISION REQUESTED — TWO LINES

1. **Which form of `lw = 1`?** (a) `lw = 1` alone — accepted as a deliberate **+67 %/+102 % behaviour
   change**, not a normalization; or (b) `lw = 1` **with `BaseRatePerYear = 0.00234375`** — the
   arithmetically forced, behaviour-neutral normalization that actually matches the ruling's stated
   intent. **Recommend (b).**
2. **`BaseRatePerYear` (outcome B):** authorize a directed derivation of it against the migration
   reference class — as its own packet with its own lens, which is the T4.1f lesson — or rule that it
   stays at its current (underived) value for now with the gap recorded.

Neither is takeable from inside this packet without either overriding the Phase-1 STOP or inventing a
constant.

## FOUR-STATE — AFTER THE RULING

**DOCUMENTED: YES** — this section, including the normalization-vs-calibration distinction the ruling
asked for, sharpened by the measurement that the naive form is not neutral.
**IMPLEMENTED: NO** — stopped at Phase 1 (neutrality precondition failed) and again at Phase 4
(outcome B), both explicit STOP conditions.
**TESTED: N/A** — no code changed; suite unchanged from `40afe7b` (455 passed / 4 certified reds /
6 skipped; `Sim.Ui.Tests` 151/151).
**MEASURED: YES** — three-arm neutrality experiment (control vs `lw`=1 alone vs product-preserving),
2 seeds; five-point `BaseRatePerYear` sweep at `lw`=1. Probe deleted, tree verified byte-identical.

**T4.12: STOPPED / PENDING REVIEW. T4.10: PENDING REVIEW. Nothing merged, nothing certified.**

---

# CODE-FIRST AUDIT — AND A CORRECTION TO THIS RECORD'S OWN PRIOR RECOMMENDATION

Run under the directed order **CODE → MATHEMATICS → SPEC/ADR → MEASUREMENT → IMPLEMENTATION**.
**No simulation was run in this pass, and none was needed:** every question the packet posed was
settled by source and algebra, and the audit terminates in a STOP before measurement could be
justified. Tree verified byte-identical to `58c65b6`.

**This pass overturns the recommendation the previous section made.** That section recommended
`lw = 1` with `BaseRatePerYear = 0.00234375` as "the arithmetically forced, behaviour-neutral
normalization." **It is not behaviour-neutral.** It was measured neutral only in the canonical
world — the one world that structurally cannot detect the difference, because T4.10 established
`DeficitRatio ≡ 0` there. The code shows what the measurement could not.

## 1. CODE FINDINGS — every use site, exhaustive

| constant | production use sites | position |
| --- | --- | --- |
| `AttractivenessLandWeight` | **`MigrationSystem.cs:188` only** | `resources[s] = lw × arableKm2` |
| `BaseRatePerYear` | `:274` (gapDesire), `:298` (perCount), `:332` (desired) | multiplies the ENTIRE `push` |
| `FamineFlightFactor` | `:307`, `:322` | **inside** the `push` parenthesis |

The decisive line is `:320-322`:

```
double push = damping[src, dst] * viability[dst]
              * (gapScale[src, dst] * Math.Max(0.0, smoothed[dst] - smoothed[src])
                 + m.FamineFlightFactor * deficit[src]);
```

`BaseRatePerYear` sits **outside** this expression (`:332`); `LandWeight` reaches it **only** through
`smoothed`, i.e. only the first term. `LandWeight` does not appear in the famine term at all, by any
path — `deficit[src]` comes from `ConsumptionDeficits.DeficitRatio` and is `lw`-independent.

## 2. MATHEMATICAL FINDINGS

### 2a. Where `lw` scales and where it cancels

`resources → instant → smoothed → gap → gapDesire` all scale linearly in `lw` (the EMA at `:216-227`
is affine-homogeneous with no constant term, and its first-sighting seed is `instant` itself, so
linearity holds by induction **for a from-scratch run**; `max(0,·)` is positively homogeneous). But
in `equalizing` (`:281-283`) the numerator and denominator both scale, so **`lw` cancels exactly**,
and `cap` (`:284`) and `gapScale` (`:285`) are **invariant**. `lw` therefore changes magnitude only,
never direction, and it affects cap binding by scaling desire against a fixed ceiling.

### 2b. THE GAUGE STRUCTURE — three constants, two identifiable products

```
gap channel:    BaseRatePerYear × LandWeight        = 0.03 × 0.078125 = 0.00234375
flight channel: BaseRatePerYear × FamineFlightFactor = 0.03 × 8.0      = 0.24
```

Three constants, two identifiable combinations ⇒ **exactly one redundant degree of freedom.**
`lw` and `BaseRatePerYear` are redundant **on the gap channel only**; `FamineFlightFactor` separates
them. The earlier claim in §4 of this record — that "in no regime does `lw` carry information that
`BaseRatePerYear` does not" — is **too strong and is corrected here**: they differ precisely on
famine flight.

### 2c. WHY THE AUTHORIZED TWO-CONSTANT FORM IS NOT A NORMALIZATION

Apply `lw → lw·f`, `BaseRate → BaseRate/f` (f = 1/0.078125 = 12.8):

```
desired' = (B/f)·profile·count·dt·damping·viability·( f·gapTerm + flightTerm )
         =  B·profile·count·dt·damping·viability·( gapTerm + flightTerm/f )
```

**The gap channel is exactly invariant; the famine-flight channel is DIVIDED BY 12.8.** Famine flight
is the D-021 Exit valve — the mechanism by which starving people leave. Weakening it 12.8× is a
change to a ratified mechanism's strength, not a change of units. **STOP condition 2 / Phase 1D.**

Adversarial verification (independent agent, instructed to refute) **CONFIRMED** this and found it
understated: `desiredTotal` (`:305-309`) splits the same way, so the overdraw scaler
`scale = prevCount/desiredTotal` (`:335-336`) also changes whenever any deficit > 0 and a bucket is
overdrawn — which then multiplies the *gap* term too. So even the gap channel loses invariance in
that case, and `MigrationRemainder` carries the perturbation forward across turns.

### 2d. THE COMPLETE GAUGE FIX, IF NORMALIZATION IS STILL WANTED

Because there are two products and three constants, fixing `lw = 1` requires **two** compensations,
not one:

| constant | now | complete gauge fix | preserves |
| --- | --- | --- | --- |
| `AttractivenessLandWeight` | 0.078125 | **1.0** | (the gauge choice) |
| `BaseRatePerYear` | 0.03 | **0.00234375** | `BaseRate × lw = 0.00234375` |
| `FamineFlightFactor` | 8.0 | **102.4** | `BaseRate × FFF = 0.24` |

With all three, the continuous model is preserved exactly. With only the first two — the form this
packet authorized — it is not. The third constant is outside the authorized scope, which is why this
is reported rather than applied.

### 2e. BIT-EXACTNESS — AND THE ATTRIBUTION OF THE EARLIER RESIDUAL

`f = 12.8 = 64/5` is **not a power of two**. `0.078125 = 5/64` is exact in binary64 and `1.0` is
exact, but their ratio is not, so each cancellation above holds only to ~1 ULP rather than
bit-exactly. `ConservedMath.WholeUnits` truncation plus the persisted `MigrationRemainder` then
amplify sub-ULP drift into whole-person differences, compounding over 650 turns.

**This closes an item this record previously left open.** §4 above reported a 5–15 % divergence as
"attributable to integer flooring … *not isolated*, so reported as unattributed." It is now
attributed from source: non-power-of-two rounding amplified by integer truncation and remainder
carry. The contrast is instructive and already in the code — `MigrationSystem.cs:175-177` records an
earlier **1/256** re-denomination that *was* bit-exact, precisely because 1/256 is a power of two.

## 3. HISTORICAL / SPEC FINDINGS — the origin of 0.03

**`baseRatePerYear = 0.03` was never derived.** Introduced in commit
**`2fb2ea2c53ae162befe1952b6b259e055784ea58`, 2026-07-22, "T2.5: migration — the D-021 Exit valve"**,
which added the whole migration block under the header *"Migration tuning (T2.5, all TUNE,
per-sim-year — law 3)"*. The commit message documents mechanism, ordering, schema and acceptance
tests but says **nothing about where the number came from**. `docs/m2-spec.md:36` pinned only the
corridor (*"magnitudes a few %/decade (TUNE corridor)"*), not the constant.

**It has never changed.** Two additive hunks only: `2fb2ea2` (introduction) and `330c279`
(2026-07-27, block rewrite adding `_docLandWeight`/`_docDamping` — value untouched). The 0.018/0.012
figures in the docs are sweep probes, never committed values.

No reference class for a per-year relocation propensity exists anywhere. The only migration reference
class in the repo is the corridor's per-decade band, and that same note disclaims the constant:
*"It is NOT a migration propensity … the rate cancels out entirely."*

### Per-constant reconciliation

| | `LandWeight` | `BaseRatePerYear` |
| --- | --- | --- |
| **CURRENT CODE MEANING** | scalar on arable km² forming R; scales the gap channel only | scalar on the whole push; scales gap AND flight channels |
| **DOCUMENTED INTENDED MEANING** | *"dimensionless reconciler between X and Y"* (S8 §97) | per-year relocation propensity (implied by name and by `× dtYears`) |
| **ORIGIN** | T3.4b dispersion balance (method since ruled invalid) | `2fb2ea2`, T2.5, undocumented |
| **STILL VALID?** | **No** — the reconciler justification lapsed when T4.10 deleted Y | **Unverified** — code position is consistent with the name, but `corridors.json` denies it operationally |
| **CONFLICT?** | No code/spec contradiction; the spec basis simply expired | No contradiction; an absence of provenance, not a disagreement |

**No code-vs-spec contradiction was found**, so STOP condition 1 does not fire.

## 4. PHASE 3 — WHAT ACTUALLY NEEDED MEASURING

| Question | Answerable without simulation? | Measurement required? |
| --- | --- | --- |
| What does `LandWeight` multiply? | **CODE** (`:188`) | No |
| Where does it cancel? | **ALGEBRA** (`:281-285`) | No |
| What does `BaseRatePerYear` multiply? | **CODE** (`:274/298/332`) | No |
| Are `lw` and `BaseRate` redundant? | **ALGEBRA** — on the gap channel only | No |
| Is `lw = 1` (+ `br` compensation) neutral? | **ALGEBRA** — **no**, flight ÷ 12.8 | No |
| Why did the earlier arms diverge 5–15 %? | **CODE** — 12.8 not a power of two, + `WholeUnits` | No |
| Was 0.03 derived? | **GIT HISTORY** — no | No |
| Does the model behave sensibly after a change? | measurement | **Moot — nothing implemented** |
| Does it pass the old corridor? | measurement | **Not a valid objective** |

**Every question resolved without simulation. Zero runs in this pass.**

## 5. IMPLEMENTATION

**None.** STOP conditions **2** (the authorized `lw = 1` form is not a normalization) and **9**
(a hidden redundancy structure — three constants, two products — was discovered) both fire, and
**3/4** stand from the base-rate finding.

## 6. BASE-RATE DERIVATION STATUS

**IDENTIFIABLE BUT UNDERIVED.** Current value 0.03 (origin `2fb2ea2`, never derived, never changed).
Provisional value under the complete gauge fix would be 0.00234375 — explicitly **not** independently
derived, only inherited from the product. No reference class exists for a per-year relocation
propensity. **Required next decision: DIRECTOR DERIVATION/RULING.** Not tuned, not invented.

## 7. CORRIDOR STATUS

**UNRESOLVED / REQUIRES SEPARATE DERIVATION.** Untouched — not re-pinned, widened, narrowed, or used
to derive anything. Assessment unchanged: the band is not invalidated by T4.10 or by any gauge
choice, because it derives from a reference class about the society rather than from `R`'s shape; but
its stated scope (*">=480 km apart"*) has not matched the world since T4.1b set `minSpacingKm = 95.2`,
and `t4.1g` separately flagged gross migration as possibly the wrong observable.

## FOUR-STATE — CODE-FIRST PASS

**DOCUMENTED: YES** — this section, including the correction to this record's own prior
recommendation and the now-closed residual attribution.
**IMPLEMENTED: NO** — STOP conditions 2 and 9.
**TESTED: N/A** — no code changed; suite unchanged from `40afe7b`.
**MEASURED: N/A BY DESIGN** — no simulation was run; the audit was resolved from source, algebra and
git history, per the directed order.

---

# BASE-RATE DERIVATION AUDIT — OUTCOME C: NO DERIVATION EXISTS

Code / history / specification / mathematics only. **No simulation was run and none was required.**
Tree verified byte-identical to `7cebdcc`. **The previous finding stands unaltered and is restated
here so it is not lost: the proposed `LandWeight = 1` normalization is INVALID, because
`BaseRatePerYear` also scales the D-021 famine-flight channel.** Nothing in this section reopens it.

## 1. ORIGIN AUDIT — verified directly against git, not against current files

- **Introduced:** `2fb2ea2c53ae162befe1952b6b259e055784ea58`, **2026-07-22**, *"T2.5: migration — the
  D-021 Exit valve"*, which added the whole `migration` block under the header *"Migration tuning
  (T2.5, all TUNE, per-sim-year — law 3)"*.
- **Only value ever committed.** `git log --all -p -S baseRatePerYear` yields exactly one distinct
  line across all history: `+ "baseRatePerYear": 0.03,`. It has **never** been changed. (`330c279`
  rewrote the block to add `_docLandWeight`/`_docDamping`; the value was untouched. The 0.018/0.012
  figures in the docs are sweep probes, never committed.)
- **No derivation in the introducing commit.** Its message specifies mechanism, overdraw ordering,
  schema v9, goldens and acceptance tests, and says nothing about where 0.03 came from.
  `docs/m2-spec.md:36` set only a target — *"magnitudes a few %/decade (TUNE corridor)"* — i.e. T2.5
  pinned the **corridor**, not the constant.
- **No later ADR or review record supplied one.** Every subsequent mention (`t3.4b-migration-evidence.md:92`,
  `adr-015:353,369,793`, `t3.4c-review-record.md:91,193`) treats 0.03 as a given and sweeps it as a
  lever.

### The finding this pass adds — the constant outlived its own model, twice

Commits touching `MigrationSystem.cs`, in order: `2fb2ea2` (T2.5, intro) → `34b387c` → **`56918f0`
(T2.8, 2026-07-23)** → `8988146`/`1ae5643` (T2.13) → `df8c1c0` → `ca88b41` → `2ff3f04` → `330c279` →
`78e1260` (T4.10).

**0.03 was chosen for a model with no cap.** T2.5's own message describes the driver as a plain
product: *"base rate × cohort profile × count × exp(-cost/decay) damping × (positive attractiveness
gap + famineFlightFactor × Prev deficit)"*. In that model the base rate was a genuine linear scale on
migration volume. **One day later, T2.8 (`56918f0`) added the gap-closing cap and the EMA** — *"the
gap-driven desire per pair is scaled to never exceed gapClosingFraction (TUNE 0.25) × m\*"* — which
is exactly the mechanism that makes the rate cancel whenever the cap binds.

**And its target moved too.** T2.5's acceptance names a corridor of **[3, 8] %/decade**; the shipped
band is **[0.001, 0.01] = [0.1, 1.0] %/decade** — an **8× to 30×** move, re-derived at T3.4b. The
corridor was re-derived; the rate constant chosen against the old one never was.

So 0.03 is not merely undocumented: **the model it was chosen for and the target it was aimed at have
both since been replaced, and it has never been revisited.**

## 2. SEMANTIC AUDIT — what 0.03 means, from the equation

Dimensional analysis of the shipped code, independently verified by an agent instructed to refute it.

Let `[lw]` be unknown. `arableKm2` is km², `pop`/`Count` persons, `DtYears` years; `damping`,
`viability` and `deficit` are dimensionless.

**The decisive line is `:285`:** `gapScale = gapDesire > cap ? cap / gapDesire : 1.0`. One branch is
the literal `1.0`, so the other must be dimensionless too.

```
gapDesire (:274) = (1/yr)(persons)(yr)·gap = persons · [lw]·km²/persons = [lw]·km²
cap       (:284) = ([lw]km²·persons)/([lw]km²) = persons        ← [lw] cancels
cap/gapDesire    = persons / ([lw]·km²)
```

- If `[lw]` were **dimensionless**: `cap/gapDesire` = persons/km², against a literal `1.0`. **The two
  branches of one ternary would carry different units.** And `desired` (`:332`) would come out in
  **km²** — square kilometres floored by `WholeUnits` into a headcount.
- If `[lw] = persons/km²`: `cap/gapDesire` is dimensionless ✓, `gap` is dimensionless ✓, `push` is
  dimensionless ✓, and `desired = (1/yr)(persons)(yr) = persons` ✓.

Confirmed independently by the `+` at `:321-322`: `gapScale·gap + FamineFlightFactor·deficit` adds to
a dimensionless `deficit`, which forces `gap` dimensionless, which forces `[lw] = persons/km²`.

**Therefore:**

```
LandWeight        = a reference carrying-capacity DENSITY, in persons per km²
R_i               = the population settlement i's land supports at that density  [persons]
instant_i = R_i/P = supportable population / actual population  → a HEADROOM RATIO, dimensionless
BaseRatePerYear   = the fraction of a bucket that relocates per year, per unit headroom gap
```

**So 0.03/yr means: at unit damping and unit viability, a bucket facing a one-unit
carrying-capacity-headroom gap relocates 3 % of itself per year.** Its name is honest — it is a
genuine per-year propensity — even though `corridors.json` correctly reports that it is *operationally*
not one, because the cap erases it wherever the cap binds.

### Two consequences worth recording

**(a) A documentation-vs-code discrepancy.** `SimConfig.cs:400` ("attractiveness A = LandWeight ×
farmlandPerCapita"), `spine-s8-governance-freeze.md:96`'s "dimensionless reconciler" pass-state, and
this record's own earlier ruling section all read `lw` as dimensionless and `instant` as
*arable-per-capita*. **The code's dimensional structure requires `lw` in persons/km² and `instant`
dimensionless.** Reported, not resolved — no source was silently preferred.

**(b) An independent confirmation that `LandWeight = 1` is not a normalization.** Under the code's
own units, `lw = 0.078125 persons/km²` encodes a reference density whose reciprocal is **12.8 km² of
fertility-weighted arable per person**. Setting `lw = 1` would set that reference to **1 km² per
person** — a 12.8× change in a physically meaningful quantity, not a change of units. This reaches
the same STOP as the famine-flight argument, by a completely different route.

*Flagged for whoever eventually derives it, not asserted as a defect:* 0.078 persons/km² is a
forager-scale density; settled agriculture runs orders of magnitude denser. Because `arableKm2` is
**fertility-weighted** rather than raw cultivated area, this is not directly comparable to a
historical figure, and no claim is made here that the value is wrong.

## 3. D-021 INTERACTION

Identifiable products, unchanged from the previous section and restated for completeness:

```
gap channel:    BaseRatePerYear × LandWeight         = 0.03 × 0.078125 = 0.00234375
flight channel: BaseRatePerYear × FamineFlightFactor = 0.03 × 8.0      = 0.24
```

`BaseRatePerYear` multiplies the entire `push` (`:332`, outside the parenthesis at `:320-322`);
`LandWeight` enters only the gap term; `FamineFlightFactor` sits inside the same parenthesis.
**`LandWeight` cannot be normalized independently** because any compensation applied to
`BaseRatePerYear` also rescales the D-021 flight channel. `FamineFlightFactor` is a ratified
mechanism and **no change to it is proposed**.

## 4. REFERENCE CLASS — ABSENT, BUT THE HOUSE PATTERN IS CLEAR

**There is no reference class anywhere in the repository for a migration RESPONSE RATE.** The only
migration reference class is the corridor's per-decade band (LBK Neolithic, Bantu expansion, the
Russian/Siberian and North American frontiers), and that is an **outcome** measure — how much
relocation occurs — not a response rate, and it cannot identify the constant in any case, since the
rate *"cancels out entirely whenever the cap binds"*.

Nor does any timescale constrain it: `attractivenessSmoothingWindowYears = 20.0` carries no `_doc`
and originates as a **defect fix** for the T2.8 ping-pong attractor, not from evidence; ADR-011 is
about dt-invariant *integration*, not rate provenance.

**But the project has a clean, repeatable template**, and a future derivation should follow it rather
than invent a method — `sim.json:39` (T4.2's store bounding) is the model: *"Two DERIVED constants,
reference classes stated in docs/t4.2-manifest.md and **fixed BEFORE the world was run** …
pre-modern fixed storage loses 5-10 %/yr … **the midpoint is taken, not tuned**."* ADR-013's yield is
the heavyweight variant, and notably **held its derived value even though "THE WORLD IS OUT OF
CORRIDOR AT THIS VALUE"** — which is precisely the discipline this constant needs.

## 5. IDENTIFIABLE ≠ DERIVABLE

`BaseRatePerYear` is **identifiable**: the model responds to it monotonically wherever the cap does
not bind. That is a statement about the equations, and it is **not** a statement about what its value
should be. Nothing in this audit licenses inferring a value from that responsiveness.

## 6. DECISION — OUTCOME C

- 0.03 is **historical and undocumented** — origin `2fb2ea2`, never derived, never changed, and both
  the model and the target it was chosen against have since been replaced.
- Its **semantic role is now understood**: a per-year relocation propensity per unit
  carrying-capacity-headroom gap (§2).
- Its **value is not derivable from current project evidence**: no reference class exists for a
  migration response rate, and the only quantity that could pin it — the corridor — is an outcome
  measure that cannot identify it and is itself unresolved.
- **No replacement value is proposed or invented.**

**Recommended: a separate director decision on whether to stand up a reference class for a migration
response rate**, following the `_docStoreBounding` pattern (name the class, state its band, take the
midpoint, commit before measuring). That is its own packet with its own lens — the T4.1f lesson.

## FOUR-STATE — BASE-RATE AUDIT

**DOCUMENTED: YES** — this section.
**IMPLEMENTED: NO** — no production change was authorized or made.
**TESTED: N/A** — no code changed; suite unchanged from `40afe7b`.
**MEASURED: N/A BY DESIGN** — resolved from source, git history and dimensional algebra.

---

# CLOSED — `BaseRatePerYear` IS SEMANTICALLY UNDERSTOOD AND NUMERICALLY UNDERIVED

Final pass. **No simulation run. No production file touched.** Tree verified byte-identical to
`f00a39e`. This section closes the question; it does not open another.

## THE ANSWER: NO INDEPENDENT REFERENCE CLASS EXISTS, AND THE REASON IS STRUCTURAL

Not "we searched and found nothing" — the search was done and found nothing, but the stronger result
is that **no such reference class can pin `BaseRatePerYear` alone as the model is currently
formulated.** An adversarial agent tasked with refuting this **CONFIRMED** it and closed every route:

| candidate route | why it fails |
| --- | --- |
| **Migration corridor** | An OUTCOME measure, and forbidden as a target. Worse, it cannot identify the constant at all: the rate *"cancels out entirely whenever the cap binds"*. |
| **Adjustment timescale** ("a 2× land advantage fills in N generations") | Sub-cap it reads the product `BaseRate × lw`. **Cap-bound, `m*` is homogeneous of degree 0 in R, so `lw` AND `BaseRate` both cancel** — a timescale measured there identifies `GapClosingFraction` only, and carries zero information about `BaseRate`. Overdraw-bound, both cancel again. |
| **Scale-free dimensionless ratios** (cohort mix, migration : natural increase) | `CohortProfile` multiplies `BaseRate` identically in all three terms (`:274`, `:298`, `:332`), so cohort ratios identify `CohortProfile` and cancel `BaseRate`. Ratios identify SHAPE, never LEVEL. |
| **Famine flight** — the strongest attack, since `lw` is genuinely absent from it | Flight/yr = `BaseRate × profile × FFF × deficit × damping × viability`. One equation, and `FamineFlightFactor = 8.0` is itself undocumented (`sim.json:116`, no `_doc`, unlike its neighbours). A famine-emigration reference class pins the PRODUCT `BaseRate × FFF`. The adversarial pass sharpened this further: the flight channel carries **four** model-internal underived factors (`BaseRate`, `FamineFlightFactor`, `DampingDecayCostUnits`, `DestinationDeficitRepulsion`), not one. **The entanglement moves; it does not dissolve.** |

### The identifiability structure, stated once and completely

```
gap channel     →  BaseRatePerYear × AttractivenessLandWeight  = 0.00234375
flight channel  →  BaseRatePerYear × FamineFlightFactor        = 0.24
cap-bound       →  neither (both cancel; only GapClosingFraction survives)

3 unknowns, 2 identifiable products  ⇒  exactly ONE gauge freedom.
```

Fixing any one of the three pins the other two — but **none of the three has a derivation in the
repository.** Every route to `BaseRatePerYear` therefore runs through a second underived constant.
The nearest viable routes (fix `lw` by convention; or normalise `FamineFlightFactor` by a boundary
condition such as "the exit valve saturates at `deficit = 1`") are **choices, not derivations** —
they convert a two-unknown product into a one-unknown derivation, which is precisely the problem
restated rather than solved.

## GOVERNANCE PERMITS CLOSING THIS, AND THERE IS PRECEDENT

This is not a defect being waved through. `docs/spine-s8-governance-freeze.md:117-120` states the
disposition explicitly:

> "The COMMON case — (c) fails but (a), (b), (d) are clean — is RECORDED as **'chosen, never
> derived'** and queued. It is NOT corrected in this packet. Most of `sim.json` predates any
> derivation discipline; this disposition is what keeps a clean-ish audit at a session instead of
> inheriting the whole config's provenance debt."

and `:126`: *"'never derived' is a stated answer, never an omitted one."*

The shipped precedent is consistent and plentiful — `sim.json:174` (production, incl.
`toolWearPerEquippedFarmerPerYear`): *"ALL CHOSEN, not derived - stated per S8 4.1(c)"*; `:182`
(price solver): *"ALL TUNE, ALL CHOSEN - not one value derived"*; `:207` (harvest variance): *"none
derived"*; trade's `gapClosingFraction`: *"CHOSEN 0.25. Never derived."*

**One qualification, stated because it is the honest reading:** `BaseRatePerYear` does **not** get
the S8 §4.1(a) "dimensionless reconciler" pass-state — it is `1/yr`, not dimensionless, and its
exemplar (`attractivenessFoodWeight`) was deleted by T4.10. But (a) is nonetheless answered, by §2 of
this record: it is *the fraction of a bucket that relocates per year, per unit carrying-capacity-
headroom gap*. (b) and (d) are clean — correctly denominated at its consumption site, visible in
TUNE. Only (c) fails. That is exactly the COMMON case the freeze names.

## RULING

```
BaseRatePerYear:
  SEMANTICALLY UNDERSTOOD
  NUMERICALLY UNDERIVED
  CURRENT VALUE 0.03 RETAINED AS HISTORICAL TUNE
  NO REPLACEMENT VALUE AUTHORIZED
```

The question is **CLOSED**, not deferred to another measurement cycle. Reopening it requires new
external evidence — a reference class for a migration response rate — not another sweep of the
simulation, which by the table above cannot produce one.

**Residual provenance debt, queued not hidden:** three constants in the migration rate sector
(`BaseRatePerYear`, `AttractivenessLandWeight`, `FamineFlightFactor`) are all "chosen, never
derived", and they carry one gauge freedom between them. Deriving any ONE would pin the rest. That is
the smallest future decision available, and it is a director decision, not a packet's.

## DOCUMENTATION DISCREPANCY — CORRECTION RECORDED, NOT APPLIED

**The CODE interpretation is the one that follows**, and the evidence is structural rather than
stylistic:

- `:285` `gapScale = gapDesire > cap ? cap/gapDesire : 1.0` — one branch is a literal `1.0`, so
  `cap/gapDesire` must be dimensionless. That holds only if `lw` carries persons/km².
- `:321-322` `gapScale·gap + FamineFlightFactor·deficit` — `deficit` is dimensionless, so `gap` must
  be, which forces the same.
- Under a dimensionless `lw`, `desired` (`:332`) would evaluate to **km²**, floored by
  `ConservedMath.WholeUnits` into a headcount.

**Therefore:** `LandWeight` is a reference carrying-capacity **density in persons/km²**;
`R_i = LandWeight × arableKm2_i` is a **population**; `instant_i = R_i/P_i` is a **dimensionless
headroom ratio**.

**Corrections required (recorded per instruction; NOT applied, since no production file may change):**

1. `Sim.Core/Systems/SimConfig.cs:400` currently reads *"attractiveness A = LandWeight ×
   farmlandPerCapita"*. It should state that `R = LandWeight × arableKm2` is a population at the
   reference density `LandWeight` [persons/km²], so `A = R/P` is a dimensionless headroom ratio.
2. `Sim.Data/content/sim.json`'s `_docLandWeight` treats `lw` as a term-balancing reconciler
   (comparing 0.354 against a food differential of ~0.35). That framing died with the food term at
   T4.10 and should be annotated with the units above.
3. `docs/spine-s8-governance-freeze.md:96`'s "dimensionless reconciler" pass-state cites
   `attractivenessFoodWeight` as its exemplar — a constant T4.10 **deleted**. The exemplar is stale.
4. **This record's own DIRECTOR RULING section** reads `instant` as "arable per capita" (the H1
   reading). That is superseded by §2 here. Recorded rather than edited, so the sequence of findings
   stays auditable.

**No production code should be changed merely to make documentation agree** — and none was.

## FOUR-STATE — CLOSING PASS

**DOCUMENTED: YES** — this section.
**IMPLEMENTED: NO** — and none authorized.
**TESTED: N/A** — no code changed; suite unchanged from `40afe7b` (455 passed / 4 certified reds /
6 skipped; `Sim.Ui.Tests` 151/151).
**MEASURED: N/A BY DESIGN** — zero simulation, which is the correct outcome when no derivation exists.

**T4.12 BASE-RATE QUESTION: CLOSED. T4.12 packet: PENDING REVIEW. T4.10: PENDING REVIEW.
T4.7: PAUSED.** Nothing merged, nothing certified.
