# T3.5b — four-lens review record

**Pin: `cc6ebfd34f0c6ecab3c401cfb88ef398164f4c49`.** One worktree per lens (§6), every worktree
verified clean and removed. Lens 3's bounds stated before its sweep: filtered baseline 6.9 s →
35 s per mutant; full-suite baseline 374 s → 1,870 s escalation bound; zero hangs, every build
verified (§7.9).

## Reconciliation BY NAME against `docs/t3.5b-lens-manifest.md` (§7.11)

| # | lens | result |
| --- | --- | --- |
| 1 | `derivation-honesty` | REPORTED — items 1 and 2 both **DERIVED**; 3 findings |
| 2 | `pre-commitment-integrity` | REPORTED — s\* independently confirmed, all measurements reproduce **bit-exactly**, nothing moved; 4 findings |
| 3 | `test-power` | REPORTED — 13-mutant kill-record, every kill semantic; 1 MODERATE + 2 findings |
| 4 | `scope-and-regression` | REPORTED — every fence line CLEARED with evidence; 4 LOW/INFO findings |

## What the review CONFIRMS

- **Both derivations DERIVED, not fitted** (lens 1): the standard is *anti-convenient* — a fitted
  H\* needed only ≥ 0.8152 to make the shipped world saturate, and 0.54 made the packet's own
  acceptance criterion harder; the mix's only threshold was cleared a priori by the ruling's
  "majority farming"; values entered the tree once and never moved; both unfavourable pre-committed
  readings were applied.
- **Every headline measurement reproduces bit-exactly** on lens 2's independent reruns (density,
  population, grain store, arable growth, on both trees). Its own recomputation of s\* from
  HEAD-measured values (c = 0.828, a = 0.563) gives 0.2939 vs the packet's 0.295 — the 0.84/0.57
  inputs are stale-but-conservative and the verdict survives.
- **All fence lines CLEARED** (lens 4): no store bounding in any disguise (reserves ~1,240 years
  and growing — the packet escalates to B-2 rather than claiming credit); migration weights and
  every band byte-identical; the second golden re-pin's declared cause verified logically
  (x/x = 1.0 exactly in IEEE-754 at H = 1) and empirically; T2.13's extinction path structurally
  unweakened; §7.14 the only widening; all 26 changed files traceable to ruled items.
- **Test power** (lens 3): **10 of 13** mutants killed, all semantically, 3 survivors (C3 and V3
  fixed post-pin with red proofs; G3 an equivalent mutant) — no kill in the record rests on a
  golden alone. (Q1 CORRECTION, director certification: this line first said "11 of 13" and a
  status message said "12-mutant sweep" — BOTH were my errors, the §7.11 count pattern again;
  the kill-record is the authority and it lists 13 mutants, 10 killed. Counted from the record,
  not from memory, this time.) The 1-ulp H\* fix is pinned by a dedicated test (V4), the ghost fix and
  both cross-file guards die to single dedicated tests (G1/G2/C1/C2), and the construction share is
  specifically pinned by the PathBuild pair (S2).

## The timestamp finding (lenses 1 + 2, independently) — resolved with evidence

Both lenses measured five commits (manifest → pre-commits → derivations → implementation → first
goldens) inside a two-second window and correctly ruled the commit-order *proof* void as offered.
**Cause, measured:** the stop-hook identity rebase (`git rebase --exec "git commit --amend
--no-edit --reset-author"`, prescribed verbatim at `stop-hook-git-check.sh:58`) re-stamped author
and committer dates at 10:12. The pre-rebase originals were verified **tree-identical** to the
replayed commits on 2026-07-28 (all five pairs, by `git rev-parse ^{tree}`) and are PINNED against
garbage collection by the pushed ref `archive/t3.5b-prerebase-originals` (→ `a6e2186`, whose
parent chain carries all five; a local annotated tag `t3.5b-prerebase-originals` exists too — tag
pushes are proxy-blocked). Q4, director ruling: the first version of this sentence said the
originals "remain reachable" while they were unreachable from every ref — a claim that would have
silently become false at the next gc. Tense and pin corrected:

| original | true time | rewritten as |
| --- | --- | --- |
| `1a9b273` manifest | 09:33:49 | `eecf7f5` |
| `15f5db0` pre-commits + s\* | 09:34:34 | `8b51306` |
| `c2f758e` derivations | 09:35:37 | `ddccb80` |
| `da746d1` rig isolation (first measurement-bearing) | 09:52:43 | `66bd15f` |
| `a6e2186` implementation + goldens | 10:04:30 | `0e8e547` |

The true timeline shows **17 minutes** between the derivations and the first measurement-bearing
commit. The lenses' deeper caveat stands and is accepted: commit metadata is never cryptographic
proof, which is why lens 1's DERIVED verdicts deliberately rest on internal evidence.

## Findings and dispositions

| finding | severity | disposition |
| --- | --- | --- |
| L1-F1 / L2-F1 — commit-order proof void (2-s window) | HIGH/MEDIUM | **RESOLVED** — measurement correct, cause the identity rebase; originals tree-identical with 17-min real gap (table above) |
| L2-F2 — §7.13 "applied without deliberation" failed on first application: reading 1 misapplied to a composite observable before the director's injections forced the decomposition | MEDIUM | **ACCEPTED as recorded** — the §7.10 confession is in the shipped comment; the lens judges the correction honest (moved toward the less convenient outcome); root cause filed: the pre-committed observable lacked a discriminating arm (§7.13 rule 2) |
| L3-C3 — varietyStandard load guards deletable with 479 green (§7.4) | MODERATE | **FIXED** post-pin: three-arm `Assert.Throws` test, proven red by deleting the guard block (2/2 red, restored green) |
| L3-V3 — `varietyWeight` entirely unvalidated; a negative value is a smuggled variety bonus (law 2) | MINOR | **FIXED** post-pin: loader range check `[0,1]` + test, proven red by deleting the check |
| L3-G3 — the T2.13 extinction block is shadowed dead code (property pinned, code identity untestable) | INFO | comment added in place so no future lens re-files it |
| L3-nit — `Aggregate`'s "measured dead" comment carried superseded T3.5 numbers | LOW | comment superseded in place with the T3.5b status |
| L2-F4 — arable figures mis-transcribed in the battery comment (53,385/70,851 vs measured 53,382.9/70,854.5) | LOW | **FIXED** (transcription only; +33% conclusion unaffected) |
| L2-F3 — c ≈ 0.84 stale-but-conservative | LOW | recorded; verdict survives lens 2's recomputation; reserve deepens to ~1,254 y at measured c |
| L4-1 — needs.json wholesale re-serialized (semantic equality proven, diff noise real) | LOW | recorded; no value moved (JSON-parse equality verified by the lens) |
| L4-2 — the H\* fix rode a "WIP: tests" commit label | LOW | recorded as commit-hygiene; the declaration landed fully one commit later |
| L4-3 — fifth UI change traded an exact per-turn pin for a floor without itemizing it | LOW | recorded here as the itemization: turn-1 exact pin retained; turns 2+ floor because the derived herding share makes the value turn-varying; no ratified item requires per-turn exact pins |
| L4-4 — spec said "six-lens", manifest governs four | INFO | **FIXED** — spec line corrected with provenance |
| L1-F2 — the mix's ballpark preceded its derivation (directive-implied "majority farming") | observation | recorded; breaches nothing (the fixed order is numbers-before-measurement) |
| L1-F3 — reference class cited at genre level, below T3.2b's named-source bar | LOW | recorded honestly; the derivation is un-auditable at source level and the DERIVED verdicts rest on the internal evidence lens 1 enumerated |
| L3-(c) — AntiTautology passes under V2 (wrong normalisation) | recorded | its claim is exactly what it states; the normalisation shape is pinned by the GrainOnly + StrictlyDecreases pair — a three-legged proof, each leg load-bearing |

## Post-pin fix commits

The C3/V3 guard fixes, the G3/doc-rot comments, the F4 transcription and the spec line land in one
commit after the pin, each guard proven red. Suite after: see handback.
