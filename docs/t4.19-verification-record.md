# T4.19 VERIFICATION RECORD

Branch `t4.19-glass-box`. Baseline `feaf218` (T4.18). **Not merged to main; main at
`dbef61a`, M5 untouched on `m5-full-build`.** Every figure below was measured on
the tree it names; lane figures come from the lane's own commit and an
independent verifier's re-measurement, both recorded in the workflow journals.

## §1 THE LANES AND THEIR VERDICTS

| lane | commit | verdict | fix applied |
| --- | --- | --- | --- |
| A1 telemetry records | `ca39e7e` → `8f59166` | ACCEPT with one required fix | GrainPresent predicate copy dropped |
| A2 explain queries | `cc7b5de` → `84881e6` → `d116e2a` | NOT ACCEPTED AS-IS → ACCEPT | the system's arithmetic made callable (public pure functions `Step` itself calls; 50-turn hash identical); links stamped with the world actually read |
| B player UI | `672d44b` → `d6f5ce7` | REJECT → ACCEPT | every chain link keeps its Note and its own lever; starved-rig turn pinned exactly; turn-0 trends sample; PREV class membership |
| C founding demographics | `c605614` → `b99e11d` | ACCEPT with non-blocking findings | PathBuild pin magnitude; founding band stated honestly |
| D chrome defects | `bf9ec3a` → `6a66bfb` | FIX-NEEDED → closed | NoBackground on the scroll child; test header; Margin==WindowPadding pin |
| E CR-013 diagnostic | `041fc53` → `5ffd33b` | ACCEPT | Windows half obtained on the runner; §8.4–§8.6 measured |

## §2 THE SUITE ON THE INTEGRATED BASE `2a436f1` (A1, A2, C, D, E merged; B not yet)

Release. **Sim.Ui.Tests 208 / 208. Sim.Tests 674 passed / 14 failed / 6 skipped.**
The fourteen, by cause — every one deliberate and none hidden:

| cause | tests | ruling that unblocks |
| --- | --- | --- |
| **CR-014** — `ProductionSystem.Craft` input cap ignores the banked remainder; `LedgerOverdrawException` at driven turn 213 | 9: `DrivenGolden_Seed42Turn300`, `D1_FlowAndItsDecomposition`, `IntegratedPinAttribution.DrivenGolden…`, and six Observability tests that run the driven world for 300 turns (`WorldReconciliation.Driven…`, `SettlementIdentity.Driven…`, `StoreLoss.Driven…`, `PolicyHistory.Driven…` ×2, `Telemetry.ASettlementRecord_PrintsInFull`) | CR-014 option 1 (one expression) — then the driven golden re-pins once with two named causes |
| **CR-003 §7.6** quarantine, pre-existing on every tree since M4 | 2: `Dev_MalthusCorridors_AllInBand` seeds 42, 7 | unchanged |
| **Lane C, reported not re-banded**: `densityPerArableKm2` leaves [0.15, 0.60] at 0.625 / 0.742 because year-4500 population is +30 % at an unchanged growth rate | 2: `Canonical_FedCorridors_AllInBand` seeds 1, 2 | director re-derives the ceiling on 20 seeds, or accepts the reading |
| **Lane C, reported**: Artisans emerge at turn 4 against the documented window [10, 95] — a younger pyramid reaches the surplus threshold sooner | 1: `Artisans_EmergeInFedAutoplay_…` | director re-documents the window |

The six Observability tests are not weakened to dodge CR-014: the same six
properties are proven on the founded seed-42 world for 300 turns and on the
colonization rig, which pass. They fail on the driven world for exactly the
reason the driven golden does, and say so.

## §3 WHAT MOVED, AND WHY

Every golden movement on this branch has one cause, lane C's founding cohort
vector, and a control (turn-0 diff of every table under old and new config —
only `Buckets`, `GoodStocks` grain endowment, `Housing` dwellings and the three
`InitialEndowment` ledger rows differ, on four seeds; `GoldenHash_Seed42Turn200`
unmoved). The full table is `docs/m4-founding-demographics-correction.md` §7.
No observability lane moved any golden; each proved it by running the pins.

## §4 LANE B — THE PLAYER INFORMATION UI

`672d44b` → `d6f5ce7`. Verifier: REJECT on the first cut (the rendered chain
dropped every link's Note and showed only the head node's lever, so weather read
as if it were levered); ACCEPT after the fix (all 43 `ChainNode`s render their
own lever or "condition, no lever — reason"; 8 condition nodes; 24 of 24 Notes
rendered on the starved chain). Sim.Ui + Sim.Ui.Tests + one doc only. The
click paths for the twelve director questions, with the record behind each, are
`docs/m4-player-information-ui.md` §5; two are answered by naming the missing
layer — per-settlement deaths are UNSPLIT (gap 1), and consequences of a policy
change are shown as *observed, not attributed* because the simulation carries
no counterfactual (gap 13).

## §5 THE FINAL BATTERY on `t4.19-glass-box` at the reported SHA

Release build 0 warnings. Gates: banned-constructs OK, read-isolation OK,
readonly-proof OK. **Sim.Ui.Tests 227 / 227.** **Sim.Tests 681 passed / 14
failed / 6 skipped** — the full run at `b5d6d89` was 679 / 16 / 6 and the two
extra were A2's starved-rig exact pins measured on the old founding vector
(0.7722… → 0.7786…, grain 531/3937 for 436/3078), re-pinned with lane C named as
the single cause and re-run green under the Observability filter (41 / 47, the
six being CR-014). The fourteen are §2's table exactly: 9 CR-014, 2 CR-003
quarantine, 3 lane-C readings reported rather than re-banded.

Determinism, replay, save/load and schema: the pins that carry them —
`SnapshotTests` populated round-trips, `CiPinAgreementTests`, `ReplayTests`,
`UiSessionReplayTests`, `SessionRecordTests` (a played session reproduces
turn-for-turn from its own log), and the no-telemetry-induced-change and
JSONL-determinism tests — all pass. Schema stays **v24**; nothing observability
wrote is serialized into `WorldState`.

## §6 CR-013 — MEASURED

Windows runner turn-2 hash equals the director's own trace. Turn 1 identical
across 40 blocks; turn 2 first diverges at `PriceTerms[43].Consumption` by 2 ulp
plus two `HarvestWeather` rows; turn 3 at `GoodStocks[154].ProduceRemainder` by
8,192 ulp. Classified as a libm transcendental divergence (`Math.Exp`/`Sqrt` in
`HarvestWeatherSystem`, `Math.Exp` in `PriceSystem`) that has propagated into
simulation-meaningful state. `docs/adr/cr-013-…` §8.4–§8.6. No simulation
expression changed. The `sim diff` verb and the runner-side comparison workflow
are on the branch.

## §7 RULINGS THE DIRECTOR IS ASKED FOR

1. **CR-014** (one expression in `ProductionSystem.Craft`) — unblocks nine tests
   and lets the driven golden re-pin once with two named causes.
2. **CR-013** — reference platform, or a correctly-rounded exp/sqrt, plus the
   Windows determinism job as a standing gate.
3. **Lane C readings** — the `densityPerArableKm2` ceiling on 20 seeds; the
   Artisans emergence window; whether founding food should sit at the granary
   cap (`docs/m4-founding-demographics-correction.md` §8–§9).
4. **CR-003** — unchanged, standing.


---

## §8 THE RULINGS, EXECUTED (T4.19-A, -B; -C in measurement)

**T4.19-A — CR-014, option 1** (`187262f`, verified ACCEPT, no defects). The
craft input cap now includes the banked remainder: `max(0, stock −
ConsumeRemainder) / perOutput`. One expression; no second clamp; no other
system, constant or default touched; `Throw` kept. A unit rig reproduces the
recorded coincidence bit for bit (red on the old cap with "sinking 67 exceeds
available stock 66"), and a 96-row `(stock, bank)` sweep including banks 1, 2,
16 and 64 ulps below 1.0 asserts `sunk ≤ stock` and exact reconciliation on
every row. **Attribution, the two causes separated:** X1 = the fix on the OLD
founding vector at `feaf218` returns the OLD driven pin `01673381…` exactly at
turn 300 (turns 273–275 differ mid-run, 276–300 byte-identical) — the fix is
hash-neutral on that world's outcome; X2 = the merged tree = the new pin
`76f82629…`, and X1 → X2 first differs at turn 0. So the driven golden's
movement is the founding vector's alone, and CR-014 changed the pin's
*reachability*, not its value. The nine CR-014 failures collapsed: **Sim.Tests
692 passed / 5 failed / 6 skipped**, the five being exactly the two CR-003
quarantine tests and the three lane-C readings under T4.19-C.

**T4.19-B — CR-013, options 1 + 3** (`8f508e0` → `7a777cd`, verified ACCEPT).
**ADR-022** declares Linux x64 the reference platform: the determinism promise
is scoped there; Windows is a supported execution platform under surveillance;
option 2 is rejected for now and reopenable. `SessionManifest` v2 carries the
platform the session was played on (v1 files still read: "not recorded");
`sim inspect` prints a platform notice before its verdict — "reference
platform", or that a hash comparison against a reference replay is expected to
diverge from turn 2 while population/food/settlement columns stay comparable.
The reference set is exactly the two MEASURED spellings (`linux-x64`, the CI
runner; `ubuntu.24.04-x64`, this container, hash-for-hash equal) — a family
rule was refused on the verifier's finding. `xplat-surveillance.yml` runs on
every push to `main` and `t*.*` branches, weekly, and on dispatch: the Windows
arm runs the founded golden with `ci.yml`'s exact command and writes
`MATCH` / `DIVERGED-AT-TURN-N` into the job summary, `continue-on-error: true`
— visible, never blocking, as ruled. No simulation expression changed; every
golden re-run unmoved.

**T4.19-C — measurement only**, in flight on the merged tree: the prescribed
20-seed / 650-turn battery under the corrected founding versus `feaf218`,
`densityPerArableKm2` and Artisan emergence per seed, numerator/denominator
decomposition — reported in `docs/t4.19c-remeasurement.md`; **no band moves;
the director rules next.**

**Founding food** — kept at 21.5 years of demand, as ruled; not moved to the
granary cap. **CR-003** — standing.
