# T4.19 VERIFICATION RECORD

Branch `t4.19-glass-box`. Baseline `feaf218` (T4.18). **Not merged to main; main at
`dbef61a`, M5 untouched on `m5-full-build`.** Every figure below was measured on
the tree it names; lane figures come from the lane's own commit and an
independent verifier's re-measurement, both recorded in the workflow journals.

## §1 THE LANES AND THEIR VERDICTS

| lane | commit | verdict | fix applied |
| --- | --- | --- | --- |
| A1 telemetry records | `ca39e7e` → `8f59166` | ACCEPT with one required fix | GrainPresent predicate copy dropped |
| A2 explain queries | `cc7b5de` → (fix pending) | NOT ACCEPTED AS-IS: private arithmetic copied | make the system's arithmetic callable; observer calls it |
| B player UI | (in flight) | | |
| C founding demographics | `c605614` → `b99e11d` | ACCEPT with non-blocking findings | PathBuild pin magnitude; founding band stated honestly |
| D chrome defects | `bf9ec3a` → `6a66bfb` | FIX-NEEDED → closed | NoBackground on the scroll child; test header; Margin==WindowPadding pin |
| E CR-013 diagnostic | `041fc53` → `5ffd33b` | ACCEPT | Windows half obtained on the runner; §8.4–§8.6 measured |

## §2 THE SUITE ON THE INTEGRATED BASE `2a436f1` (A1, A2, C, D, E merged; B not yet)

Release. **Sim.Ui.Tests 208 / 208. Sim.Tests 674 passed / 14 failed / 6 skipped.**
The fourteen, by cause — every one deliberate and none hidden:

| cause | tests | ruling that unblocks |
| --- | --- | --- |
| **CR-014** — `ProductionSystem.Craft` input cap ignores the banked remainder; `LedgerOverdrawException` at driven turn 213 | 9: `DrivenGolden_Seed42Turn300`, `D1_FlowAndItsDecomposition`, `IntegratedPinAttribution.DrivenGolden…`, and six Observability tests that run the driven world for 300 turns (`WorldReconciliation.Driven…`, `SettlementIdentity.Driven…`, `StoreLoss.Driven…`, `PolicyHistory.Driven…` ×2, `Telemetry.ASettlementRecord_PrintsInFull`) | CR-014 option 1 (one expression) — then the driven golden re-pins once with two named causes |
| **CR-003 §7.6** quarantine, pre-existing on every tree since M4 | 2: `Dev_MalthusCorridors_AllInBand` seeds 42, 7 | unchanged |
| **Lane C, reported not re-banded**: `densityPerArableKm2` leaves [0.15, 0.60] at 0.625 / 0.742 because year-4500 population is +30 % at an unchanged growth rate | 2: `Canonical_FedCorridors_AllInBand` seeds 1, 2 | director re-derives the ceiling on 20 seeds, or accepts the reading |
| **Lane C, reported**: Artisans emerge at turn 4 against the documented window [10, 95] — a younger pyramid reaches the surplus threshold sooner | 1: `Artisans_EmergeInFedAutoplay_…` | director re-documents the window |

The six Observability tests are not weakened to dodge CR-014: the same six
properties are proven on the founded seed-42 world for 300 turns and on the
colonization rig, which pass. They fail on the driven world for exactly the
reason the driven golden does, and say so.

## §3 WHAT MOVED, AND WHY

Every golden movement on this branch has one cause, lane C's founding cohort
vector, and a control (turn-0 diff of every table under old and new config —
only `Buckets`, `GoodStocks` grain endowment, `Housing` dwellings and the three
`InitialEndowment` ledger rows differ, on four seeds; `GoldenHash_Seed42Turn200`
unmoved). The full table is `docs/m4-founding-demographics-correction.md` §7.
No observability lane moved any golden; each proved it by running the pins.

(§4–§7 — B, the final battery, CR-013 evidence and the director test — follow when B lands.)
