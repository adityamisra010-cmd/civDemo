# T3.8 — required-lens manifest

Written BEFORE implementation (§7.3); reconciled BY NAME at handback in the six-column table
(lens, status, findings raised, findings confirmed, disposition). A name with no result is
BLOCKING.

| # | lens | what it attacks | required |
| --- | --- | --- | --- |
| 1 | `derivation-honesty` | Are the maintenance/build rates DERIVED (RC-H1/H2/H3, committed before any measurement)? Attack any rate adjusted after a disappointing H-reading, any reference class picked for convenience, the CHOSEN build-labor value's stated frame. Commit order is evidence. | YES |
| 2 | `geometry-and-corridors` | The Item 4 hazard: catchment bonus moves EffectiveArableKm2, the density denominator (CR-002/CR-003/T3.5b — three prior hits). Was density decomposed before interpretation? Did any band edge move? Does the equilibrium-density invariant's precondition still hold? | YES |
| 3 | `conservation-and-accounting` | Materials conservation EXACT through Ledger (build + maintenance sinks); the dwelling stock itself Ledger-audited (source/sink reasons); no double-spend of construction labor between housing and PathBuild (the lagged split measured, not assumed); FsCheck or exactness rig over the build/maintain/degrade cycle. | YES |
| 4 | `test-power` | Mutation sweep, bounds first (§7.1); every guard proven red (§7.4). Named mutants at minimum: maintenance shortfall never degrades (τ_h → ∞); Shelter still reads the flow (rebind reverted); degradation ignores the maintenance fraction (always full decay); labor split deleted (PathBuild unaffected by housing draw); stock cap deleted. The one-turn-stop assertion must be the killer for the rebind mutant, not a golden. | YES |
| 5 | `scope-and-fence` | No tiers, no store bounding, no transport mechanisms, no merchants, no trade change, no band edge; needs.json edits MINIMAL-DIFF (T3.5b Q5 convention); goldens re-pinned once, itemized; the Comfort decision stated explicitly (fixed same-shape, or finding-and-stop — never silent widening). | YES |

## Discipline
Bounds before any mutant run (§7.1); no finding actionable before verdict (§6); §7.10
name-and-quote; §7.12 every claim measured; §7.13 the three H-readings reconciled against the
spec's text; summary prose transcribed from records (standing pattern line). Before-column
MEASURED on main via the director's replayed order log, never cited from the gate.

## Handback checklist
- [x] Item 0's five filings (done, 168b7e8) + Q-F (done, c6e384d)
- [x] Before-column measured on main, provenance noted (gate world = pre-T3.6b)
- [x] Reference classes with commit order (5a70ace before 50d6127)
- [x] H1/H2/H3 quoted, which fired — H1/H2 fired the FAILURE branch (after-column section: the canonical-world collapse, cause measured); H3's trigger did not fire at corridor scale
- [x] Comfort measurement + verdict stated (flow-bound confirmed; durable-goods stock is its own packet)
- [x] Five lenses by name, six-column table (review record)
- [x] Goldens itemized, one re-pin (ca29b85: toy v19, founded v19 + ci pin, FirstReign v14)
- [x] Suite green both projects, counts (Sim.Tests 428/0/4-skip of 432; Sim.Ui.Tests 127/127)
