# T4.4 — CORRECTED COLONIZATION TRIGGER: REVIEW RECORD

**Branch `t4.4-colonization-fix`, cut from `main` `8586863`.** Supersedes the record at `4d11c02`.
Design context: `docs/migration-simplification-audit.md`, D-037 B1/B3, ADR-012, m2-spec §3:25,
CR-003 §5.1/§5.2(a). Schema decision: `docs/adr/adr-021-unplaced-departure-demand.md`.

## §0 DIRECTOR RULING — OPTION (A), and what it settles

**Ruled:** D-037 B1 is authorized and complete as a **collapse-driven** colonization mechanism. It
extends ADR-012's "no viable destination" rule and converts genuinely unplaced migration demand
into a new settlement. **T4.4 is NOT required to deliver CR-003 §5.2(a)'s entire frontier-closure
behaviour.**

The ruling's architectural interpretation, recorded verbatim in substance:
1. **T4.4 = refugee / unplaced-departure colonization.**
2. **A separate future mechanism** must handle voluntary/frontier expansion driven by land
   opportunity.
3. **CR-003 remains quarantined** until that separate mechanism exists and demonstrates genuine
   frontier closure.
4. **T4.4's lack of activation in current canonical worlds is therefore NOT a defect.**

### §0.1 CR-003 §5.2(a) REMAINS OPEN — an intentional sequencing boundary, not a T4.4 failure

CR-003 §5.2(a) asks for *"how population converts empty land into settled, worked land, and
therefore how the frontier eventually closes and Malthusian pressure legitimately emerges."*
**T4.4 does not deliver that, and under this ruling it was never required to.** The land-driven
expansion mechanism has **not been implemented**, and no part of it is implemented here. Until it
exists and demonstrates genuine frontier closure:

- **CR-003's quarantine stands, untouched.** No corridor band, window, quarantine disposition or
  Malthus assertion was altered by this packet.
- The measured zero activation in canonical worlds (§3.2) is the **expected** reading of a
  collapse-only mechanism in worlds that do not collapse — not evidence of a defect.

**STATUS: CERTIFICATION AUDIT COMPLETE — READY FOR DIRECTOR CERTIFICATION.**
Not certified, not self-certified, not merged. No independent reviewer ran; §7 is my own
adversarial pass and is labelled as such. The audit itself is recorded in §10.

---

## §1 THE THREE DECISIONS

### D1 — WHAT COUNTS AS UNPLACED DEPARTURE DEMAND? **RESOLVED. Current implementation retained.**

**The apparent conflict.** Part 4 said the mechanism "must not be derived from settlement
deficit". ADR-012 defines the Exit valve's desire as *"source-driven (`FamineFlightFactor ×
deficit_source`), uncapped by the gap mechanism, exactly as D-021 ratified"*. D-037 B1 places
colonization at that exact hinge: *"ADR-012 rules that with no viable destination people die at
home. **Extend it**."*

**Why it is not a conflict.** B1 names ADR-012's rule as the thing being extended, and ADR-012's
rule governs the **flight channel**, which is deficit-driven by ratified construction. The gap
channel cannot supply an alternative: a gap is `max(0, S_dst − S_src)` and **needs a destination
to exist**, so there is no destination-free gap desire to leave unplaced. **There is no
deficit-free source-driven migration desire anywhere in the ratified model.** Read literally
against the flight term itself, the prohibition would forbid implementing B1 at all.

The narrowest reading that satisfies ADR-012, D-037 B1, D-037 B3, CR-003 §5.2(a), the existing
`MigrationSystem` and laws 1/5 is therefore: **the prohibition bars deriving a founding trigger
from deficit *independently, inside colonization* — which is exactly what the defective
implementation did (`floor(count × deficit)`) — and does not bar consuming migration's own
already-ratified flight desire.** The two differ in the property that decides everything:

| | old trigger | current mechanism |
|---|---|---|
| computed where | inside colonization, from a deficit ratio | inside migration, as its own flight term |
| when a viable destination exists | **fires anyway** | **exactly zero** |
| dischargeable | no — a ratio is scale-free | yes — placing the party consumes a count |

**Retained as implemented.** The condition is **binary and is B1's own**: no reachable, viable
destination *at all*. §1.1 records why the graded alternative was rejected on evidence.

#### 1.1 The graded reading, examined and rejected — not merely declined
Viability is graded (`max(0, 1 − Repulsion × deficit_j)`) and damping is always `< 1`, so most
flight desire goes unrealised even when destinations are nominally viable. Reading "unplaced" as
*desire that was not realised* would fire far more often — and that is precisely why it fails:
`damping = exp(−cost/25)` is **never 1**, so under the graded reading a settlement in deficit
would have non-zero unplaced demand **every turn it is in deficit, forever**. That is the
scale-free, never-clearing shape of the original defect wearing different clothes. The binary
reading is the only one with a discharge condition.

### D2 — WHAT IS THE FOUNDING PARTY? **RESOLVED, and it exposed two real defects.**

Audited against each question the packet asked:

| question | answer |
|---|---|
| population stock the party comes from | the source's **live post-migration** `BucketRow.Count`, drawn key-for-key (D-026), moved only by `Ledger.Transfer` |
| fractional people | `ConservedMath.WholeUnits` floor, sub-person fraction banked in `UnplacedRemainder` (D-004) |
| remainder conservation | it is a **desire** accumulator, not a stock — see the defect below |
| can the source be drained below a valid state | it can be emptied, and **that is ratified**: ADR-012 says flight *"still empties a starving settlement into healthy neighbors"* and is *"a surge by design, bounded by the overdraw scaler alone"*. No new floor was invented |
| does the daughter start with a viable food stock | **it did not — this was a defect. Fixed.** |
| one-person settlement spam | not observed; and with the clearing cost binding, a party must be outfittable from a real granary |
| is the spacing floor sufficient against spatial spam | yes, and unchanged (§4) |

**DEFECT 1 — the remainder banked WHOLE PEOPLE.** `UnplacedRemainder` was assigned
`exact − take` **after** the availability clamp, so a bucket whose desire far exceeded its
population banked the difference and carried it forward. Measured during implementation: a bucket
with desire ~500 and 25 people **banked 475**, which would discharge later as one oversized
party. Fixed by taking the bank **before** the clamp, so it holds `exact − floor(exact) < 1` by
construction; desire the population cannot satisfy is dropped, since nobody moved and nobody is
owed a move. Pinned by `TheRemainderBanksOnlyASubPersonFraction_NeverWholePeople`.

**DEFECT 2 — ZERO-PROVISION FOUNDING, and it was disabling the cascade brake.** The old code
commented *"refugees leave with nothing"* and founded anyway when the source granary was empty.
That is not a cosmetic issue: **the brake IS the daughter's food.** A daughter holding provisions
satisfies ADR-012's absolute food gate, becomes a viable destination, and zeroes its founder's
demand. A daughter founded with zero provisions is non-viable, brakes nothing, and the founder
keeps founding. Fixed by making the **already-authorized clearing cost binding** — the same
per-capita share `TransferProvisions` computes, now *required to be satisfiable* rather than
silently clamped to nothing. **No new constant, no threshold, no timer**: one shared arithmetic,
called once as the precondition and once to move the grain, so "must be outfitted" and "this is
what it leaves with" cannot drift apart. Pinned by
`ZeroProvisionFoundingIsIMPOSSIBLE_AnEmptyGranaryOutfitsNobody` and
`EveryFoundedSettlementCarriesRealProvisions_NoEmptyDaughters`.

### D3 — FIRSTREIGN / ORDER SEMANTICS. **RESOLVED: it was DEFECT 2, not order semantics.**

**Order scope, established from source, not from naming:**
- `SectorAllocationRow(SettlementId Settlement, double Farming, …)` — `WorldState.cs:308`. The
  carrier is keyed by **settlement alone**.
- `OrderValidation.cs:26` — a `SectorAllocation` order packs `settlement × 8 + sector` into
  `TargetId` and is validated against the settlement table. The order **addresses a settlement**.
- A settlement with no row gets `Sectors.Default` (`WorldState.cs:375`) — the 55 %-farming
  subsistence mix — read that way by `ProductionSystem`, `HousingSystem` and `AppropriationSystem`.
- `BucketRow`'s key is `(Settlement, Culture, Religion, Class, CohortIdx)`. **No policy, order or
  polity dimension exists on people anywhere in the tree.**

**So the order is explicitly settlement-local, and no carrier exists by which it could follow
emigrants.** D-037 B1 independently confirms it should not: *"A settlement founded by departing
population NEED NOT belong to the polity they left — refugee foundings may be stateless."*

**But the question is now moot, and the honest answer is neither A nor B.** At `4d11c02` this
world founded 16 settlements over turns 6–19 and its shape asserts went red. That was **not** the
Exit valve operating correctly — it was DEFECT 2. The FirstReign source's granary is empty from
turn 5, so every one of those daughters was founded with **no provisions at all**. With the
clearing cost binding, **no founding is possible in that world**, the reign dies exactly as it
always did, and the test passes with **every shape assert intact** and at main's own trajectory.

**The shape asserts were never weakened, and they earned their keep: they caught a real bug.**

---

## §2 IMPLEMENTATION

```
food deficit at the source
  -> MigrationSystem forms flight desire   (FamineFlightFactor x deficit, per bucket)
  -> is ANY destination reachable AND viable?          (ADR-012's gates, unchanged)
       YES -> migration transfers.  UnplacedDeparture = 0.  Colonization does nothing.
       NO  -> UnplacedDeparture = the flight desire, per bucket, keys intact
  -> ColonizationSystem floors it to whole people (sub-person fraction banked, D-004)
  -> CAN THE EXPEDITION BE OUTFITTED?  per-capita share of the home granary > 0?
       NO  -> no founding.  The people stay.
       YES -> frontier site (ADR-018 spacing floor + explicit SiteCell distinctness)
  -> Ledger.Transfer of the party, key for key, plus its provisions
  -> the daughter holds food, so ADR-012's own gate makes it a VIABLE DESTINATION
  -> next turn the founder's demand is ZERO.  Founding stops.
```

**No cooldown, age threshold, minimum-population constant, founding probability, RNG stream, TUNE
constant or timer exists anywhere in the diff.** The brake is a mechanism: the old trigger made a
newborn **emit** a founding, this one makes a newborn **absorb** one.

`MigrationSystem` gains a **pure readout** and nothing else — it writes `UnplacedDeparture` after
every input it reads is final and before any transfer. No equation, flow, cap, gate, EMA,
attractiveness term, pair selection, remainder, overdraw rule or pipeline position changed. **That
is proven in §5, not asserted.**

One further guard, found by measurement during implementation: a **missing** `SettlementDistances`
row means "the network has not been computed yet", not "nowhere to go" — a genuinely unreachable
pair still has a row storing `+∞`. Treating missing data as isolation founds settlements out of an
empty table. `hasDistances` closes it; `n == 1` is exempt because being alone in the world **is**
the condition.

---

## §3 MEASUREMENTS

### 3.1 The runaway is gone
The driven golden **never terminated** on the defective branch (two runs, 33 and 21 minutes). It
now completes in **~1 m 50 s**.

| | defective (deficit trigger) | this branch |
|---|---|---|
| driven, settlements at turn 77 | **178**, climbing ~2.3/turn | **12** |
| driven, population trend | 4330 → **3192** (fragmenting) | unchanged from `main` |
| driven, foundings in 300 turns | non-terminating | **0** |

### 3.2 Canonical sweep — 650 turns, seeds 1/2/3 — and the decisive attribution
| seed | settlements | foundings | **turns with any deficit** | **turns with unplaced demand** | final population |
|---|---|---|---|---|---|
| 1 | 12 → 12 | 0 | **18** | **0** | 128,466 |
| 2 | 12 → 12 | 0 | **19** | **0** | 127,109 |
| 3 | 12 → 12 | 0 | **13** | **0** | 102,004 |

**This is the measurement the packet asked for.** Founding is provably not driven by deficit
count: deficits occur on 13–19 turns per seed and unplaced demand occurs on **none**, because a
viable neighbour always exists. Populations are identical to the pre-fix measurement, confirming
canonical behaviour is untouched.

### 3.3 Part 6 — frontier closure, answered honestly
| question | answer |
|---|---|
| does founding eventually stop? | **yes** — and in the canonical world it never starts |
| does population fragment? | **no** — the fragmentation signature of the old trigger is absent |
| do new settlements survive? | **not measurable in a shipped world** — none is founded |
| does the world expand spatially? | **no** |
| resurrection/cascade cycle? | **no** — and the cascade brake is now genuinely armed (D2 defect 2) |
| is land opportunity progressively exhausted? | **no. The frontier does not close at all.** |
| do the Malthus corridors become meaningful? | **No. They must stay quarantined.** Nothing here entitles them to change, and nothing in CR-003 was touched |

---

## §4 SITE PLACEMENT — the director ruling, kept

The minimum-distance rule remains the **physical settlement-distinctness floor** at its ratified
value. It is not territory, ownership, attractiveness, viability, or a replacement for catchment
ownership. `minSpacingKm` was not touched and was not recalibrated. **SiteCell distinctness
remains an explicit, independent rejection** on its own line, and is tested by driving the two
apart at `MinSpacingKm = 0.0` so the SiteCell rejection is shown to fire alone. Catchment
exclusivity did not replace spacing.

---

## §5 GOLDENS — three move, ALL SCHEMA-ONLY, and that is proven

Every golden was re-measured on this exact tree with **only the two new fields removed from the
serialized stream** (schema back at v21, all logic unchanged):

| golden | OLD (`main`) | NEW | v21-stream control | CAUSE |
|---|---|---|---|---|
| driven 300 | `aae82e38…` | `5b204b45…` | **matched OLD byte-for-byte** | **SCHEMA ONLY** |
| founded 300 | `c0e3c842…` | `87bba713…` | **matched OLD byte-for-byte** | **SCHEMA ONLY** |
| FirstReign | `dfd14560…` | `bf9312a2…` | **matched OLD, with every shape assert passing** | **SCHEMA ONLY** |
| `GoldenHash_Seed42Turn200` | `0f94b4ad…` | **`0f94b4ad…`** | — | **UNMOVED, untouched** |

**No golden moved behaviourally.** The FirstReign row is the important one: at `4d11c02` it moved
for behaviour and failed its shape asserts; after DEFECT 2 was fixed it reproduces `main`'s
trajectory exactly. The synthetic control was not touched — the mechanism cannot reach that world
(no terrain) and the bucket widening cannot either (it holds no bucket rows).

The founded value was derived twice — in-test harness and built CLI — and they agree.
`ci.yml`'s `FOUNDED_GOLDEN` moves with it, in the same commit.

**Schema v21 → v22** is documented in **ADR-021** (number chosen against the repo convention;
019 is taken by an unmerged branch, 020 is the highest on `main`).

---

## §6 TESTS, GATES, DETERMINISM

- **`ColonizationTests`: 34/34 green**, including the three new pins from D2 and the adversarial
  runaway pin.
- **Full `Sim.Tests`: 6 failures, and they are EXACTLY `main`'s certified baseline** —
  4 `CalibrationBatteryTests` (2 canonical fed-corridor, 2 dev Malthus) + 2 `ClassSystemTests`.
  **No new red.** FirstReign passes, shape asserts included.
- **`Sim.Ui.Tests`: 151/151.**
- **Gates:** `check-banned-constructs` **OK**, `check-read-isolation` **OK**,
  `check-readonly-proof` **OK**.
- **Determinism:** founded orderless cross-process **IDENTICAL**; founded ordered-vs-replay
  **IDENTICAL**; built binary's hash equals the in-test pin and `ci.yml`.

---

## §7 ADVERSARIAL REVIEW — SELF-CONDUCTED ONLY

**No independent reviewer ran; this is not an independent review.**

| attack | outcome |
|---|---|
| runaway founding | closed, pinned, and measured (§3.1) |
| one-person settlement spam | not observed; clearing cost now gates it |
| source starvation from emigration | ratified behaviour (ADR-012 surge); no floor invented |
| repeated founding from the same deficit | closed — demand is zero whenever a viable destination exists |
| founding from fractional/remainder population | **DEFECT FOUND AND FIXED** (D2 defect 1), pinned |
| double-spending provisions | closed and now **directly pinned** by `ProvisionsAreTRANSFERRED_SourceLosesEXACTLYWhatTheDaughterGains`: the source's grain falls by exactly the daughter's gain and world grain is unchanged. This guards the ruling's "precondition and actual transfer use shared arithmetic" invariant — `ProvisionsFor` is called twice with different `partyHasLeft` flags, and the test pins that they agree |
| zero-provision founding | **DEFECT FOUND AND FIXED** (D2 defect 2), pinned twice |
| duplicate SiteCell | explicit independent rejection, pinned |
| spacing bypass | pinned, driven apart from occupancy at `MinSpacingKm = 0.0` |
| creation without Ledger conservation | `Transfer` only; whole-world auditor arm green |
| population appearing/disappearing | exact population equality, both arms |
| order inheritance/scope | settled from source (D3); no carrier exists, and B1 says none should |
| determinism | array scans, no RNG; harness green |
| golden contamination | v21-stream control on all four (§5) |
| schema serialization | v22, ADR-021; **round-trip pinned only by the existing bucket tests** — see below |
| founding during unsuitable pipeline phases | runs after migration, before demographics; pinned at index 10 |
| migration/colonization double-spend | structurally exclusive per source, plus a test |
| newborn immediately a founding source | closed by the brake; pinned |
| frontier closure never occurring | **CONFIRMED — this is §8 R1** |
| frontier closure too aggressive | not observed |

**Closed during this pass:** `SchemaV22_PopulatedUnplacedDepartureFields_ExactLengthRoundTripHash`
writes both new fields **non-zero and unequal to each other** (so a writer/reader transposition
fails), and asserts exact `ExpectedLength`, bit-exact round-trip, hash equality, and that the
neighbouring `ReboundReservoir`/`MigrationRemainder` doubles were not shifted by the widening.

---

## §8 CLOSED BY THE RULING

The question this section previously held — whether D-037 B1 alone should deliver CR-003
§5.2(a)'s frontier closure — was answered by the director as **Option (A)**; see §0. The three
options and their evidence are preserved in this file's history at `8f738c8`. Nothing in the
implementation changed as a result: Option (A) ratifies the mechanism exactly as it was built.

Standing prohibitions carried forward from the ruling, so a later packet does not undo this one:
**do not** broaden B1 to be land-driven; **do not** reinterpret the graded land-gap signal as
partial unplaced demand (§1.1 shows it also fails on measurement); **do not** reintroduce a
`deficit × population` founding trigger; **do not** introduce a threshold, timer, cooldown,
minimum population, RNG stream or TUNE parameter; **do not** alter ADR-012, CR-003's quarantine
or any corridor band; **do not** revert v22 on the grounds that canonical worlds do not exercise
B1.

## §10 CERTIFICATION AUDIT (director-directed, run on `8f738c8` + this document)

Each item re-verified against the tree at the certification commit, not carried from earlier runs.

| # | item | result | how it was established |
|---|---|---|---|
| 1 | D-037 B1 faithfully implemented | **PASS** | Trigger is B1's own condition — no reachable, viable destination — consuming migration's ADR-012 flight desire. Refugee foundings are stateless: no `ClaimRow`/`ControlRow` is written. §1 |
| 2 | unplaced departure consumed exactly once | **PASS** | Repo-wide grep: exactly **one writer** (`MigrationSystem`, which zeroes all buckets then sets), exactly **one consumer** (`ColonizationSystem.DrawParty`, which zeroes on draw). Pinned by `OneTurnsDemandCannotFoundTwoSettlements` and `MigrationWritesTheDemandAndColonizationCONSUMESIt` |
| 3 | no population created or destroyed | **PASS** | `Ledger.Transfer` only; whole-world `ConservationAuditor` arm plus exact population equality with no epsilon. `SourceLosesEXACTLYWhatTheDaughterGains` asserts left-once and arrived-once |
| 4 | provisions make the daughter viable under ADR-012 | **PASS** | Asserted through the gate's **own** condition (`store > 0 ∨ lastHarvest > 0`), not a helper: `ANewlyFoundedSettlementIsAVIABLEDestinationUnderADR012_TheCascadeBRAKE`, plus `EveryFoundedSettlementCarriesRealProvisions_NoEmptyDaughters` |
| 5 | clearing cost conserved and applied consistently | **PASS** | ONE `ProvisionsFor` arithmetic used both as the founding precondition and for the transfer, so precondition and payment cannot drift. Moved by `Ledger.Transfer`, `ClampToAvailable`. `ZeroProvisionFoundingIsIMPOSSIBLE_AnEmptyGranaryOutfitsNobody` |
| 6 | spacing = the existing physical distinctness floor | **PASS** | `git diff origin/main -- Sim.Data/` touches **only** `pipeline.json`; `sim.json` and `minSpacingKm` are untouched, so no recalibration occurred. Not used as territory, ownership, attractiveness or viability |
| 7 | SiteCell distinctness an independent rejection | **PASS** | `SettlementSiting.cs:376` (occupancy) and `:378` (travel-cost spacing) are two separate `continue` statements. Driven apart in test at `MinSpacingKm = 0.0`, proving the SiteCell rejection fires alone |
| 8 | no new migration mechanism introduced | **PASS** | `git diff origin/main` on `MigrationSystem.cs` **removes exactly one line** — `if (n < 2) return;` — which is re-added below the readout so a lone settlement is covered. Every other added line is the readout or `hasDistances`. No equation, flow, cap, gate, EMA, attractiveness term, pair selection, remainder or overdraw rule changed |
| 9 | FirstReign's original assertions intact | **PASS** | `git diff origin/main` on `FirstReignTests.cs` shows **exactly two** non-comment changes: the harness now passes worldgen, and the golden constant. **Every assertion, shape asserts included, is byte-identical to `main`** |
| 10 | schema v22 justified by the ratified carrier | **PASS** | ADR-021, now **ACCEPTED** under this ruling. Populated-table test writes both fields non-zero and unequal, asserting exact `ExpectedLength`, bit-exact round-trip, hash equality, and that neighbouring doubles did not shift |
| 11 | no behavioural golden changed | **PASS** | v21-stream control: with only the two new fields removed from the stream, driven, founded **and FirstReign** all reproduce `main` byte-for-byte, FirstReign with every shape assert passing. `GoldenHash_Seed42Turn200` untouched and unmoved |
| 12 | determinism green | **PASS** | founded orderless cross-process IDENTICAL; founded ordered-vs-replay IDENTICAL; toy orderless cross-process IDENTICAL; built binary's hash `87bba713…` equals the in-test pin and `ci.yml` |
| 13 | all new tests pass | **PASS** | `ColonizationTests` 34/34; `SchemaV22_PopulatedUnplacedDepartureFields…` green; `Sim.Ui.Tests` 151/151 |
| 14 | only the six certified baseline reds remain | **PASS** | See §11 for the exact counts, the CI-vs-local reconciliation, and the correction to the baseline figure |

Gates at this commit: `check-banned-constructs` **OK**, `check-read-isolation` **OK**,
`check-readonly-proof` **OK**. Build clean. Working tree clean; no stray files.

**The audit found no defect.** It is nonetheless **my own** audit — no independent reviewer ran at
any point in this packet, and §7's adversarial pass is likewise self-conducted.

## §11 CI PARITY AGAINST `main`, AND A CORRECTION TO THE BASELINE FIGURE

### 11.1 The stated target figure was wrong, and this is the evidence
The verification packet gave the expected baseline as *"6 failed / 519 passed / 6 skipped / 531
total"*. **That is not `main`'s baseline.** It is a mid-flight number from this branch at an
earlier commit (after `ColonizationTests` existed but before the v22 schema test), quoted back out
of my own earlier hand-back. Measured from `main`'s own CI run at `8586863`
(run `32550333018`, job `96975914427`):

> `Failed!  - Failed: 6, Passed: 485, Skipped: 6, Total: 497`

**`main`'s real baseline is 497 tests, 485 passing.** The instruction "do not force the numeric
total to 531" is therefore exactly right, and forcing it would have meant deleting real tests.

### 11.2 The delta reconciles exactly
| | total | passed | failed | skipped |
|---|---|---|---|---|
| `main` @ `8586863` (CI) | 497 | 485 | **6** | 6 |
| this branch @ `436a75a` (CI, verified) | 532 | 520 | **6** | 6 |
| this branch, final (local) | 533 | 521 | **6** | 6 |
| delta vs `main` | **+36** | **+36** | **0** | **0** |

The +36 are all new T4.4 tests, all passing: **35 `ColonizationTests` + 1
`SchemaV22_PopulatedUnplacedDepartureFields…`**. At `436a75a` the count was +35; the last test
(`ProvisionsAreTRANSFERRED_…`, §7) was added by the final adversarial pass. Local Debug and CI
Release agreed exactly at `436a75a` (532/520), which rules out a configuration-dependent test
discovery difference and means the local 533/521 is what CI will report.

**Not one existing test was deleted, weakened, re-expected or re-banded to reach this.** Every
number above moves in the +passed direction only.

### 11.3 The six failures are the SAME six, verbatim
Not merely the same count — the same tests, with the same quarantine messages and the same
measured values on both `main` and the branch:

| test | message on BOTH |
|---|---|
| `ClassSystemTests.Artisans_EmergeInFedAutoplay_PlateauAtTheCap_DocumentedWindow` | CR-003 quarantine, *"artisan share drained post-boom (min 0.032)"* |
| `ClassSystemTests.Famine_DrainsArtisansBeforePeasantStarvationPeaks` | *"demote-first violated: drain at turn 3, starvation peaked at turn 3"* |
| `CalibrationBatteryTests.Canonical_FedCorridors_AllInBand(seed: 1)` | `densityPerArableKm2: 0.479659` |
| `CalibrationBatteryTests.Canonical_FedCorridors_AllInBand(seed: 2)` | `densityPerArableKm2: 0.564256` |
| `CalibrationBatteryTests.Dev_MalthusCorridors_AllInBand(seed: 42)` | *"seed 42: 6 starvation deaths"* |
| `CalibrationBatteryTests.Dev_MalthusCorridors_AllInBand(seed: 7)` | *"seed 7: 2 starvation deaths"* |

**Identical values, so T4.4 did not perturb the quarantined premises even slightly.** None of the
six was touched, re-banded or re-expected.

### 11.4 Job-level CI parity
| job | `main` @ `8586863` | branch @ `436a75a` |
|---|---|---|
| `build-and-test` | **failure** (the 6 reds) | **failure** (the same 6 reds) |
| `calibration` | **failure** (4 of 6) | **failure** (4 of 6) |
| `determinism` | success | **success** |
| `determinism-xproc` | success | **success** |
| `ui-artifact` | success | **success** |
| `calibration-nightly` | success (push) | skipped (not scheduled on PRs) |

**`main` itself is red in CI**, because the six certified quarantine reds make the suite exit 1 —
that is the repository's existing CI policy the packet refers to. The branch reproduces `main`'s
job pattern exactly, with no job newly failing and both determinism jobs green.

## §9 SCOPE

Not touched: CR-002, CR-003 and its quarantine disposition, CR-004, ADR-012, m2-spec, D-decisions,
migration corridor bands, density corridor bands, any calibration parameter, any Malthus
assertion, `minSpacingKm`, the spacing rule, frozen Spine documents, T4.10, T4.13, or any
unrelated packet. Migration was not simplified: its equations are byte-for-byte behaviourally
identical, which §5's control proves.
