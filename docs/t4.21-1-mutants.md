# T4.21-1 — mutant kill-record (measured)

**Status:** measured record, T4.21-1 fix lane, 2026-09-17. Closes the T4.21-1 verifier's finding
"no mutant kill-record is committed" (CLAUDE.md: every mutant ships a kill-record with at least one
semantic test per mutant; ADR-015 §6: a claim in the tree must have been measured by the agent who
wrote it; §7.1: every run bounded, a hang is a finding; §7.2: teeth AND aim). ADR-024 §8 names the
mutants; this file is the measurement. Written by the agent who ran it.

## 1. Set-up

| | |
| --- | --- |
| tree | `claude/civdemo-work-b1z2y4` @ `05b23e6`, one private worktree (`scratchpad/t421/fix-mutants`), never shared |
| build / run | `dotnet build Sim.Tests -c Release`; `dotnet test -c Release --no-build --filter "<F>"` per mutant; the mutant reverted with `git checkout -- Sim.Core` after each run |
| filter `<F>` | `FoodStateTests \| DisasterSystemTests \| FoodHeadroomTests \| HarvestWeatherTests \| IntegratedPinAttributionTests \| SnapshotTests \| ProductionTests \| SimConfigTests` — 143 tests: every test spec §6.1/§6.2/§6.4 names for T4.21-1 plus the goldens and the attribution ladder (the verifier's filter, reused so the two measurements are comparable) |
| bound | 3 × the CLEAN baseline measured first in the same worktree under the same load (below); `timeout` enforced; a hit is recorded as non-termination |
| load | the five `--emit-session` runs of `docs/t4.21-1-foundations-audit.md` (≈ 2 min total) and one Release build of `45046eb` overlapped parts of this run on the 4-CPU machine; every mutant run took 106–115 s against the 115 s baseline (all within 8 % of it, none above it), so the 345 s bound was never approached |
| mutants | the spec's five (§6.1 M-FS-WEATHER in both forms, M-FS-DISASTER, M-FS-ABANDON in the two forms the spec names plus the implementer's `abandoned := false` form, §6.2 M-PROD-EXTRACTION, §6.4 M-DEM-SUMFOOD) and five extras the verifier added (M-FS-APPLIED, M-DIS-CONDDRAW, M-DIS-NOCARRY, M-DIS-DTLOSS, M-FS-DEADZONE); the exact diffs are in §3 |
| cross-check | the verifier's independent run of the same 12 mutants on `05b23e6` (its worktree `mutant-1`, filtered set, 109–114 s each vs a 339 s bound) reported the same failing-test sets; the implementer's earlier full-suite runs of five mutants (`abandoned := false`, `struck := false`, the latch, extraction, sumfood) agree on the semantic kills and add the goldens/reconciliation tests the filter omits |

## 2. Results


| mutant | verdict | failed / passed | elapsed | failing tests (parameterised cases collapsed) |
|---|---|---|---|---|
| CLEAN (baseline) | — | 0 / 143 | 115 s | bound := 3 × 115 = 345 s |
| M-FS-WEATHER-LATCH | KILLED | 8 / 135 | 113 s | F_AbandonmentTiming_TurnExact, F_Famine_Abandonment, F_Famine_DisasterApplied, F_NotAbandoned_FarmingAlive, F_NotAbandoned_HerdingAlive, F_Severe_WhereAdaptationEnds, F_Stress_OrdinaryWeather_NeverFamine |
| M-FS-WEATHER-STRUCK | KILLED | 3 / 140 | 113 s | F_Stress_OrdinaryWeather_NeverFamine |
| M-FS-DISASTER | KILLED | 5 / 138 | 111 s | D_Classification_DtDifference_Pinned, F_DisasterTiming_TurnExact, F_Famine_Both, F_Famine_DisasterApplied, F_Stockpile_DecidesFamine |
| M-FS-ABANDON-FALSE | KILLED | 3 / 140 | 108 s | F_AbandonmentTiming_TurnExact, F_Famine_Abandonment, F_Famine_Both |
| M-FS-ABANDON-FARMING | KILLED | 1 / 142 | 109 s | F_NotAbandoned_HerdingAlive |
| M-FS-ABANDON-SHARE | **SURVIVED — equivalent** | 0 / 143 | 109 s | — |
| M-FS-APPLIED | KILLED | 4 / 139 | 112 s | D_Classification_DtDifference_Pinned, F_DisasterTiming_TurnExact, F_Famine_Both, F_Famine_DisasterApplied |
| M-PROD-EXTRACTION | KILLED | 6 / 137 | 111 s | D_AppliedOnce_FarmingAndHerding_NeverExtraction, DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne, FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone, FoundedGolden_Seed42Turn300_MatchesPinnedConstant |
| M-DEM-SUMFOOD | KILLED | 1 / 142 | 107 s | H_Limit_ExactMirrorOfSubstitution |
| M-DIS-CONDDRAW | KILLED | 2 / 141 | 106 s | D_RngConsumption_IsTwoDrawsPerSettlement_WhateverTheHazard, FoundedGolden_Seed42Turn300_MatchesPinnedConstant |
| M-DIS-NOCARRY | KILLED | 3 / 140 | 107 s | D_Classification_DtDifference_Pinned, D_Rows_PersistWhileActive_CarryTheAppliedFactOneTurn_ThenVanish, F_DisasterTiming_TurnExact |
| M-DIS-DTLOSS | KILLED | 4 / 139 | 108 s | D_Classification_DtDifference_Pinned, D_HazardInfinite_EveryoneStruck, D_Rows_PersistWhileActive_CarryTheAppliedFactOneTurn_ThenVanish, F_Stockpile_DecidesFamine |
| M-FS-DEADZONE | KILLED | 10 / 133 | 108 s | EffectiveDeficit_DeadZone_WorkedValues, F_Severe_WhereAdaptationEnds, F_Stress_OrdinaryWeather_NeverFamine |


Every run terminated well inside its bound; no non-termination was observed. Twelve of thirteen
mutants are killed by at least one semantic test; the thirteenth (M-FS-ABANDON-SHARE) survives
because it is equivalent by construction, which is recorded as a correction to the spec's claim, not
as a gap in the tests.

## 3. Per-mutant record: diff, failing tests, the property each asserts


### M-FS-WEATHER-LATCH

**Mutation.** `FoodState.Of`: FAMINE iff `d ≥ 0.15` OR the cause rule (the old chronicle latch re-introduced; cause ignored above the latch) — spec §6.1 M-FS-WEATHER form 1

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..fdb6c17 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -77,7 +77,7 @@ public static class FoodState
         bool struck = IsStruck(w, s);
         bool abandoned = IsAbandoned(w, s);
 
-        if (d > 0.0 && (struck || abandoned))
+        if (d >= 0.15 || (d > 0.0 && (struck || abandoned)))
         {
             reason = struck && abandoned ? FamineReason.Both
                 : struck ? FamineReason.Disaster
```

**Result: KILLED — 8 failing test(s), 113 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `F_AbandonmentTiming_TurnExact` — abandonment is read IN FORCE (the prev sector row) — the turn the row lands, not one later
- `F_Famine_Abandonment` — Farming = 0 ∧ Herding = 0 in force with d > 0 is FAMINE/Abandonment; all-zero and legacy pct-0 rows abandoned; absent row not
- `F_Famine_DisasterApplied` — an APPLIED famine-class disaster with d > 0 is FAMINE/Disaster; with d = 0 it is NORMAL (an absorbed disaster is not a famine)
- `F_NotAbandoned_FarmingAlive` — Farming > 0 with Herding = 0 is NOT abandonment (single-sector rule, other side)
- `F_NotAbandoned_HerdingAlive` — Farming = 0 with Herding > 0 is NOT abandonment (the director's single-sector rule; the pastoralist is G6, not a famine by policy)
- `F_Severe_WhereAdaptationEnds` — SEVERE begins exactly at d = a (θ_sev := a) and moves with a; the label predicts the kernel's response
- `F_Stress_OrdinaryWeather_NeverFamine` — a shortfall with no cause row (weather alone, d = 0.05/0.15/0.20) is STRESS or SEVERE, never FAMINE — CR-015 "famine is exceptional": the cause, not the magnitude, makes a famine

Semantic kills: 7 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-WEATHER-STRUCK

**Mutation.** `FoodState.Of`: `struck := true` when the settlement's harvest-weather multiplier `< 0.7` — an ordinary bad decade counts as a cause — spec §6.1 M-FS-WEATHER form 2

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..bf8bff3 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -75,6 +75,8 @@ public static class FoodState
     {
         double d = DeficitRatio(w, s);
         bool struck = IsStruck(w, s);
+        for (int i = 0; i < w.HarvestWeather.Count; i++)
+            if (w.HarvestWeather[i].Settlement == s && w.HarvestWeather[i].Multiplier < 0.7) struck = true;
         bool abandoned = IsAbandoned(w, s);
 
         if (d > 0.0 && (struck || abandoned))
```

**Result: KILLED — 3 failing test(s), 113 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `F_Stress_OrdinaryWeather_NeverFamine` — a shortfall with no cause row (weather alone, d = 0.05/0.15/0.20) is STRESS or SEVERE, never FAMINE — CR-015 "famine is exceptional": the cause, not the magnitude, makes a famine

Semantic kills: 1 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-DISASTER

**Mutation.** `FoodState.Of`: `struck := false` — the applied disaster is never read — spec §6.1 M-FS-DISASTER

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..2751d16 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -74,7 +74,7 @@ public static class FoodState
         IReadOnlyWorldState w, SettlementId s, SimConfig cfg, out FamineReason reason)
     {
         double d = DeficitRatio(w, s);
-        bool struck = IsStruck(w, s);
+        bool struck = false;
         bool abandoned = IsAbandoned(w, s);
 
         if (d > 0.0 && (struck || abandoned))
```

**Result: KILLED — 5 failing test(s), 111 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `D_Classification_DtDifference_Pinned` — one forced strike at ρ = 1.3: dt = 10 gives one FAMINE turn with d ∈ [0.19, 0.21]; dt = 0.5 gives seven at d = 1 after three at 0 — the inherited F4 difference, pinned as a difference
- `F_DisasterTiming_TurnExact` — the disaster is read as APPLIED (AppliedMultiplier = the factor the harvest that produced d was multiplied by) — one turn after the strike row, exactly
- `F_Famine_Both` — disaster AND abandonment with d > 0 is FAMINE/Both — both causes are read
- `F_Famine_DisasterApplied` — an APPLIED famine-class disaster with d > 0 is FAMINE/Disaster; with d = 0 it is NORMAL (an absorbed disaster is not a famine)
- `F_Stockpile_DecidesFamine` — full pipeline, forced strike: a 0.1-y store gives FAMINE, a 1.5-y store at ρ = 2 gives NORMAL — the food balance decides, the cause qualifies

Semantic kills: 5 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-ABANDON-FALSE

**Mutation.** `FoodState.Of`: `abandoned := false` — the abandonment cause is never read (the implementer's M-FS-ABANDON form)

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..27b6c8a 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -75,7 +75,7 @@ public static class FoodState
     {
         double d = DeficitRatio(w, s);
         bool struck = IsStruck(w, s);
-        bool abandoned = IsAbandoned(w, s);
+        bool abandoned = false;
 
         if (d > 0.0 && (struck || abandoned))
         {
```

**Result: KILLED — 3 failing test(s), 108 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `F_AbandonmentTiming_TurnExact` — abandonment is read IN FORCE (the prev sector row) — the turn the row lands, not one later
- `F_Famine_Abandonment` — Farming = 0 ∧ Herding = 0 in force with d > 0 is FAMINE/Abandonment; all-zero and legacy pct-0 rows abandoned; absent row not
- `F_Famine_Both` — disaster AND abandonment with d > 0 is FAMINE/Both — both causes are read

Semantic kills: 3 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-ABANDON-FARMING

**Mutation.** `FoodState.IsAbandoned`: predicate reads `Farming == 0` only — spec §6.1 M-FS-ABANDON "Farming only"

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..4f234af 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -121,7 +121,7 @@ public static class FoodState
         {
             if (w.SectorAllocations[i].Settlement == s) { row = w.SectorAllocations[i]; break; }
         }
-        return Sectors.Raw(row, Sectors.Farming) == 0.0 && Sectors.Raw(row, Sectors.Herding) == 0.0;
+        return Sectors.Raw(row, Sectors.Farming) == 0.0;
     }
 
     /// <summary>A famine-class disaster multiplier was APPLIED to the harvest that
```

**Result: KILLED — 1 failing test(s), 109 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `F_NotAbandoned_HerdingAlive` — Farming = 0 with Herding > 0 is NOT abandonment (the director's single-sector rule; the pastoralist is G6, not a famine by policy)

Semantic kills: 1 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-ABANDON-SHARE

**Mutation.** `FoodState.IsAbandoned`: predicate reads `Sectors.Share` instead of `Sectors.Raw` for both sectors — spec §6.1 M-FS-ABANDON "reads Share"

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..3df7f95 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -121,7 +121,7 @@ public static class FoodState
         {
             if (w.SectorAllocations[i].Settlement == s) { row = w.SectorAllocations[i]; break; }
         }
-        return Sectors.Raw(row, Sectors.Farming) == 0.0 && Sectors.Raw(row, Sectors.Herding) == 0.0;
+        return Sectors.Share(row, Sectors.Farming) == 0.0 && Sectors.Share(row, Sectors.Herding) == 0.0;
     }
 
     /// <summary>A famine-class disaster multiplier was APPLIED to the harvest that
```

**Result: SURVIVED, 0 failures — an EQUIVALENT mutant.** `Sectors.Share` (`WorldState.cs:350-354`) computes `sum > 0.0 ? Raw/sum : 0.0`; for the all-zero row it returns 0.0, and for every other row `Share == 0 ⇔ Raw == 0`. So the Share predicate and the Raw predicate agree on every reachable row and no test can distinguish them. The spec's §6.1 claim that this form fails `F_Famine_Abandonment` was an assertion; it is false. `IsAbandoned` reads Raw because it is the field `ProductionSystem` reads (one fact, one field), not because Share is undefined — FoodState.cs's doc-comment and ADR-024 §8 are corrected accordingly.


### M-FS-APPLIED

**Mutation.** `FoodState.IsStruck`: reads `DisasterRow.Multiplier` (the NEXT shock) instead of `AppliedMultiplier` (the factor that produced `d`)

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..c2d914a 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -133,7 +133,7 @@ public static class FoodState
         for (int i = 0; i < w.Disasters.Count; i++)
         {
             if (w.Disasters[i].Settlement != s) continue;
-            return w.Disasters[i].AppliedMultiplier < 1.0;
+            return w.Disasters[i].Multiplier < 1.0;
         }
         return false;
     }
```

**Result: KILLED — 4 failing test(s), 112 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `D_Classification_DtDifference_Pinned` — one forced strike at ρ = 1.3: dt = 10 gives one FAMINE turn with d ∈ [0.19, 0.21]; dt = 0.5 gives seven at d = 1 after three at 0 — the inherited F4 difference, pinned as a difference
- `F_DisasterTiming_TurnExact` — the disaster is read as APPLIED (AppliedMultiplier = the factor the harvest that produced d was multiplied by) — one turn after the strike row, exactly
- `F_Famine_Both` — disaster AND abandonment with d > 0 is FAMINE/Both — both causes are read
- `F_Famine_DisasterApplied` — an APPLIED famine-class disaster with d > 0 is FAMINE/Disaster; with d = 0 it is NORMAL (an absorbed disaster is not a famine)

Semantic kills: 4 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-PROD-EXTRACTION

**Mutation.** `ProductionSystem`: the extraction `FromDeposits` call receives `foodMultiplier` instead of `1.0` — spec §6.2 M-PROD-EXTRACTION

```diff
diff --git a/Sim.Core/Systems/Production/ProductionSystem.cs b/Sim.Core/Systems/Production/ProductionSystem.cs
index 3086982..16ddc48 100644
--- a/Sim.Core/Systems/Production/ProductionSystem.cs
+++ b/Sim.Core/Systems/Production/ProductionSystem.cs
@@ -186,7 +186,7 @@ public sealed class ProductionSystem : ISimSystem<ProductionTables>
                 pool: Sectors.Share(shares, Sectors.Extraction) * adults,
                 perWorkerPerYear: _cfg.Production.OutputPerExtractorPerYear,
                 foodSector: false,
-                weather: 1.0);
+                weather: foodMultiplier);
             Craft(ctx, prev, stocks, goods, settlement,
                 pool: Sectors.Share(shares, Sectors.Crafting) * adults);
         }
```

**Result: KILLED — 6 failing test(s), 111 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — attribution control (teeth only)
- `DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne` — attribution control (teeth only)
- `FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — attribution control: the pre-packet pin reproduced byte-for-byte after stripping the disaster streams + table (teeth only)
- `FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone` — attribution control (teeth only)
- `FoundedGolden_Seed42Turn300_MatchesPinnedConstant` — golden pin (reproducibility, not semantics — counted as teeth only)
- `D_AppliedOnce_FarmingAndHerding_NeverExtraction` — the food multiplier is applied once to farming and herding/fishing; extraction (ore) never sees weather or disaster

Semantic kills: 1 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DEM-SUMFOOD

**Mutation.** `FoodHeadroom.Limit`: `X := grain + Σ non-staple LastProduced` (the sum form) instead of the fixed point — spec §6.4 M-DEM-SUMFOOD

```diff
diff --git a/Sim.Core/State/FoodHeadroom.cs b/Sim.Core/State/FoodHeadroom.cs
index e865e48..3745ea7 100644
--- a/Sim.Core/State/FoodHeadroom.cs
+++ b/Sim.Core/State/FoodHeadroom.cs
@@ -148,7 +148,8 @@ public static class FoodHeadroom
             }
             double stapleShare = 1.0 - surplusShare;
             if (!(stapleShare > 0.0)) return double.PositiveInfinity;
-            x = numerator / stapleShare;
+            x = stapleProduced;
+            for (int q = 0; q < n; q++) if (foods[q] != staple) x += produced[q];
 
             bool moved = false;
             for (int g = 0; g < n; g++)
```

**Result: KILLED — 1 failing test(s), 107 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `H_Limit_ExactMirrorOfSubstitution` — FoodHeadroom.Limit is the exact mirror of ConsumptionSystem's one-directional substitution (X = S + Σ min(NS_g, b_g X)); a livestock surplus cannot feed a grain shortfall — F5

Semantic kills: 1 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DIS-CONDDRAW

**Mutation.** `DisasterSystem`: the severity uniform is drawn only on onset (RNG consumption depends on the hazard outcome)

```diff
diff --git a/Sim.Core/Systems/Disaster/DisasterSystem.cs b/Sim.Core/Systems/Disaster/DisasterSystem.cs
index 4c067bd..4b87cdf 100644
--- a/Sim.Core/Systems/Disaster/DisasterSystem.cs
+++ b/Sim.Core/Systems/Disaster/DisasterSystem.cs
@@ -108,7 +108,7 @@ public sealed class DisasterSystem(SimConfig cfg) : ISimSystem<DisasterTables>
             SettlementId id = prev.Settlements[i].Id;
             RngStream rng = ctx.Rng(new RegionId(id.Value));
             double uHazard = rng.NextDouble();
-            double uSeverity = rng.NextDouble();   // ALWAYS taken, even when unused
+            double uSeverity = uHazard < p ? rng.NextDouble() : 0.0;
 
             double applied = 1.0, severity = 0.0, remaining = 0.0;
             for (int r = 0; r < prev.Disasters.Count; r++)
```

**Result: KILLED — 2 failing test(s), 106 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `FoundedGolden_Seed42Turn300_MatchesPinnedConstant` — golden pin (reproducibility, not semantics — counted as teeth only)
- `D_RngConsumption_IsTwoDrawsPerSettlement_WhateverTheHazard` — exactly two NextDouble per settlement per step whatever the hazard — the λ = 0 world differs from the pre-packet world by RngStreams rows only (the attribution control)

Semantic kills: 1 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DIS-NOCARRY

**Mutation.** `DisasterSystem`: the Kind-0 carry row (`AppliedMultiplier`) is not written after the event ends

```diff
diff --git a/Sim.Core/Systems/Disaster/DisasterSystem.cs b/Sim.Core/Systems/Disaster/DisasterSystem.cs
index 4c067bd..af344fa 100644
--- a/Sim.Core/Systems/Disaster/DisasterSystem.cs
+++ b/Sim.Core/Systems/Disaster/DisasterSystem.cs
@@ -133,8 +133,7 @@ public sealed class DisasterSystem(SimConfig cfg) : ISimSystem<DisasterTables>
             else
             {
                 // Carry the Applied fact one turn, nothing else.
-                if (applied < 1.0)
-                    disasters.Add(new DisasterRow(id, 0, 0.0, 0.0, 1.0, applied));
+                // carry removed
                 continue;
             }
```

**Result: KILLED — 3 failing test(s), 107 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `D_Classification_DtDifference_Pinned` — one forced strike at ρ = 1.3: dt = 10 gives one FAMINE turn with d ∈ [0.19, 0.21]; dt = 0.5 gives seven at d = 1 after three at 0 — the inherited F4 difference, pinned as a difference
- `D_Rows_PersistWhileActive_CarryTheAppliedFactOneTurn_ThenVanish` — rows persist while RemainingYears > 0, carry the Applied fact exactly one turn after the event ends, then vanish
- `F_DisasterTiming_TurnExact` — the disaster is read as APPLIED (AppliedMultiplier = the factor the harvest that produced d was multiplied by) — one turn after the strike row, exactly

Semantic kills: 3 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DIS-DTLOSS

**Mutation.** `DisasterSystem`: `multiplier := 1 − severity` (ignores `overlap/dt`; a whole-turn loss whatever the overlap — law 3 violation)

```diff
diff --git a/Sim.Core/Systems/Disaster/DisasterSystem.cs b/Sim.Core/Systems/Disaster/DisasterSystem.cs
index 4c067bd..bb00802 100644
--- a/Sim.Core/Systems/Disaster/DisasterSystem.cs
+++ b/Sim.Core/Systems/Disaster/DisasterSystem.cs
@@ -139,7 +139,7 @@ public sealed class DisasterSystem(SimConfig cfg) : ISimSystem<DisasterTables>
             }
 
             double overlap = Math.Min(remaining, dt);
-            double multiplier = 1.0 - severity * overlap / dt;
+            double multiplier = 1.0 - severity;
             remaining -= overlap;
             if (multiplier < 1.0 || applied < 1.0 || remaining > 0.0)
                 disasters.Add(new DisasterRow(id, KindCropFailure, severity, remaining, multiplier, applied));
```

**Result: KILLED — 4 failing test(s), 108 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `D_Classification_DtDifference_Pinned` — one forced strike at ρ = 1.3: dt = 10 gives one FAMINE turn with d ∈ [0.19, 0.21]; dt = 0.5 gives seven at d = 1 after three at 0 — the inherited F4 difference, pinned as a difference
- `D_HazardInfinite_EveryoneStruck` — λ = ∞ strikes every settlement in the onset turn with the dt-exact multiplier 1 − s·min(D, dt)/dt
- `D_Rows_PersistWhileActive_CarryTheAppliedFactOneTurn_ThenVanish` — rows persist while RemainingYears > 0, carry the Applied fact exactly one turn after the event ends, then vanish
- `F_Stockpile_DecidesFamine` — full pipeline, forced strike: a 0.1-y store gives FAMINE, a 1.5-y store at ρ = 2 gives NORMAL — the food balance decides, the cause qualifies

Semantic kills: 4 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-FS-DEADZONE

**Mutation.** `FoodState.EffectiveDeficit`: `d_eff := d` outside FAMINE (no dead zone; `a` ignored)

```diff
diff --git a/Sim.Core/State/FoodState.cs b/Sim.Core/State/FoodState.cs
index 60ae382..d0fe538 100644
--- a/Sim.Core/State/FoodState.cs
+++ b/Sim.Core/State/FoodState.cs
@@ -109,7 +109,7 @@ public static class FoodState
     {
         if (state == FoodStateKind.Famine) return d;
         double a = cfg.FoodState.AdaptationAbsorbableShortfall;
-        return d <= a ? 0.0 : (d - a) / (1.0 - a);
+        return d;
     }
 
     /// <summary>Farming == 0 ∧ Herding == 0 on the RAW row in force (absent row →
```

**Result: KILLED — 10 failing test(s), 108 s against the 345 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `EffectiveDeficit_DeadZone_WorkedValues` — d_eff = 0 for d ≤ a and (d − a)/(1 − a) above — the dead-zone adaptation form at worked values
- `F_Severe_WhereAdaptationEnds` — SEVERE begins exactly at d = a (θ_sev := a) and moves with a; the label predicts the kernel's response
- `F_Stress_OrdinaryWeather_NeverFamine` — a shortfall with no cause row (weather alone, d = 0.05/0.15/0.20) is STRESS or SEVERE, never FAMINE — CR-015 "famine is exceptional": the cause, not the magnitude, makes a famine

Semantic kills: 3 (teeth-only kills excluded). At least one semantic test per mutant: YES.



## 4. What this record changes

- ADR-024 §8's M-FS-ABANDON sentence is corrected (the `abandoned := false` form is killed by
  `F_Famine_Abandonment`/`F_Famine_Both`/`F_AbandonmentTiming_TurnExact`; the Farming-only form by
  `F_NotAbandoned_HerdingAlive` alone; the Share form is equivalent).
- `Sim.Core/State/FoodState.cs`'s doc-comment no longer claims "Share would divide 0/0".
- `docs/t4.21-architecture.md` header block carries erratum E1 for §3.1, the §6.1 row and §13(ii).
- No test was added: the Share form cannot be killed, and every other mutant already has a
  semantic kill. A test for the all-zero row on Share would assert the same outcome as the Raw
  predicate and prove nothing.
