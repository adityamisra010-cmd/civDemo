# Libur 1170 Cascade — Diagnostic Investigation Report (Step 13)

Candidate build 2807155 (t4.19-glass-box); tool build 45046eb (m4-forensic-p013, Sim.Core/Sim.Data byte-identical except additive Observability/Forensic files). Seed 42, 161 turns, 12 settlements, dt = 10 y. Nothing was implemented, nothing committed, nothing pushed, no branch of /home/user/civDemo modified. Every number below traces to a lane result under `scratchpad/cascade/runs/<lane>/{metrics.json,table.csv,telemetry-*.jsonl}` and was re-verified by two independent measurement refutations. Tags: CONFIRMED / PROBABLE / POSSIBLE / REJECTED / UNRESOLVED.

---

## 1. Baseline

### 1.1 Linux BASE versus the Windows real run (CR-013 / ADR-022)

- Instrumented Linux BASE (45046eb + read-only `diag-telemetry.patch`) replays seed 42 with the playtest orders to the same final hash as the pristine tool build: `e234616b…b4e9`; `cmp pristine-hash.log base/hash.log` exit 0 (sha256 `c133756d…b788`, 161 lines each). CONFIRMED: instrumentation changes no state.
- Windows trace vs Linux BASE trace: hashes agree at turns 0–1, first differ at turn 2 (as CR-013 states). Leaf-by-leaf telemetry diff (609,554 leaves): 42 differing leaves in 39 turns, all `economy.goods[].price` (33) or `social.grievance[].value` (9), max relative difference 3.67e-16 (last ulp). 0 of 1932 settlement-turns differ in closing population; world population identical at every turn (final 16948 both); every Libur t115–t120 food/population/migration value bit-identical (t117 deficit 0.29720704310868246 both; t118 outflow 641/809; t119 inflow 1213; t120 S 11.131309401039).
- Consequence (CONFIRMED on this seed only): the Windows numbers ARE the Linux BASE numbers on every population/food/migration row; the Linux BASE is the comparison baseline for every counterfactual; the Windows run is reported as the director's observed run and equals the BASE column on every summary metric (all deltas 0).

### 1.2 Step 1 extremes (BASE Linux = Windows)

| extreme | settlement / turn | value |
|---|---|---|
| maxPopLoss | 10 Libur, t118 | 809 → 69, fraction 0.914710 |
| maxPopGain | 10, t119 | 69 → 1520, fraction 21.0290 |
| maxDeficit | 10, t117 | 0.297207 |
| maxOutflowFrac | 10, t118 | 641/809 = 0.792336 |
| maxInflowFrac | 10, t119 | 1213/69 = 17.5797 |
| maxDeathFrac | 10, t119 | 539/69 = 7.81159 (opening-denominator artefact; arrivals die in the same turn) |
| maxRecoveryOvershoot | 10, t118 event | +0.948084 (closing 1576 at t122) |
| minWeatherApplied | 2 Zareshu, t56 | 0.382889 (that turn's deficit 0.0483) |
| maxWeatherApplied | 3 Teshulit, t132 | 2.12603 |

### 1.3 Distributions (N = 1932 settlement-turns; weather N = 1920)

| metric | N | min | p5 | p10 | p25 | median | p75 | p90 | p95 | max |
|---|---|---|---|---|---|---|---|---|---|---|
| weatherApplied | 1920 | 0.382889 | 0.606754 | 0.671735 | 0.798907 | 0.966799 | 1.18140 | 1.41123 | 1.56353 | 2.12603 |
| deficitRatio | 1932 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0.297207 |
| outflowFrac | 1932 | 0 | 0 | 0 | 0 | 0.00134138 | 0.00478511 | 0.0114523 | 0.0245625 | 0.792336 |
| inflowFrac | 1932 | 0 | 0 | 0 | 0 | 0.00116925 | 0.0051014 | 0.0148989 | 0.026088 | 17.5797 |
| lossFrac | 1932 | 0 | 0 | 0 | 0 | 0 | 0 | 0.00746292 | 0.019063 | 0.91471 |
| gainFrac | 1932 | 0 | 0 | 0 | 0.00180018 | 0.00760663 | 0.0142283 | 0.0255992 | 0.0384767 | 21.029 |
| deathFrac | 1932 | 0.127318 | 0.39745 | 0.402393 | 0.406578 | 0.409469 | 0.412322 | 0.417091 | 0.420704 | 7.81159 |

Summary metrics (shared definitions): famine settlement-turns >0 / ≥0.05 / ≥0.20 = 15 / 3 / 1 (the 3 are Libur t84 0.12677, t92 0.07829, t117 0.29721; Zareshu t56 0.04833 just under); 15 famine episodes, every one of length 1; loss events ≥20%: 13; recovery times [19, 1, 67, 71, 3, 1, 30, 1, 1, 1, 37, 1] mean 19.42, max 71, 1 not recovered (Libur t123); oscillation episodes 3, all Libur (t85 649→191→1033, ×4.41; t93 672→323→906, ×1.80; t118 809→69→1520, ×21.03); grossMigrationPerDecade 0.00712441 (in band [0.001, 0.01]); total outflow 11,335 person-moves; starvation deaths 246; extinctions 0; final world population 16948; crashes20 = 0 (the cascade is invisible at world level). 1077 of 1932 settlement-turns have 0 < outflowFrac < 0.05.

Whole-run facts framing everything: 58.1% of BASE outflow persons sit on outflowFrac ≥ 0.05 turns; Libur's inflow 3,623 + outflow 3,502 = 62.9% of all gross migration in the run; Libur owns 7 of the 15 famine turns and is the only repeat famine site.

### 1.4 Libur t110–t125 (BASE; turn | weather | foodProduced | required | deficit | happiness | opening | closing | births | deaths | out | in | S | A | D = Σ damping·viability)

```
110 | 1.01849  | 12733 |  6291 | 0        | 100    |  759 |  762 | 317 | 314 |   0 |    0 |  1.914 |  1.863 | 4.346
111 | 0.754748 |  9457 |  6312 | 0        | 100    |  762 |  775 | 321 | 310 |   0 |    2 |  1.888 |  1.856 | 4.356
112 | 0.836258 | 10624 |  6413 | 0        | 100    |  775 |  774 | 319 | 316 |   4 |    0 |  1.872 |  1.825 | 4.361
113 | 1.07745  | 13721 |  6413 | 0        | 100    |  774 |  781 | 324 | 317 |   0 |    0 |  1.848 |  1.827 | 4.381
114 | 0.86498  | 11117 |  6471 | 0        | 100    |  781 |  785 | 325 | 320 |   1 |    0 |  1.838 |  1.811 | 4.381
115 | 1.07836  | 13888 |  6498 | 0        | 100    |  785 |  790 | 327 | 321 |   1 |    0 |  1.824 |  1.801 | 4.382
116 | 0.779034 | 10080 |  6537 | 0        | 100    |  790 |  796 | 331 | 328 |   0 |    3 |  1.813 |  1.790 | 4.382
117 | 0.442741 |  5779 |  6588 | 0.297207 |  80.84 |  796 |  809 | 334 | 323 |   0 |    2 |  1.801 |  1.777 | 4.383   grainClosing 0
118 | 0.838171 | 11112 |  6697 | 0        | 100    |  809 |   69 |   4 | 103 | 641 |    0 |  1.789 |  1.748 | 4.384   pushDeficit 0.297207 (bit-equal to t117)
119 | 0.939037 |  1230 |   600 | 0        |  91.34 |   69 | 1520 | 777 | 539 |   0 | 1213 |  1.768 | 20.494 | 4.392
120 | 1.41965  | 36318 | 12655 | 0        | 100    | 1520 | 1571 | 664 | 613 |   0 |    0 | 11.131 |  0.930 | 4.408
121 | 1.04942  | 26909 | 12985 | 0        | 100    | 1571 | 1568 | 637 | 640 |   0 |    0 |  6.031 |  0.900 | 4.408
122 | 0.655309 | 16995 | 13000 | 0        | 100    | 1568 | 1576 | 651 | 643 |   0 |    0 |  3.465 |  0.902 | 4.409
123 | 1.29283  | 33763 | 13054 | 0        | 100    | 1576 | 1235 | 479 | 526 | 294 |    0 |  2.184 |  0.897 | 4.420   gap-driven back-flow
124 | 1.39638  | 28097 | 10197 | 0        | 100    | 1235 |  813 | 291 | 359 | 354 |    0 |  1.540 |  1.145 | 4.420
125 | 0.627005 |  8223 |  6707 | 0        | 100    |  813 |  812 | 333 | 334 |   0 |    0 |  1.343 |  1.739 | 4.429
```

Weather ordering CONFIRMED (code + measurement): weatherNext(t) == weatherApplied(t+1) bit-for-bit on 1920/1920 pairs; the t117 multiplier 0.442741 was drawn in turn 116's step (HarvestWeatherSystem writes next; ProductionSystem.cs:329-337 reads prev). Production reconciles: Libur foodProduced t117/t116 = 0.573313 = weather ratio 0.568321 × prev-adult ratio 1.009009 (residual < 0.03%, flooring); all three food goods fell by the same factor (0.5733/0.5734/0.5731). Gapi received 0.5324 the same turn (56% production drop) with deficit 0.

---

## 2. Four Counterfactuals

All lanes: Linux, 45046eb + diag-telemetry.patch, seed 42, playtest orders, 161 turns, 12 settlements on every turn, ledgers reconcile on every turn (5152 flags, 0 false), no NaN. M2b and M3b are SUPPLEMENTARY arms.

### 2.1 Headline table

| metric | BASE | M1 flight bound | M2 vacancy memory | M3 σ 0.1933 | M4 = M1+M2 | M2b (suppl.) recovery friction | M3b (suppl.) σ 0.1468 |
|---|---|---|---|---|---|---|---|
| max outflowFrac | 0.792336 (s10 t118, 641/809) | 0.299632 (s9 t2 founding, = BASE); pushDeficit>0 max = Libur t118 0.134347 (106/789) | 0.791822 (s10 t118, 639/807) | 0.372121 (s10 t118, 307/825) | 0.299632 (founding); pushDeficit>0 max = Libur t118 0.133 (104/782) | 0.795285 (s10 t118, 641/806) | 0.299632 (founding); Libur t118 3/818 = 0.004 |
| max inflowFrac | 17.5797 (s10 t119, 1213 into 69) | 1.94063 (s10 t119, 621 into 320) | 3.66129 (s10 t119, 227 into 62) | 1.37121 (s10 t119, 543 into 396) | 0.483264 (s10 t120, 231 into 478) | 20.0328 (1222 into 61) | 0.440433 (s4 t2 founding) |
| max deficit | 0.297207 (s10 t117) | 0.295138 | 0.293663 | 0.078635 (multiplier 0.5905 vs 0.4427) | 0.293711 | 0.297147 | 0.000186 (s5 t48) |
| max population loss | 0.91471 (809→69) | 0.594423 (789→320) | 0.923172 (807→62) | 0.52 (825→396) | 0.589514 (782→321) | 0.924318 (806→61) | 0.393382 (s9 t2 founding) |
| max population gain | 21.029 (69→1520) | 2.36875 (320→1078) | 4.37097 (62→333) | 1.65657 (396→1052) | 0.566787 (s4 t2 founding; Libur max t120 478→736 = 0.54) | 24.0656 (61→1529) | 0.552347 (founding) |
| famine turns >0 / ≥0.05 / ≥0.20 | 15 / 3 / 1 | 15 / 5 / 1 | 14 / 3 / 1 | 8 / 1 / 0 | 20 / 6 / 1 | 11 / 4 / 1 | 11 / 0 / 0 |
| famine episodes (mean/max length) | 15 (1/1) | 15 (1/1) | 14 (1/1) | 8 (1/1) | 20 (1/1) | 11 (1/1) | 11 (1/1) |
| loss events ≥20% | 13 | 7 | 10 | 5 | 7 | 12 | 4 |
| recovery time mean / max / not recovered | 19.42 / 71 / 1 (Libur t123) | 23.14 / 71 / 0 | 19.67 / 71 / 1 (t160, end-censored) | 31.8 / 70 / 0 | 25 / 71 / 0 | 17.73 / 71 / 1 (t124) | 40 / 71 / 0 |
| Libur cascade-event recoveries (turns) | t85 1, t89 30, t93 1, t102 1, t118 1, t123 never, t124 37, t160 1 | t85 1, t93 1, t118 1 | t85 2, t93 2, t102 2, t118 2, t160 censored | t118 1 | t85 2, t93 3, t118 2 | t85 1, t89 30, t93 1, t102 1, t118 1, t124 never, t160 1 | none |
| maxOvershoot (5-turn) | 0.948084 (t118) | 0.366286 | 0.202505 (s2 t57) | 0.30303 | 0.0409207 | 0.960298 | −0.0909 |
| oscillation episodes (gain over trough) | 3: t85 ×4.41, t93 ×1.80, t118 ×21.03 | 1: t118 ×2.37 | 3: t85 ×1.00, t93 ×1.51, t118 ×4.37 | 1: t118 ×1.66 | 1: t118 321→736 at t120, ×1.29 | 3: ×4.24, ×1.89, ×24.07 | 0 |
| grossMigrationPerDecade (band [0.001, 0.01]) | 0.00712441 | 0.00410024 (0.576×) | 0.00525657 (0.738×) | 0.00329157 (0.462×) | 0.0037295 (0.523×) | 0.00700756 (0.984×) | 0.00248572 (0.349×) |
| total starvation deaths | 246 | 565 (2.30×) | 252 | 42 | 594 (2.41×) | 242 | 0 |
| maxDeathFrac | 7.81159 (s10 t119) | 1.26562 | 2.03226 | 1.00253 | 0.616822 | 8.81967 | 0.592727 (s1 t2) |
| final world population | 16948 | 16168 (−4.6%) | 16964 (+0.1%) | 17372 (+2.5%) | 16104 (−5.0%) | 16966 (+0.1%) | 17449 (+3.0%) |
| crashes20 (world) | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

### 2.2 Derived rows (shared definitions verbatim)

| derived | BASE | M1 | M2 | M3 | M4 | M2b | M3b |
|---|---|---|---|---|---|---|---|
| D7 (pushDeficit>0 and outflowFrac ≥ 0.5 after a length-1 episode) | 2 (t85 0.544 at 0.127; t118 0.792 at 0.297) | 0 | 2 | 0 | 0 | 2 | 0 |
| pushDeficit>0 and outflowFrac ≥ 0.40 | 2 | 0 | 3 (adds t93 0.409 at 0.085) | 0 (Libur t118 0.372 at 0.079) | 0 | 2 | 0 |
| D4b: loss ≥ 0.50 then inflowFrac ≥ 1.0 within 3 turns | 3 | 1 (t118→t119 1.94) | 1 | 1 (1.37) | 0 | 3 | 0 |
| median outflowFrac (D2) | 0.001341 | 0.001228 (0.92×) | 0.001352 (1.01×) | 0.001031 (0.77×) | 0.001417 (1.06×) | 0.001230 (0.92×) | 0.000867 (0.65×) |
| n(0 < outflowFrac < 0.05) (D2 ±20% = 862–1292) | 1077 | 1100 (+2.1%) | 1121 (+4.1%) | 1067 (−0.9%) | 1145 (+6.3%) | 1054 (−2.1%) | 1016 (−5.7%) |
| n(outflowFrac ≥ 0.20) | 9 | 1 | 7 | 2 | 2 | 9 | 1 |
| cascade gain outflowFrac(t+1)/deficit at 0.01 < deficit < 0.10 | 4.25, 4.83, 4.55, 5.64, 5.13 (mean 4.88) | 0.59, 0.66, 0.73, 0.58, 0.54, 0.69 (mean 0.63) | 4.28, 5.44, 4.84, 4.64, 5.03 (mean 4.85) | 4.73 (single point) | 0.58, 0.62, 0.79, 0.59, 1.11, 0.66, 0.57, 0.52 (mean 0.68) | 4.25, 4.74, 4.63, 4.97 | none |
| Libur t118: opening / outflow (frac) / deaths / starvation / births / closing | 809 / 641 (0.792) / 103 / 41 / 4 / 69 | 789 / 106 (0.134) / 389 / 195 / 26 / 320 | 807 / 639 (0.792) / 110 / 44 / 4 / 62 | 825 / 307 (0.372) / 238 / 42 / 116 / 396 | 782 / 104 (0.133) / 384 / 194 / 27 / 321 | 806 / 641 (0.795) / 108 / 46 / 4 / 61 | 818 / 3 (0.004) / n-e / 0 / n-e / 822 |
| Libur t119: inflow / A_actual (= 2S(t+1) − S(t)) / S read by others at step 120 / closing | 1213 / 20.49 / 11.13 / 1520 | 621 / 4.42 / 3.11 / 1078 | 227 / 2.23 / 2.00 / 333 | 543 / 3.57 / 2.65 / 1052 | 121 / 2.10 / 1.96 / 478 | 1222 / 23.18 / 12.47 / 1529 | 0 / 1.72 / 1.73 / 828 |
| 3-turn cumulative outflow t118–t120 / opening118 (pre-registered window) | 0.792 | 0.231 (106+30+46) | 0.792 | 0.372 | 0.196 (104+30+19) | 0.795 | 0.005 |
| 5-turn cumulative t118–t122 (added per refutation 1) | 0.792 (no back-flow until t123: 294, t124: 354) | 0.506 (106, 30, 46, 63, 154 = 399/789; t121–122 are stress-tail plus gap back-flow after the 621 refill) | 0.792 | 0.595 (307, 0, 0, 51, 133) | 0.225 (104, 30, 19, 15, 8) | 0.795 | 0.010 |
| weatherApplied bit-identical to BASE | — | no: first diff t84 s0 (851/1920 rows; dampingSum diverges t83; Libur t117 0.4427435 vs 0.4427413) | no: t12 s1 (1674 rows; Libur t117 identical to 10 s.f.) | no by design (t2) | no: t12 s1 (M2's signature) | no: t112 s0 (329 rows) | no by design (t2) |
| first closing-population / trace-hash divergence | — | t13 / t11 | t2 / t2 | t4 / t1 | t2 / t2 | t58 / t11 | t4 / t1 |

n-e = not extracted by any lane. CONFIRMED: the weather non-identity under every population-moving arm is caused by HarvestWeatherSystem.cs:219-224 building the regional kernel from prev.SettlementDistances travel costs (dampingSum diverges one turn earlier in every lane); the RNG streams are per-settlement and unaffected; the t117 shock draw is 0.4427 to 4 s.f. in every migration-side lane.

### 2.3 What each diagnostic patch actually changed (patch lane; isolation CONFIRMED by refutation 1)

**M1 — bounded/smoothed famine flight** (`patches/m1.patch`, MigrationSystem.cs only; a bundle of four elements). (1) An in-instance stress EMA `stress_i += (prevDeficit_i − stress_i) × min(1, dt/20)` (α = 0.5 at dt = 10; a deliberate, commented statelessness violation, unserialized, unhashed). (2) Per-bucket flight FRACTION = min(0.25, 1 − exp(−0.03 × CohortProfile × 8.0 × stress × dt)) — exact hazard plus a hard 0.25/bucket/turn ceiling. (3) Flight distributed over destinations by normalised weights w_j = damping·viability / Σ_k damping·viability (Σ = 0 ⇒ nobody leaves), replacing the Σ_j sum that multiplied flight MAGNITUDE by D = 1.34–4.75. (4) Flight moved outside the gap product; skip gate `(push ≤ 0 && flightWeight ≤ 0)`; the D-037 B1 UnplacedDeparture readout became stress-gated with the bounded fraction as value. Gap channel, per-pair cap m*, S, viability, damping, overdraw scaler, transfer order, ledger calls untouched. Hashes identical to BASE turns 1–10, diverge at 11 (first prev deficit > 0 is turn 10, settlement 8). At t118 the 0.25 ceiling BINDS for profile-1.0 cohorts (stress 0.1476 → hazard 0.354 → fraction 0.298 > 0.25), so the measured 0.134 is a bundle value; analytic isolation on Libur's exact prev cohorts: normalisation + exact hazard alone → 0.288 at t118 and 0.140 at t85 (PROBABLE, no run); EMA-halved stress + ceiling → 0.151 (measured 0.134 after ClassMobility artisan netting).

**M2 — vacancy ≠ desirability** (`patches/m2.patch`, one line of logic). In-instance population memory `mem += (pop − mem) × min(1, dt/50)` (α = 0.2), first sighting mem = pop; instant attractiveness A = R / max(max(P,1), mem). R, P, the gap cap m* (raw population), S, viability, damping, flight term, transfer loop untouched. Active in ordinary population dips (hash diverges at turn 2), so it measures "vacancy memory everywhere", not only after famine. Telemetry's `instantAttractiveness` column is the BASE formula; M2's actual A was inferred as 2S(t+1) − S(t), exact on all non-shock turns.

**M3 / M3b — weather amplitude** (`patches/m3.patch`, `m3b.patch`; sim.json only). `harvestVariance.sigmaLogYield` 0.2936 → 0.1933 (the 10-year-mean log-SD of the ratified annual process) / → 0.1468 (half), plus an inert `_diagnostic` string key the default System.Text.Json loader ignores (SimConfig.cs:637-639, the same path the existing `_doc` keys rely on). correlationTimeYears, spatialSharedFraction, spatialRangeCostUnits unchanged; the multiplier stays mean-exactly-1. Consumption proven: the rebuilt Sim.Data.dll embeds the new value; hash diverges at turn 1. Sign check: the log-state x scales by exactly the sigma ratio 0.658379 (M3) / 0.5 (M3b) on every row until travel-cost drift (t62 onward in M3b, ≤ 0.0016 in log); sign(x) matches BASE on 1920/1920 (M3) and 1931/1932 (M3b) rows.

**M4 = m1 then m2** applied in sequence (composed file byte-identical to `m4-composed-m1-m2.patch` and to the live wt-m4 file). Hash diverges from BASE at turn 2 (M2's signature). It carries M1's ceiling and EMA and M2's non-adopted form; it is the nearest measured proxy of "both halves", not the design.

**M2b — recovery friction (supplementary)** (`patches/m2b.patch`). In-instance hardship EMA (α = 0.5, 20 y) of prev deficit; `viability[s] *= (1 − hardship)` applied after the three ratified gates. Shading at the refill step 119 was 7.4% (hardship 0.0743; the 14.9% figure is the step-118 value, when inflow was zero anyway — corrected per refutation 1). Measured INERT: outflow 0.795, inflow 1222, oscillation 3, overshoot 0.96, gross 0.984×.

---

## 3. Prediction vs Reality

Scoring rule (per refutation 2, replacing the earlier post hoc "causal vs ancillary" rule): the pre-registered IF/THEN/ELSE of `predictions.md` (mtime 09:29:12, before every patch 09:32+ and run 09:37–09:45) applied verbatim — CONFIRMED only if every listed threshold passes and no ELSE fires; REJECTED if an ELSE fires; UNRESOLVED otherwise. No threshold was moved; the substance each prediction tested is tagged separately.

**A — Weather.** Prediction (verbatim): "IF M3 (sigma 0.1933) THEN famine settlement-turns >=0.05 <= 0.4 x BASE and >=0.20 = 0; Libur t117 deficit <= 0.10; maxOutflowFrac <= 0.5 x BASE only because triggering deficits shrink; gross migration 0.5-0.9 x BASE, still in band. ELSE REJECTED as sufficient explanation if famine(>=0.05) remains >= 0.7 x BASE, or if any residual famine with deficit in [0.05,0.15] still yields outflowFrac >= 0.40 (cleanest kill)." Actual: famine ≥0.05: 1 vs 3 (0.33×, PASS); ≥0.20: 0 (PASS); Libur t117 deficit 0.078635 (PASS; multiplier 0.5905 = 1.00975 × 0.4427^0.658 as derived); maxOutflowFrac 0.372 vs 0.396 bar (PASS); gross 0.00329 = 0.462× BASE — in band (PASS) but below "0.5–0.9×" (MISS by 0.038×). ELSE: famine(≥0.05) = 1 < 2.1 (not fired); the one residual famine in [0.05, 0.15] (0.0786) yields outflowFrac 0.372 < 0.40 (NOT fired, by 0.028). That surviving normal-severity shock still produced a 52% population loss, a 137%-of-opening refill (inflowFrac 1.37 > D5's 1.0), overshoot 0.303 (> 0.30) and one oscillation episode. Verdict: **UNRESOLVED** (4 of 5 thresholds pass, one fails, no ELSE fired). Substance: sigma's frequency/severity effect CONFIRMED (famine ≥0.05 3→1, maxDeficit 0.297→0.079, starvation 246→42); "sigma sets frequency, not gain" PROBABLE (the kill did not fire; F unresolved on one point, 4.73 vs 4.88). M3b (σ/2): 0 famine turns > 0.01, 0 starvation, 0 oscillation, gross 0.349× — the flattened foil.

**B — Migration elasticity.** Prediction (verbatim): "IF M1 THEN Libur t118-equivalent outflowFrac in [0.05,0.40]; 3-turn cumulative outflow/opening <= 0.45; run maxOutflowFrac <= 0.40 and <= 0.5 x BASE; settlement-turns with outflowFrac >= 0.20 <= 0.5 x BASE (anchor 9); D7 count 0 (anchor 2); oscillation <= 1; D2 preserved; weather bit-identical; famine turns +-3. ELSE REJECTED if under M1 the Libur t118-equivalent outflowFrac >= 0.50, or the 3-turn cumulative >= 0.70, or maxOutflowFrac > 0.5 x BASE." Actual: t118 0.134347 (PASS; analytic centre with EMA 0.159); 3-turn cumulative 0.231 (PASS; 5-turn 0.506 because of gap back-flow 63/154 at t121–122 after the 621 refill, reported for completeness); maxOutflowFrac 0.2996 ≤ 0.40 and 0.378× BASE (PASS); n(≥0.20) 1 vs 9 (PASS); D7 0 (PASS); oscillation 1 (PASS); D2: median 0.92×, n 1100 (+2.1%), gross 0.576× in band (PASS); famine turns 15 vs 15 (PASS); weather bit-identical: MISS (t84 s0; travel-cost kernel). ELSE: none fired. Verdict: **UNRESOLVED** as a label (the single miss is a corrected wrong pre-registered assumption — predictions.md:88-90 said population feedback cannot reach the weather; it can, through prev travel costs; the event draw is identical to 4 s.f.). Substance: Step 4's HIGH-ELASTICITY classification CONFIRMED and M1 removes D7 at the identical deficit. What B does NOT establish (refutation 3): which of M1's four bundled elements is necessary — only normalisation and exact hazard have a measured/derived basis.

**C — Destination attraction.** Prediction (verbatim): "IF M2 (denominator max(P, populationMemory), slow EMA) THEN Libur instant A_119 <= 1.25 x A_118 (vs x11.72 BASE); S_119 <= 2.0 (vs 11.13); neighbour-seen gap <= 0.25 (vs 9.36); Libur t119 inflow <= 0.25 x BASE (anchor 1213); maxOvershoot <= 0.30; outflow metrics unchanged within +-1 event; oscillation <= 1. ELSE REJECTED if under M2 the Libur t119-equivalent inflow >= 0.5 x BASE (>= ~600 into ~69) or maxOvershoot >= 0.6." Actual: A-jump 2.2268/1.7523 = 1.271 (MISS by 0.021; implied memory 635 vs analytic 661 because the 50-y EMA lags a rising population); S at step 120 = 1.9988 (PASS); neighbour-seen gap 0.288 vs mean neighbour / 0.259 vs max neighbour (MISS by 0.04/0.01); t119 inflow 227 = 0.187× (PASS); maxOvershoot 0.2025 (PASS); outflow metrics within ±1 event (PASS: maxOutflowFrac 0.7918, D7 2, ≥0.40 count 3 vs 2); oscillation 3 (MISS; troughs 188/297/62 untouched by M2). ELSE: not fired (227 < 606; 0.20 < 0.6). Verdict: **UNRESOLVED**. Substance CONFIRMED: the 1/P denominator is the sole origin of the S = 11.13 spike and the refill past pre-crisis size (M2 removes 81% of the inflow; refill 227 + 405 over two turns to 837 vs pre-crisis 807). Destination attraction is a real amplifier that is NOT dominant. ADMISSIBILITY (refutation 3): M2's measured effect does not make it an admissible design form (ADR-012:73-77; queue.md:65; T2.8(b)/(c)) — evidence for Branch D, not the selected design.

**D — Interaction.** Prediction (verbatim): "IF M4 (M1 + M2, current weather) THEN oscillation = 0; D7 = 0; maxOutflowFrac <= 0.40; maxInflowFrac <= 1.0; maxOvershoot <= 0.30 simultaneously; any remaining loss>=20% event recovers gradually per D8 (2-15 turns); D2 preserved, D3 met; M4 at least as good as the better of M1 and M2 on every metric (no interaction penalty). ELSE: Strong form REJECTED if M1 alone already gives oscillation 0 and maxInflowFrac <= 1.0. REJECTED as a composition claim if M4 shows a pathology absent from both M1 and M2. REJECTED as sufficient if M4 still has oscillation >= 1." Actual: oscillation 1 (MISS: 782 → 321 at t118 = 13.3 pp outflow + 49.1 pp deaths, births 27 vs ~325 normal → 736 at t120, ×1.29); D7 0 (PASS); maxOutflowFrac 0.2996 (PASS); maxInflowFrac 0.483 (PASS); overshoot 0.041 (PASS); cascade-event recoveries 2/3/2 turns (PASS); D2 median 1.06×, n +6.3%, gross 0.523× in band (PASS); D3 famine turns 20 vs 15 = +5 (MISS: 11 new turns of which 9 are ≤ 0.000303, 2 are Libur stayer deficits t122 0.0155 / t125 0.0258; Libur t94 rises 0.0022 → 0.0525; 6 BASE turns gone), weather not identical (MISS), world pop −5.0% (PASS). "At least as good as the better single on every metric" FAILS as written: M4 beats both on maxInflowFrac (0.483 vs 1.94/3.66), overshoot (0.041 vs 0.366/0.203), maxPopLoss (0.5895 vs 0.594/0.923), D4b (0 vs 1/1), recovery (2–3 turns, no back-flow); TIES M1 on oscillation (1 = 1) and D7 (0 = 0); is WORSE than both on famine turns (20 vs 15/14), starvation (594 vs 565/252) and final population (16104 vs 16168/16964). ELSE: (1) M1 alone has oscillation 1 and maxInflowFrac 1.94 → strong-form kill NOT fired; (2) no pathology absent from both singles → composition kill NOT fired; (3) oscillation = 1 → sufficiency kill FIRED. Verdict: **REJECTED as sufficient**; necessity (both halves) and composition SURVIVED. Third channel identified: DemographicsSystem.cs:126-140 (fertility × 0.118) and :277 (starvation 0.12 × 0.294/yr) integrate the single prev deficit scalar over the whole decade (candidate 1); the gap channel on raw m* (candidate 3) supplies the 352 refill; the viability-gate reset (candidate 2) is inert (M2b).

**E — Trade-off.** Prediction (verbatim): "IF M1 THEN at Libur t118-equivalent: deaths/opening >= 0.30 (anchor 0.127); Libur t118 starvation deaths >= 150 (vs ~55-60 in BASE); run-total starvation deaths >= 1.5 x BASE (anchor 246); Libur t118 closing in [350,550]. ELSE if closing >= 600 with starvation < 100, re-open the 'demographic wiring correct' finding." Actual: deaths/opening 389/789 = 0.493 (PASS); t118 starvation 195 (Libur is the only pushDeficit>0 settlement that turn; BASE 41) (PASS); run starvation 565 = 2.30× (PASS); closing 320 vs [350, 550] (MISS on the low side). ELSE not fired. Verdict: **UNRESOLVED** as a label; the miss STRENGTHENS the hypothesis. Decomposition of M1's 59.4% loss: 13.4 pp flight, 24.7 pp starvation, 24.6 pp natural deaths, births +3.3 pp (vs ~42 pp normal). Same pattern at t85 (starvation 83 vs 33) and t93 (63 vs 46); M4 reproduces it (194; run 594 = 2.41×). The accepted "demographic wiring is correct" finding is NOT reopened.

**F — Sigma does not change per-famine gain.** Prediction (verbatim): "IF M3/M3b THEN for remaining famine settlement-turns with 0.01 < deficit < 0.10, outflowFrac(t+1)/deficit within +-25% of BASE mean 4.88; same per-cohort saturation threshold; bin-conditional outflow within +-25%. ELSE REJECTED if the conditional gain shifts by > 50%. UNRESOLVED if M3 leaves fewer than 3 settlement-turns with deficit > 0.01." Actual: M3 leaves exactly ONE (Libur t117, 0.078635); M3b none. The single point: 0.372121/0.078635 = 4.73 (BASE mean 4.88, −3%; nearest BASE comparator t92 0.0783 → 0.378, gain 4.83). Verdict: **UNRESOLVED** by the pre-registered clause (1 < 3). Evidence, not a verdict.

**G — Destination sum D and cohort saturation.** Prediction (verbatim): "IF the instrumented BASE reports damping and viability THEN D_Libur at t118 in [3.0,5.0] with viability(j) = 1.0 for all 11 neighbours (mean damping in [0.27,0.45]); at t118 cohorts 3-5 show moved/prevCount = 1.000 exactly, cohort 2 saturates iff D >= 2.80, and at t85 cohorts 3-4 saturate iff D >= 3.28; elders' outflow fraction at t118 in [0.10,0.30] while adults' in [0.85,0.95]. ELSE REJECTED if instrumented D_Libur < 2.0 at t118 or if prime cohorts show moved/prevCount < 0.95." Actual: D_Libur t118 = 4.384292 = dampingSum (all 11 neighbours prev deficit 0, happiness 100 → viability exactly 1.0) (PASS); damping vector 0.341, 0.526, 0.464, 0.841, 0.162, 0.304, 0.228, 0.185, 0.415, 0.679, 0.240 (mean 0.399, PASS); t85 D = 4.1944; Libur holds the run maximum D (4.755 at t160; run median 3.085, min 1.339). Per-cohort moved/prevCount NOT measured (the audit dumped prev buckets, not flows); with the exact prev cohorts [116,113,102,91,81,67,60,49,39,29,24,15,11,6,3,3] the clamp min(1, 2.4·profile·D·deficit) binds for cohorts 2–7 (55.6% of Libur) at 0.297; the aggregate reproduces 641 to +1.2 persons and t85's 353 to +1.0 once the same-turn ClassMobility artisan drain (64 adults withheld) is netted. Analytic elders ~0.11; adults ~0.81–0.92 (bracket not measured). ELSE not fired / not measurable. Verdict: **UNRESOLVED** as a label; core CONFIRMED (D is a magnitude multiplier of 4.4× at Libur, contradicting ADR-012's "only redistributes WHERE"), cohort detail PROBABLE. Corrected per refutation 2: the pre-registered sub-claim (predictions.md:325-326, "the ceiling is NOT what caps the 79% — it is the cohort profile") and the earlier diagnosis's reversal are BOTH wrong as either/ors: the 100%-of-bucket overdraw ceiling caps cohorts 2–7 at 100% and the cohort profile keeps cohorts 0–1 and 8–15 home; both operate, and the compositional side effect (stayers are infants and elders, 61% of whom die the same turn) follows from both together.

---

## 4. Cascade Diagnosis

**Dominant amplifier: INTERACTION** (source flight term × destination vacancy signal, coupled through the trough), selected by Branch D of the pre-registered tree.

### 4.1 Step 4 — migration-response transfer function (CONFIRMED to ±1.2 persons)

Flight desire per prev bucket = 0.03 × 8.0 × deficit_{t−1} × CohortProfile × prevCount × 10 × Σ_j damping_ij·viability_j, clamped at 100% per bucket (MigrationSystem.cs:393-403, 427-431); equivalently bucket fraction = min(1, 2.4·profile·D·deficit) at dt = 10. Response table (Libur D = 4.384, exact cohort mix; "AGG" = aggregate outflow fraction):

| deficit | linear/turn prime, D=4.384 | prime, D=1 | AGG linear D=4.384 | AGG linear D=1 | AGG exact hazard D=4.384 | AGG exact D=1 |
|---|---|---|---|---|---|---|
| 0.02 | 0.210 | 0.048 | 0.107 | 0.025 | 0.100 | 0.024 |
| 0.05 | 0.526 | 0.120 | 0.268 | 0.061 | 0.225 | 0.059 |
| 0.10 | 1.052 → 1.000 | 0.240 | 0.526 | 0.122 | 0.385 | 0.113 |
| 0.15 | 1.578 → 1.000 | 0.360 | 0.661 | 0.184 | 0.501 | 0.163 |
| 0.20 | 2.104 → 1.000 | 0.480 | 0.756 | 0.245 | 0.587 | 0.209 |
| 0.30 | 3.157 → 1.000 | 0.720 | 0.870 | 0.367 | 0.704 | 0.290 |

Unclamped demand at deficit 0.30: 160% of the population; the clamp makes it 87%. Validation: t85 naive 0.5909, clamp-aware (ClassMobility demote-first, ClassMobilitySystem.cs:226-255) 0.5454 vs observed 0.5439 (+1.0 person); t118 naive 0.8675, clamp-aware 0.7938 vs observed 0.7923 (+1.2 persons). Gain decomposition: g1 (K = 0.24/yr, 0.03 and 8.0 both underived) ≈ g3 (D = 4.384 at Libur, run range 1.34–4.75, a magnitude multiplier not the redistribution ADR-012 describes) > g4 (100%-of-bucket ceiling; strips cohorts 2–7 and leaves infants/elders: 79% exodus → 91.5% loss) > g2 (linear dt; −17 pp if exact) > g5 (memoryless one-turn scalar; "sustained" has no variable). Classification: **HIGH-ELASTICITY, CONFIRMED**; inside the letter of ADR-012 / MigrationSystem.cs:46-48 ("surge by design, bounded by the overdraw scaler alone"); the ratified texts are silent on per-event magnitude → the letter-vs-intent gap is a ruling matter. Incidental F4: the ClassMobility → Migration shared-Buckets same-turn coupling withholds demoted artisans (−7.4 pp at t118) — queue item, not a cause.

### 4.2 Step 5 — destination trace (exact re-execution of step 119 from a hash-verified t118 save; reproduces every settlement's inflow and outflow to the person)

At step 119 Libur's viability is exactly 1.0 (prev deficit reset the same step the people left: the 809 pre-flight residents' harvest of 6,508 fed t118; grain 268; happiness 100 with 1,278 housing capacity for 69); every neighbour's viability is 1.0. 100% of the destination differential is 1/P: A = 1414.10/69 = 20.494, S = 11.131 vs neighbours 1.657–1.742; gap into Libur 9.39–9.47 from every source (every other pair < 0.09). Raw gap desire into Libur 7,456; per-pair caps 0.25·m* = 95.9–127.9, sum 1,206.8; observed 1,213 (residual +6.17 = remainder carry). The cap allowed 16.2% of the desire and still delivered 17.6× Libur's population because m*_i ≈ R_L·P_i/(R_L + R_i) scales with each SOURCE's size and the destination's scale drops out; eleven caps sum to 1.53–1.59× Libur's own equalizing inflow (758–788). Post-move A 1.103, t120 0.930 — Libur becomes the least attractive settlement one step after being the most; the EMA memory (S 6.03/3.47/2.18) locks inflow and outflow at 0 for t120–t122; correction 294 + 354 at t123–124. Four ping-pong cycles in the run. F1–F5 CONFIRMED; IF(1) vacancy-as-desirability CONFIRMED with the amplification located in the cap fan-in (the T2.8 header assumption at :93-95 is pinned only at N = 4, MigrationStabilityTests.cs:194); IF(2) REJECTED; IF(3) premises CONFIRMED, remedy tested as M2b → INERT (cap-bound). Candidate targets ranked: T1 per-destination aggregate cap (same m* physics, no constant) > T2 recovery friction (unfalsifiable constant) > T3 smoothing window (insufficient alone: −1.5% at 50 y, −16% at 100 y, −37% at 200 y).

### 4.3 Decision-tree trace

- **Branch A — WEATHER dominant?** M3 cuts triggers (famine 15 → 8; ≥0.05 3 → 1; ≥0.20 1 → 0; maxDeficit 0.297 → 0.079; starvation 246 → 42) but at the surviving normal-severity shock (0.0786) the response is outflow 37.2%, loss 52%, refill 137%, overshoot 0.303, one oscillation — fails D4 and D5. A's cleanest kill did not fire (0.372 < 0.40 by 0.028); F is UNRESOLVED. M1 removes D7 with the identical draw (0.4427). M3b flattens the model (Step 12 non-goal). **REJECTED as dominant**, resting on B plus M3's D4/D5 failures; "frequency not gain" PROBABLE. SECOND FINDING recorded for ruling (§8).
- **Branch B — MIGRATION ELASTICITY dominant?** M1: D7 2 → 0; pushDeficit>0 maxOutflowFrac 0.792 → 0.134 at the same deficit; maxPopLoss 0.915 → 0.594; oscillation 3 → 1; maxInflowFrac 17.6 → 1.94 (−89%, because the vacancy is 320 not 69); overshoot 0.95 → 0.37; D2 preserved; famine 15/15. **FIRST-ORDER amplifier, CONFIRMED** — but M1 alone fails D4 (osc 1; D4b 1), D5 (1.94 > 1.0; 0.366 > 0.30), D8 (1-turn refill spikes then back-flow; 5-turn cumulative 0.506) → continue.
- **Branch C — DESTINATION ATTRACTION dominant?** M2: inflow −81% (227), overshoot 0.20, S 2.0, refill over 2 turns to pre-crisis; but maxOutflowFrac 0.792, maxPopLoss 0.923, D7 2, oscillation 3. **CONFIRMED amplifier, REJECTED as dominant (second-order)** yet independently defective (under M1 alone A = 1414/320 = 4.42 still pulls 621 into 320).
- **Branch D — INTERACTION?** (i) M1 and M2 move disjoint metric sets: YES. (ii) Pre-registered wording "at least as good as the better of M1 and M2 on every metric": beats both on inflow/overshoot/loss/D4b/recovery; ties M1 on oscillation and D7; worse than both on famine turns (noise-level, verified), starvation, final population. The "no interaction penalty" clause FAILS as written; the strong-form and composition kills did NOT fire; M4 is strictly better than M1 on the destination-side criteria and strictly better than M2 on the source-side ones → both halves necessary, neither alone meets D4/D5/D8. **BRANCH D SELECTS THE STATUS.** Residual M4 episode (trough 321 = 49 pp deaths + 39 pp missing births, refilled 352 + natural +112, ×1.29, recovered in 2 turns, overshoot 0.04) lies in Demographics' decade integration, outside migration.
- **Step 11** (all ≈ BASE?): NO — the only null arm is M2b, itself a finding (cap-bound refill).
- **Step 12** (calibration principle on M4 as proxy): ordinary migration preserved (median 1.06×, n +6.3%, gross 0.0037 in band; the removed 48% of gross is the surge movers and their mirror refills); famine occurrence preserved and slightly raised because stayers eat; volatility preserved (weather untouched; per-event response 3–5% outflow for deficits < 0.10, 13% outflow + 25% starvation at 0.29); recovery gradual; trade-offs starvation 2.41×, final population −5.0%, Libur single-shock loss still 59% but death-driven (E).

Ranking inside the interaction: source flight term first-order (necessary and largest); destination vacancy signal second-order (necessary for D5/D8); Demographics decade integration third (residual, outside migration).

### 4.4 Adversarial refutation outcome

- **Refutation 1 (patch isolation / measurement validity): NOT refuted.** Every headline and derived row reproduced from the run files; metrics script reproduces `metrics.json` byte-identically on BASE/M1/M4; every lane worktree's Observability diff is byte-identical to `diag-telemetry.patch`; M4 recomposed and byte-compared. Four text corrections applied: M2b shading 7.4% at step 119; M1 5-turn cumulative 0.506 added beside 0.231; M4 famine-turn reconciliation 11 new / 6 gone / 9 tiny; m3/m3b's inert `_diagnostic` key.
- **Refutation 2 (pre-registration integrity): NOT refuted, five mustChange items applied.** The earlier "causal vs ancillary" scoring rule was post hoc → verbatim scoring restored (A, B, C, E, G now UNRESOLVED as labels); packet-acceptance bars D3 (±3) and D4 (oscillation = 0 AND D4b) restored with the relaxations presented as explicit Director asks; Branch A downgraded to PROBABLE with the non-firing kill stated; G rewritten as both-operate with the reversal flagged; a replacement for D3's weather-identity clause stated. Branch outcomes, dominant amplifier and status are IDENTICAL under both rules (no ELSE clause was mis-evaluated).
- **Refutation 3 (governance / minimality): REFUTED the earlier selectedTarget as a one-target minimal design; accepted in full.** The source half is reduced to its two evidenced elements (normalisation, exact hazard) with the ceiling and stress EMA marked OPEN; the destination half's form deferred until a T1-only arm has run; M2 not adopted as a design form with the ADR-012:73-77 / queue.md:65 / T2.8(b)(c) precedent written out; CR-003 §8.5 and ADR-021 / D-037 B1 added to the ruling list; Branch D condition (ii) restated in the pre-registered wording with the tie and the failed clause; the flat per-turn ceiling marked unsupported by any isolating counterfactual.
- **Design challenge (on the redesign, §5): REFUTED the design as written** (two load-bearing findings: the flight integrator and the bit-identity claims), without touching the diagnosis or the status. Disposition in §5.4.

---

## 5. Proposed Redesign

### 5.1 IF/ELSE tree to the smallest required change

```
IF weather dominant (M3 removes gain, A's kill fires)            → weather calibration ruling
   ELSE (M3 removes triggers only; A's kill NOT fired; F unresolved) → continue
IF migration elasticity alone sufficient (M1 meets D4/D5/D8)      → migration redesign only
   ELSE (M1 fails D4: osc 1, D5: inflowFrac 1.94, D8: 1-turn spikes) → continue
IF destination attraction alone sufficient (M2 meets D7/D1)        → destination redesign only
   ELSE (M2 leaves D7 = 2, loss 0.923)                             → continue
IF M1 and M2 reduce DIFFERENT parts AND M4 beats each on the       → INTERACTION: redesign BOTH halves,
   other's metrics, strong-form and composition kills not fired       ONE locus (MigrationSystem.cs)
   ELSE                                                            → STOP, additional evidence
```
Outcome: INTERACTION. Smallest required change: two mechanisms in one system, one file of logic, one ADR in the ADR-012 line, plus the state/schema plumbing a new row requires (if the stress state is adopted — see the challenger's sequencing verdict in §5.4).

### 5.2 Mechanism A — famine-stress flight (source side; MigrationSystem.cs:393-404 desire, :415-431 transfer, :333-341 readout)

Notation: d_i = prev DeficitRatio; N_b = prev bucket count; damping[i,j] = exp(−TravelCost/25); viability[j] as today (:225-235); K = BaseRatePerYear × FamineFlightFactor = 0.24/yr per unit deficit (both constants NOT moved).

- **A1 Tolerance and stress** (new per-settlement `FamineStressRow(Settlement, Value)`, read from prev, written before the n < 2 return): x_i = max(0, d_i − θ); σ_i = x_i + (σ_i,prev − x_i)·exp(−dt/τ) — the closed form of dσ/dt = (x − σ)/τ; unit DC gain, bounded by 1 − θ; absent row = 0; Colonization does not seed it.
- **A2 Exit openness and destination shares** (replaces the Σ_j at :397-403 that multiplies flight MAGNITUDE): ω_i = max_{j≠i} damping[i,j]·viability[j] ∈ [0, 1]; w_ij = damping·viability / Σ_k damping·viability (Σ_k = 0 ⇒ nobody leaves; the D-037 B1 readout owns the demand). ω is a value-max, not an argmax (no composite-key tie-break needed; permutation test ships anyway).
- **A3 Per-year hazard with a per-year ceiling**: h_i = min(H_max, K·σ_i); h_b = CohortProfile[c]·h_i·ω_i.
- **A4 Exact integration**: φ_b = 1 − exp(−h_b·dt) ∈ [0, 1); F_b = φ_b·N_b; flight b→j = F_b·w_ij. The gap channel keeps its exact current form (the flight addend inside the product at :402 is removed; when d_i = 0 it was exactly +0.0). Overdraw scaler, floor + MigrationRemainder, ClampToAvailable, pinned ascending (src, dst, bucket) order unchanged.
- **A5 D-037 B1 readout**: condition unchanged (deficit > 0 AND no reachable viable destination, ADR-021:98 binary); VALUE = N_b·(1 − exp(−CohortProfile·h_i·dt)) with ω = 1. The m1 patch's stress-gated condition is NOT adopted.

Constants (S8 §4.1(c) style; all CHOSEN, TUNE, sim.json "migration"): τ = famineStressTimeConstantYears 10.0 (frame [5, 20]); θ = famineStressTolerance 0.05 (frame [0.02, 0.10]; 0 = null arm; the seed-42 Libur t101 event 0.0499 sits at this threshold, so arms 0 / 0.02 / 0.05 must be run); H_max = famineFlightHazardCapPerYear 0.06 (inert by construction on seed 42: max σ 0.188 at θ 0, 0.156 at θ 0.05; ≥ K = 0.24 is the null arm). See §5.4 for the challenger's rulings on H_max and on the stress state.

### 5.3 Mechanism B — basin-aggregate gap cap (destination side; MigrationSystem.cs:348-382)

- **B1 Basin** of destination j: B_j = { i : gapDesire_ij > 0 } — exactly the pairs that receive a gapScale in the existing first pass (which already computes gapDesire at :365-372).
- **B2 Joint-equalizing inflow** (derivation, no constant): with P_B = P_j + Σ_{i∈B_j} P_i and R_B = R_j + Σ R_i, the shared per-capita level is a* = R_B/P_B, so M*_j = max(0, R_j·P_B/R_B − P_j). For |B_j| = 1 this is algebraically identical to the T2.8(a) pair form m*_ij (verified numerically to 9 decimals; NOT bit-identical — see §5.4). For k identical sources the pair caps SUM to (k+1)/2 × f·M*_j exactly (k = 11 → 6.0×; the audit measured Σ m* = 6.1–6.4× at Libur t119): the destination's effective damping fraction under BASE is f·(k+1)/2, reaching 1.0 at k = 7 and 1.5× at k = 11.
- **B3 Aggregate cap** (second pass over destinations ascending, sources ascending): G_j = Σ_{i∈B_j} gapScale_ij·gapDesire_ij; if G_j > f·M*_j then gapScale_ij *= f·M*_j/G_j for every i in B_j (proportional across sources, the discipline the overdraw scaler uses across destinations). Structural property: P_j + f·M*_j < R_j/a*, so the destination stays above the basin's joint level after the move — no overshoot at the destination, hence no t120–t122 lock-in and no correction outflow. f reuses GapClosingFraction 0.25 — no new constant.
- **B4 Vacancy vs attractiveness** (Step 8): desirability unchanged (S = EMA of R/P); what changes is CAPACITY — a vacancy can absorb at most f × its own equalizing inflow per turn however many sources want in. Recovery friction rejected (M2b inert; no physical referent beyond the T3.8 housing factor already inside viability); population floors rejected (ADR-012:73-77); crowding term rejected (T2.8 c).

Why not a simple multiplier on flight: it scales only g1; ×0.5 still yields ~65% of Libur (prime buckets still saturate), ~15% needs ×0.08 — a Libur fit (CR-003 §5.4; t4.12 §2c STOP); it leaves the neighbour-count scaling, the linear integrator, the 100%-of-bucket ceiling with its compositional side effect, and the 1,213-person cap-bound refill untouched; and it is law 2's free-floating buff in its purest form.

### 5.4 Challenger's verdict on the design (REFUTED as written; diagnosis and status unaffected)

Load-bearing:
1. **Flight integration is not exact (REJECTED as written).** The design applies the END-OF-TURN stress over the whole turn (a rectangle rule): only the state composes across dt, not the flow. Re-derived from the design's own formulas: prime bucket 1000, deficit 0.30 held, θ 0.05, τ 10, ω 1 — one dt = 10 step moves 316 persons; ten dt = 1 steps move 213; test 1(b) is false by ~100 persons; a 19% integrator drift at the dt10→dt5 era boundary. The exact form is one closed-form evaluation: I_i = x_i·dt + (σ_prev − x_i)·τ·(1 − exp(−dt/τ)); φ_b = 1 − exp(−CohortProfile·K·ω_i·I_i) (composes to the ulp: 0.1981 both ways). Under it the headline Libur expectation changes materially: t118 ≈ 0.088 (71 persons) not 0.145; t119 tail ≈ 0.095 not 0.057. mustChange: adopt the integral form and re-derive every number in §6 and tests 1(b), 2, 3, 9, 10; add an era-boundary continuity pin in the ADR-016 §7 family.
2. **Bit-identity for |B_j| = 1 does not follow from arithmetic (REJECTED).** Over 100,000 random single-source pairs f·m* ≠ f·M* in bits in 36,423 cases and the basin pass WOULD FIRE in 17,800, perturbing gapScale by (1 − ε) and hence MigrationRemainder and the hash on every deficit-free single-source rig. mustChange: an explicit `if (basinSize < 2) continue;` branch (bit-identity by branch), or tolerance assertions; note GapCap (MigrationStabilityTests.cs:226-253) currently pins a range, not a value.

Material:
3. **"Structurally no overshoot" is destination-only.** The mirror over-count exists on the SOURCE side and is untouched: one poor source with k richer destinations keeps k pair caps summing to (k+1)/2 × its own equalizing OUTFLOW (k = 11 → 1.5× at f = 0.25); the canonical world has k = 11; stability is pinned only at N = 4. mustChange: add the symmetric source basin cap in the same ADR or queue it with the numbers; withdraw the class claim.
4. **H_max fails the repository's unfalsifiable-constant precedent** (T4.10 / CR-003: it cannot bind on any available run) and is NOT inert in the existing suite: `DemographyRetuneTests.Mortality_PositionIndependent_MidTurnMoversStillDie` (:509-585, FamineFlightFactor 80, deficit 1.0, asserts inflowB > 150,000 of 160,000) moves 62,101 under H_max 0.06 — FAILS, absent from the blast radius. The extreme-regime numbers hung on it are overstated (15–22%/decade, not 26%) and test 10 arm X's "≥ 20% for at most 6 turns" is vacuous (zero turns ≥ 20%). mustChange: drop H_max from this packet (its null arm is bit-identical on seed 42) or pre-register a rig that binds it plus the DemographyRetune re-rig.
5. **Minimality: FamineStress (schema v24→v25, ~17 touch points, every golden re-pinned) is not part of the minimal change.** The diagnosis's own O2 ruling says stress is not needed for D7 = 0; arm 1b (share × openness + exact hazard, no state) is pre-registered at Libur t118 0.251 / t85 0.120, inside the [0.05, 0.40] bar; the only evidence for a stress variable is a regime (famine episodes ≥ 2) that occurs in ZERO settlement-turns of any lane. mustChange: sequence the packet — arm 1b + basin cap is the evidenced, schema-free change; FamineStress becomes a follow-on gated on a MEASURED sustained-episode regime (seeds 1/2, collapse rigs).
6. **θ is a threshold on a TURN-averaged intensity**, dt-denominated by construction (one year at deficit 0.5 in an otherwise-fed decade: x = 0.45 at dt = 1, 0 at dt = 10); declare it honestly in the S8 §4.1 dimensional declaration, not as "dimensionless, dt-invariant".
7. **ω = max keeps viability as a magnitude term**; only pure normalisation makes ADR-012:58 literally true; rewrite the ADR sentence rather than claim it satisfied.

Minor: test 19's rig text (H_max must be ≥ 0.96 with FFF ×4, since K·σ = 0.576 binds 0.24); the readout changes colonization party size ~5× (deficit 1.0 prime: 0.451 vs 1.0 clamped) — record in ADR-024; "deficit-free turns bit-identical" holds world-wide, not per settlement after any famine (σ never returns to exactly 0).

Challenger-CONFIRMED strengths: Law 1 (Ledger.Transfer with ClampToAvailable unchanged); Law 5 (no RNG, no argmax over doubles, NaN impossible); Law 6 (prev tables only); every listed plumbing site is real (WorldState.cs 432/794/919/986/1029/1058/1093/1122, SystemCatalog.cs 246/271, PathBuildSystem.cs 342, WorldStates.cs 43, CanonicalSchema tail = Structures :561/:986, Version 24 at :95); the stress closed form composes at the state level (0.15803013970713942 both ways); basin algebra (k = 1 reduces algebraically; Σ pair caps = (k+1)/2·f·M*_j exactly; M*_j > 0 whenever any pair cap is positive); ADR-012's gates, damping, overdraw scaler, MigrationRemainder, ADR-021:98 binary condition genuinely untouched; the neighbour-count magnitude multiplier removed is real and undocumented; Step 12 volatility not flattened; the "why not a simple multiplier" argument is sound.

**Resulting selected minimal change (design as amended by the challenger):** (S1) share-of-destinations normalisation with ω = max (or pure normalisation, arm 1a — Director question 2), (S2) exact hazard on the turn INTEGRAL of the driver (with no stress state the integral is x·dt, i.e. φ = 1 − exp(−CohortProfile·K·ω·max(0, d − θ)·dt)), (B) basin-aggregate gap cap with an explicit |B_j| < 2 skip, plus the readout value change (A5). OPEN / deferred: FamineStress state and τ (follow-on packet gated on a measured sustained regime), H_max (dropped unless a binding rig is pre-registered), the source-side symmetric basin cap (same ADR or queue). No constant is fitted to Libur; 0.03 and 8.0 are not moved.

---

## 6. Expected New Behavior

All figures in this section are EXPECTATIONS reasoned from the transfer function (reconstructed to ±1.2 persons) and the design equations, not measurements. Where the challenger's integral form changes a number, both are given.

### 6.1 The Libur 1170 event (t117–t124) after the redesign

- **t117**: unchanged (weather untouched): multiplier 0.4427, deficit 0.2972, happiness 80.8, no migration response yet (prev deficit 0).
- **t118 (opening 809)**: design as written (endpoint stress, τ 10, θ 0.05): prime hazard 0.0375/yr × openness 0.841; aggregate 116.9 persons = 14.5% (θ 0: 17.0%; τ 20: 9.4%) minus ~2 pp ClassMobility netting → outflowFrac ~0.12–0.15 [BASE 0.792; M1 measured 0.134]. Challenger's integral form: ~0.088 (71 persons). Arm 1b (no stress, ω = max): 0.251; arm 1a (pure normalisation): 0.288. Stayers ~690–740; Demographics then integrates the same 0.297 over the decade (fertility × 0.11, starvation 0.12 × 0.297/yr): deaths ~380–390 (M1 measured 389), births ~26 → closing ~320–330, a ~60% loss of which ~13 pp flight and ~47 pp deaths and missing births (finding 3, outside migration). Compositional side effect gone: no cohort stripped, the remnant is not infants and elders.
- **t119 (opening ~325)**: stress tail flight ~5.5% (design) / ~9.5% (integral form). Inflow: A = 1414/325 = 4.35, S = 3.06 vs neighbours ~1.71; basin M*_j ≈ 470 → aggregate cap ≈ 117 (pair caps would sum to ~770); expected inflow ~115–125 (inflowFrac ~0.35–0.4) [BASE 1,213 = 17.6×; M4 121 + 231]. Natural growth of the young remnant ~+50 → closing ~470.
- **t120–t123**: caps ~83, 53, 27 as P rises 470 → 600 → 710 → ~800; equilibrium ~780–850 around t122–t123 with the cap zeroing itself; no overshoot, no lock-in, no back-flow (BASE's 294 + 354 out at t123–124 disappear).
- **Pre-registered bars**: D7 = 0; D5 maxInflowFrac ~0.4 ≤ 1.0 and 5-turn overshoot ~+0.05 ≤ 0.30; D4b none; D8 recovery to 90% of the t118 opening (728) at ~t122 = 4 turns. D4 "oscillation = 0" is EXPECTED TO STILL COUNT ONE EPISODE (trough ~325 → t121 ~710 = +118%), because 47 pp of the trough is deaths and a young remnant regrows ~15%/decade naturally on top of the capped inflow — Director ask (i), restated not relaxed.
- **Other Libur events**: t85 (0.127): ~4.8% [BASE 54.4%]; t93 (0.078): ~1.8% [37.8%]; t102 (0.0499): 0 at θ 0.05, ~2% at θ 0.02 [22.7%]; t152/t160: 0 at θ 0.05 [6.8%, 24.2%]. Cascade gain at 0.01 < deficit < 0.10: 0–0.35 [BASE mean 4.88; M1 0.63].
- **Run level**: gross migration ~0.0025–0.0040 (0.35–0.55× BASE) — in band but AT RISK against the pre-registered D2 floor ≥ 0.4× (M4 measured 0.523× without a basin cap; arm 4 measures the basin cap alone); starvation deaths toward ~500–600 (M1 565, M4 594; trade-off E); final world population ~−4 to −5%; famine settlement-turns ~+2 to +5 (fails restored D3 ±3 as M4 did — Director ask (i)). ADR-012 collapse teeth stay true: no viable destination → ω = 0, nobody leaves; a starving destination receives exactly zero.

### 6.2 Step 12 behaviours (expectations)

- **Normal shock** (one decade, deficit 0.05–0.15): flight 0–6% of the settlement in the shock turn (prime adults 0–11%), a 1–2% tail over the next two turns (with stress) or none (arm 1b); starvation and fertility suppression exactly as today; the vacancy refills at ≤ f × its own equalizing inflow per turn, never past pre-crisis size; no back-flow. "Moderate demographic response."
- **Persistent severe shock** (deficit 0.2–0.4 for 2–3 turns): with stress, per-turn flight rising 12% → 18% → 20% of the settlement; three-decade cumulative 40–50% plus starvation 24–48% per decade; the settlement shrinks by well over half and stays small while the crisis lasts. Without stress (arm 1b) each turn repeats the single-shock response on the survivors. "Substantial migration, decline, destabilization." UNTESTED on seed 42 (D6 = 0 in every lane).
- **Extreme sustained crisis** (deficit ≥ 0.5, several turns): flight 15–22% of the settlement per decade (challenger-corrected; not 26%) plus starvation ≥ 45% per decade → collapse within 2–3 turns; if every reachable destination is non-viable, ω = 0, nobody leaves, the D-037 B1 readout carries the bounded demand and people die at home (ADR-012). The CollapseStabilityTests streak bar (≤ 8 consecutive ≥ 20% turns) expected to hold; the arm-X assertion as written is vacuous and must be replaced.
- **Recovery**: geometric approach to the basin equilibrium at f = 0.25 of the remaining equalizing inflow per turn (~4–6 turns for a 60% vacancy, faster with natural growth), structurally no destination overshoot, no correction outflow. "Gradual repopulation."
- **Ordinary migration (D2)**: gap desire, S, damping, viability unchanged; single-source basins bit-identical to BASE by branch; multi-source destinations whose pair caps bind receive up to (k+1)/2 less per turn — median outflowFrac and n(0, 0.05) expected inside ±20% of BASE while gross may fall toward 0.35–0.55× (measured, not assumed). Density dependence and stochasticity still interact; what is removed is the single-turn amplification chain, not the environmental risk.

---

## 7. Regression Tests

Ten named tests (setup / assertions), as amended by the challenger. Numbers marked † must be re-derived under the integral-form hazard before pinning.

1. **FlightFraction_ExactHazard_NeverSaturatesABucket** — Two settlements, source = one prime bucket (cohort 4) of 1000, destination fed and equally attractive (gap 0), travel cost 0. Case A deficit 0.30; case B deficit 1.0 with H_max inert. Asserts: A outflow = round(1000·(1 − exp(−K·x·dt))) ± 1†; B < 1000 always; the linear mutant (rate × count × dt then overdraw clamp) yields 1000 in B — kill.
2. **FlightIntegral_DtComposition** (replaces the design's test 1(b) after the challenger's finding) — Same rig, deficit 0.30 held; (a) one dt = 10 step, (b) ten dt = 1 steps via FlatEra(1.0), (c) two dt = 5 steps. Asserts: persons moved equal within ±1 per bucket across (a)/(b)/(c) (the endpoint-stress form fails by ~100 of 316); era-boundary continuity in the ADR-016 §7 family.
3. **FlightTotal_IndependentOfNeighbourCount_SharesTieDense_PermutationInvariant** — Source 16 × 1000 at deficit 0.30; W1: one fed destination at cost 5; W11: eleven identical destinations at cost 5 (dense tie); W11r: same with reversed ids. Asserts: outflow(W11) == outflow(W1) ± 11; each W11 destination receives total/11 ± 1; W11r inflows are the exact permutation and the total bit-identical; the BASE form moves ~11× W1 up to the clamp — the g3 kill.
4. **ExitOpenness_IsTheBestRefuge_NotTheSum_AndZeroWithoutRefuge** — Source at deficit 0.30; D1 at cost 5 (damping 0.819), D2 at cost 60 (0.091); variants D1 only / D2 only / both / both food-less. Asserts: outflow(D1+D2) == outflow(D1) ± 1; D2's share == 0.091/0.910 within floor slack; D2-only == closed form with ω 0.091 ± 1; no viable destination → outflow 0.
5. **UnplacedDeparture_BinaryGate_BoundedValue_ADR021** — Source deficit 0.30, single non-viable destination; turn 2 deficit 0; variant with one viable destination. Asserts: turn 1 every bucket's UnplacedDeparture == N_b·(1 − exp(−CohortProfile·K·x·dt)) within 1e-12 (ω = 1)†; turn 2 all zero (gate stays on deficit > 0); with a viable destination all zero and the people move. Also pins the ~5× smaller party size.
6. **NormalShortage_BelowTolerance_NoFlight_NullArmProvesTheTolerance** — Rig of test 1 at deficit 0.03; θ 0.05 vs θ 0. Asserts: θ 0.05 → outflow 0; θ 0 → prime outflow 1000·(1 − exp(−0.24·0.03·10)) ± 1†; 0 recovers the pure filter.
7. **BasinCap_EqualsPairCap_WhenOneSource** — The existing GapCap_PairGrossFlow rig (TwoSettlements 2000 vs 10, R_dest 10,000). Asserts: moved ≤ f·m* + 16 and ≥ 0.9·f·m* as today AND MigrationFlows bit-identical to the pre-redesign value via the explicit |B_j| < 2 skip branch (the arithmetic alone fires in 17.8% of random pairs — challenger).
8. **BasinCap_BoundsFanIn_LowPopulationDestination** — Eleven AdultsHeavy(100) sources (1,600 each), one fed destination of 80, equal land (R = 1,000), all costs 5; first sighting S = A so the gap is maximal. Asserts: Σ pair caps = 2,090 would exceed the destination 26-fold; M*_j = 1000·17,680/12,000 − 80 = 1,393.3; inflow ≤ 0.25·1,393.3 + 11 = 359 and ≥ 330; post-move A_dest ≥ the basin's joint level 0.679; each source's outflow == total/11 ± 1; the mutant without the basin pass moves ≥ 2,000 — teeth.
9. **Recovery_GeometricRefill_NoOvershoot_NoBackflow** — Fan-in rig of test 8 stepped 8 migration-only turns with all deficits 0. Asserts: per-turn inflow non-increasing after turn 1; destination population never exceeds P_j* = R_j·P_B/R_B recomputed each turn; destination outflow 0 every turn; ConsecutiveAlternationCount(net) == 0; ≥ 90% of P_j* within 8 turns (0.75^8 = 0.10).
10. **Seed42_FoundedReplay_161Turns_CascadeCorridor** — Founded seed 42 on the new build with the three playtest SectorAllocation orders (t21, t33, t80), 161 turns, telemetry emitted; also seeds 1 and 2 with no orders. Asserts: (a) hash log equals the re-pinned golden; (b) per-event corridor on every pushDeficit > 0 settlement-turn: outflowFrac ≤ 0.40, Libur t118-equivalent in [0.05, 0.32] (non-vacuous; upper bound covers arm 1a's 0.288)†; D7 == 0; after any loss ≥ 0.5, inflowFrac ≤ 1.0 within 3 turns (D4b/D5); 5-turn overshoot ≤ 0.30 for every loss ≥ 20% event; recovery of the t118 event in [2, 15] turns (D8); gross in [0.001, 0.01]; (c) weather replacement criterion: Libur t117 weatherApplied == BASE 0.442741 to 4 s.f. and the first weather difference preceded by a dampingSum divergence; (d) REPORTED, asserted only after Director ruling (i): famine settlement-turns vs 15 (restored D3 ±3), oscillation episodes (restored D4 = 0), starvation deaths, final population, median outflowFrac and n(0 < x < 0.05) vs BASE (D2 ±20%), gross vs 0.4× BASE; per-bucket flows dumped so cohort saturation is measured, not inferred.

Further tests carried in the design (to ship if the corresponding element is adopted): FamineStress_ExactRelaxation / DecaysBelowTolerance and SchemaV25_PopulatedFamineStress_ExactLengthRoundTripHash (three rows: 0.1875, 0.29720704310868246, 5e-324; exact ExpectedLength, bit-exact round trip, hash equality) — only with the stress state; Determinism_SaveLoadMidFamine_AndIdenticalWorlds_HashEqual; NormalMigration_DeficitFree_SingleSourceBasins_BitIdentical (by branch); ExitBeforeDeath_ReDerived_LiftsT41bSkip (MigrationTests.cs:378-457, flight-only attribution); Overdraw_ScalesProportionally_ReRigged (MigrationTests.cs:789-827; rig H_max ≥ 0.96 with FFF ×4 — challenger correction); FamineFlight_FiresWithZeroGap_AndScalesWithTheFactor kept unchanged; DemographyRetuneTests.Mortality_PositionIndependent_MidTurnMoversStillDie added to the blast radius (needs H_max ≥ 2.4 in its cfg if H_max exists at all); a FlightHazardCap test only if H_max survives with a binding rig.

---

## 8. Governance Check

### 8.1 Untouched (each confirmed against the files read)

- **CR-003**: ship yield 26.0 — untouched. Weather ruling §3 / §5.2(b) and HarvestWeatherSystem.cs — untouched (finding 2 is framed separately, below). §7.3 transient-starvation vs Malthusian-crash definitions — used unamended (Crashes(m, 0.20) = 0 in every lane; the Libur event is transient starvation; the §8.2 quarantine is not invoked). §5.1/§5.4 — no constant fitted to a corridor; BaseRatePerYear 0.03 and FamineFlightFactor 8.0 are not moved.
- **Food preservation / granary (T4.2, CR-004)**: ConsumptionSystem, GoodStocks, granaryYearsOfDemand 1.5, spoilage 0.08 — untouched.
- **D-035-A consumption basket** — untouched.
- **Founding food and founding demographics** (WorldFounding, Founding config) — untouched; a new settlement's stress (if adopted) starts at 0 by absence, not seeded.
- **Density calibration** (corridors.json densityPerArableKm2) — untouched.
- **M5 code**: the D-021 Grievances table stays behaviour-inert; no grievance stock read or written.
- **Food production semantics** (ProductionSystem.cs, shared harvest-weather multiplier at :171-175/:239) — untouched; the accepted prior finding is corroborated, not reopened.
- **The 10-year atomic turn (CR-006)** and **0.5-year demographic microsteps** — untouched; finding 3 is reported, not changed.
- **Determinism gates (ADR-006)**, **serialization architecture (ADR-005 chokepoint, ADR-020 clone)** — followed, not changed (a table appended at the stream tail through the three-edit rule, if adopted).
- **Kernel §3.2 prev-read**; **ADR-011/ADR-016** applied, not amended; **T4.10** (food term removed from A), **T4.13** (happiness weight 0.15), **ADR-018 spacing** — untouched.
- **corridors.json:81-98** band "ACCEPTED AS MEASURED, NOT tuned" and **m2-spec.md:36** — text unchanged; the band is not re-cut.

### 8.2 Frozen / ratified text the proposal touches — each needs a Director ruling before code (t4.12 §2c: a change to a ratified mechanism's strength is a STOP, not TUNE)

1. **ADR-012 "D-021 Exit-valve preservation" and its code letter** (MigrationSystem.cs:46-48; SimConfig.cs:428-429): "the Exit valve is a surge by design, bounded by the overdraw scaler alone" → "bounded by an exact hazard, distributed by share × openness". Whether S1 is a restatement of "viability only redistributes WHERE the fleeing go" or a change to it is ruling item (ii); under ω = max the ADR sentence must be rewritten, not claimed satisfied (challenger). D-021 doctrine §3 ("openness of exits … transport reach governs the valve", d021-stability-doctrine.md:28) is honoured by the openness term.
2. **T2.8(a) header assumption** (MigrationSystem.cs:93-95, "the collective inflow stays inside the basin — pinned empirically by the oscillation regression tests", pinned only at N = 4 in MigrationStabilityTests.cs:194) becomes structural — same ADR. The challenger's source-side mirror ((k+1)/2 over-count on fan-out) must be ruled in or queued.
3. **CR-003 §8.5** (cr-003.md:506-517, director ruling 2026-09-05): migration "provisionally accepted for M4 with the causal explanation open; it was not tuned, no constant was moved … no replacement cause is asserted". This diagnosis asserts a cause (Libur's ping-pong = 62.9% of run gross migration; 58% of outflow persons in surge turns; destination fan-in over-count (k+1)/2) and proposes changing the §8.5-accepted mechanism inside M4 — the ruling must explicitly reopen §8.5.
4. **ADR-021 / D-037 B1** (adr-021.md:97-99, schema v22): the UnplacedDeparture readout at MigrationSystem.cs:338-341 shares the flight-desire form; its binary condition stays; its VALUE changes to the bounded fraction × count and its party size shrinks ~5× — a change to a ruled readout, to be written into the ADR. The m1 patch's stress-gated condition violates the ruled condition and must not be adopted.
5. **CanonicalSchema v24 → v25** (D-008; a mid-milestone bump is the Director's decision) — ONLY if the FamineStress state is adopted; the challenger's sequencing verdict defers it.
6. **A NEW per-event corridor per ADR-014 / S8 §4.1** (definition first, band second), because migrationGrossPerDecade cannot see the pathology (BASE in band at 0.0071 while removing 79% of a settlement); proposed: maxOutflowFrac after a length-1 famine episode ≤ 0.40; maxInflowFrac into a settlement that lost ≥ 30% ≤ 1.0. Plus a ruling on the pre-registered D2 floor (gross ≥ 0.4× BASE): is BASE's fan-in over-count "ordinary migration" to preserve, or part of the defect?
7. **Acceptance bars D3 (famine turns ±3) and D4 (oscillation = 0)**: as measured (M4) and as expected (§6) the redesign fails both on seed 42 for a reason no migration-side change can remove (a death-driven ~60% trough from Demographics' decade integration followed by natural regrowth). Director ask (i): relax D3 to ±5 and D4 to the D4b clause; or widen the packet to finding 3; or redefine oscillation with an absolute trough floor. The diagnosis does not choose.
8. **T4.1b skip** (MigrationTests.cs:378) names "M4 migration" as owner — this packet lifts it.
9. **S8 §4.1 items for the packet spec**: dimensional declaration (θ is a threshold on a TURN-averaged intensity; τ years; H_max per year if kept); coupling map Consumption → Migration (DeficitRatio), Catchment → Migration (SettlementDistances, EffectiveArableKm2), GoodStocks/Housing/Happiness → viability, Migration → Buckets (shared with Demographics and ClassMobility), Migration → Colonization (UnplacedDeparture); corridor independence: no constant set from a corridor reading.

### 8.3 SECOND and THIRD findings recorded separately for the Director (not the target)

**Finding 2 — weather dt-dimension (CONFIRMED by derivation; defect-vs-invariant UNRESOLVED pending ruling).** sigmaLogYield 0.2936 is ratified in three places as the SD of LOG yield in ONE YEAR (sim.json:221; SimConfig.cs:353-354 "T3.4b, director ruling"; derived from interannual CV 0.30 at :362-365) and is applied as the stationary SD of every draw at every dt by design (HarvestWeatherSystem.cs:42-45, :139), while one draw multiplies dtYears = 10 years of output (ProductionSystem.cs:242, :309). The 10-year mean of the ratified annual AR(1) (τ = 3) has log-SD 0.2936 × 0.6585 = 0.1933 (discrete), 0.1917 (continuous OU), 0.1937 (Monte Carlo of the exact lognormal mean). Under the shipped envelope P(decade multiplier ≤ 0.57) = 3.9% vs 0.25% (15.6×; ~74 vs ~5 such decades per 12 × 161 run); P(< 0.50) 1.3% vs 0.024% (55×). Libur's 0.4427 is a z = −2.63 event (8.2 per run) shipped, z = −4.12 (1 in 27 runs) under the decadal-mean meaning. The BASE distribution matches the configured model in every moment and tail (log-SD 0.286 vs 0.2936; mean 1.009; lag-1 0.08 vs ρ 0.036; cross-settlement 0.45): no implementation defect. ρ = exp(−10/3) = 0.036 makes correlationTimeYears 3.0 inert at dt = 10, so CR-003 ruling 3's multi-year-drought purpose is unreachable at the canonical turn length whatever σ is. Governance record: flagged once as "context, explicitly not a finding" (t3.4b-review-record.md:385-388, "~52% overstatement of decadal variance"), never ruled; CR-003 §5.2(b) is silent on the decade-vs-year moment; CR-004:171-173's "no units error" is scoped to rates and ρ; food-evidence-dt-experiment.md:37-45 reports deficit incidence as THE one dt-sensitive quantity (the signature this predicts). The mathematics implies a dt-dependent variance factor with σ keeping its one-year meaning (a plain value change to 0.1933 would break the dt = 1 eras); it alters a frozen T3.4b invariant and re-pins every golden → CR/ADR + Director ruling, not TUNE. Deliberately NOT part of this redesign (Step 12: eliminate amplification, not environmental risk; M3/M3b measured as trigger removal; M3b flattens the model). Libur's famine sensitivity given bad weather (50% vs 2% elsewhere) is unexplained and untouched by every lane.

**Finding 3 — Demographics' decade integration of the one-turn deficit scalar** (DemographicsSystem.cs:126-140 fertility × max(0, 1 − 3·deficit) = 0.118; :277 starvation 0.12 × deficit/yr, for the whole 10-year turn). This is what remains of the single-shock loss once flight is bounded (59% in M1 and M4: 49 pp deaths + 39 pp missing births) and what produces M4's residual oscillation episode; no migration-side change can remove it; no lane produced a famine episode of length ≥ 2 (D6 = 0 everywhere), so the "sustained crisis" regime is untested. The accepted "demographic wiring is correct" finding is NOT reopened (the code reads the deficit exactly as documented); what is open is whether a decade of 0.12 × deficit starvation and 88% fertility suppression from a single decade's shortfall is Step 6's "some mortality" or an amplifier in its own right. Offered for ruling, not as a code change.

Queue items (not fixes): the ClassMobility → Migration shared-Buckets same-turn coupling (ClassMobilitySystem.cs:226-255, MigrationSystem.cs:424-431, 464; −7 pp at t118); GapClosingFraction's per-turn denomination (foundations-audit item); the source-side fan-out over-count if not ruled into the ADR.

---

## 9. Implementation Plan

Status GO; NOTHING HAS BEEN IMPLEMENTED AND NOTHING COMMITTED. Nothing below may start before the Director's rulings in §8.2 and before the measurement sequence (throwaway worktrees, Linux, seed 42 then seeds 1 and 2): arm 1a = share normalisation + exact hazard (pre-registered: Libur t118-equivalent in [0.25, 0.32], t85 in [0.12, 0.16], D7 = 0, D2 preserved; kill: t118 ≥ 0.40); arm 1b = arm 1a with ω = max (expect 0.251 / 0.120); arm 2 = arm 1b + stress on the turn INTEGRAL (θ 0.05, τ 10; sensitivity θ 0 / 0.02, τ 20) — only if the Director admits the stress state; arm 3 = H_max — only if a binding rig is pre-registered (otherwise dropped); arm 4 = basin cap alone on BASE (pre-registered: Libur t119 inflow ≤ 0.25 × 1213 = 303, expect ~180; maxOvershoot ≤ 0.30; outflow metrics within ±1 event of BASE; gross reported; kill: inflow ≥ 606); arm 5 = full design against the restored D2–D8 with per-bucket flows dumped. Then STOP for review.

Files / classes / methods (line numbers at 45046eb = 2807155):

| file | class / method | change |
|---|---|---|
| Sim.Core/Systems/Migration/MigrationSystem.cs | class header doc (14-120) | Rewrite DRIVER lines 20-27 with the two-channel form; replace 44-51 ("surge by design, bounded by the overdraw scaler alone") with the bounded-hazard statement and the ADR-024 reference; replace the T2.8(a) collective-inflow paragraph at 93-95 with the basin-aggregate cap statement (structural, pinned at N = 11); 118: "STATELESS except the EMA filter rows" (+ FamineStress rows only if adopted); note the source-side fan-out over-count as ruled or queued. |
| MigrationSystem.cs | `MigrationSystem.Step` — openness/share block after the damping matrix (after 277) | `openness[n]`, `shareSum[n]`: for src, dst ≠ src: v = damping·viability; shareSum += v; openness = max. Local `FlightFraction(cohortIdx, src)` = 1 − exp(−CohortProfile·K·openness·I_src) where I_src is the turn integral of the driver (x·dt with no stress; the closed-form integral with stress); `ReadoutFraction` with openness 1.0. |
| MigrationSystem.cs | `Step` — T4.4 / D-037 B1 readout (333-341) | Gate unchanged (deficit ≤ 0 → continue; anyViableDestination; hasDistances). Value: `UnplacedDeparture = b.Count.Value * ReadoutFraction(b.CohortIdx, src)`. Comment block 295-316 cites ADR-024 for the value and ADR-021:98 for the unchanged binary condition; record the ~5× party-size change. |
| MigrationSystem.cs | `Step` — T2.8(a) pair-cap pass (348-382) + new basin pass | Store gapDesire[src,dst] (already computed 365-372). After the pair pass, for dst ascending: collect basin (src ascending with gapDesire > 0 && gapScale > 0); `if (basinSize < 2) continue;` (bit-identity by branch); basinP, basinR, G; M* = basinR > 0 ? max(0, resources[dst]·basinP/basinR − population[dst]) : 0; cap = GapClosingFraction·M*; if G > cap scale gapScale[src,dst] *= cap/G for the basin (cap == 0 → 0). |
| MigrationSystem.cs | `Step` — desiredTotal (384-406) | `flightAmount[prev.Buckets.Count]`; per bucket if shareSum[src] > 0 && driver > 0: flightAmount = FlightFraction × Count; total += perCount·damping·viability·(gapScale·max(0, S_dst − S_src)) [flight addend at 402 removed] + flightAmount·(damping·viability/shareSum). Keep the existing 425 zero-count/remainder semantics. |
| MigrationSystem.cs | `Step` — transfer loop (408-474) | push = damping·viability·(gapScale·gap); flightWeight = (shareSum > 0 && driver > 0) ? damping·viability/shareSum : 0; skip gate `(push <= 0 && flightWeight <= 0)`; desired = Base·Profile·prevCount·dt·push + flightAmount·flightWeight; overdraw scale, remainder, WholeUnits, FindBucket, Ledger.Transfer, flows unchanged (429-471). |
| MigrationSystem.cs (only if stress adopted) | `MigrationTables` record (6-12); `Step` stress block after the signals loop (after 190, before the n < 2 return at 346) | Add `Table<FamineStressRow> Stress`; per settlement ascending: locate prev row by linear scan in the cloned owned table; x = max(0, deficit − θ); value = x + (σ_prev − x)·exp(−dt/τ); update or Add; also compute the turn integral I for the flight fraction. |
| Sim.Core/Systems/SimConfig.cs | `MigrationConfig` record (434-466); migration validation block (731-758); record doc 408-433 | Add `famineStressTolerance` (θ, in [0, 1)); and only if adopted `famineStressTimeConstantYears` (τ finite > 0) and `famineFlightHazardCapPerYear`; §4.1(c) provenance in XML docs (CHOSEN, frame, null arm; θ declared as a threshold on a turn-averaged intensity). Lines 428-429 → bounded hazard, ADR-024. |
| Sim.Data/content/sim.json | "migration" block (115-145) | Add the new keys with `_doc` entries (loader ignores `_doc`, SimConfig.cs:637-639) and a `_docFamineFlight` entry recording that baseRatePerYear and famineFlightFactor are unchanged and why (t4.12 §2c, CR-003 §5.4). |
| Sim.Core/State/WorldState.cs (only if stress adopted) | `FamineStressRow` (new record struct after 432); `FamineStress` property (near 919); `IReadOnlyWorldState.FamineStress` (near 794); public ctor (near 1029); private ctor (1045-1064); `Clone()` (1114-1126); explicit interface member (near 986) | `public record struct FamineStressRow(SettlementId Settlement, double Value)` — filter state, not a stock, lazily added, absent = 0; add to every enumeration site. |
| Sim.Core/Kernel/CanonicalSchema.cs (only if stress adopted) | `Version` (95); width constants (after 135); Write / Read (near 819 pattern); ExpectedLength (near 973) | 24 → 25; `FamineStressRowWidth = 4 + 8`; table appended after Structures (the v24 tail); ExpectedLength += CountPrefixWidth + Count × 12. |
| Sim.Core/SystemCatalog.cs (only if stress adopted) | `SystemCatalog.Migration` (240-248) | Pass `next.FamineStress`; Colonization (268-274) unchanged (no seeding). |
| Sim.Core/Systems/PathBuild/PathBuildSystem.cs (only if stress adopted) | prev-view IReadOnlyWorldState wrapper (342 pattern) | Expose `FamineStress => prev.FamineStress`. |
| Sim.Core/Observability/SettlementRecord.cs (149 region), Observer.cs (579 region), TelemetryWriter.cs | migration section | Additive fields: exitOpenness, flightOutflow vs gapOutflow, per-destination pairCapSum and basinCap (and famineStress if adopted); schema comment at TelemetryWriter.cs:25 updated; telemetry goldens re-pin by design. |
| Sim.Tests/TestUtil/WorldStates.cs (only if stress adopted) | `TableEquals` (43 region) | Add the FamineStress table. |
| Sim.Tests/Systems/FamineFlightTests.cs (new), BasinCapTests.cs (new) | hand-world rigs via MigrationTests helpers (MigrationTests.cs:35-109) | Tests 1–9 of §7. |
| Sim.Tests/Systems/MigrationTests.cs | Overdraw_ScalesProportionally (789-827); FamineAtOneOfTwelve_ExitCrossesTheFractionBeforeDeathDoes (378-457, Skip); FamineFlight_FiresWithZeroGap (717-747); MagnitudeCorridor_FedPhaseDrift (533) | Re-rig overdraw (H_max ≥ 0.96 with FFF ×4 if H_max exists); lift the T4.1b Skip on flight-only attribution; zero-gap test passes unchanged; re-measure and re-pin the fed-phase corridor. |
| Sim.Tests/Systems/MigrationStabilityTests.cs, CollapseStabilityTests.cs, DemographyRetuneTests.cs | GapCap (226-249), Bifurcation (103-183), Occupancy (185-222), collapse detectors (101-203), SmallN (205-246); Mortality_PositionIndependent_MidTurnMoversStillDie (509-585) | GapCap gains the basin-size-1 bit-identity assertion; the DemographyRetune test is in the blast radius (fails under any H_max < 2.4). |
| Sim.Tests/Kernel/SnapshotTests.cs (only if stress adopted) | new `SchemaV25_PopulatedFamineStress_ExactLengthRoundTripHash` (after 1078) | POPULATED-table test per T1.1/T1.3. |
| goldens, ci.yml FOUNDED_GOLDEN, telemetry goldens, docs/adr/adr-024-*.md, docs/queue.md, docs/m4-spec.md packet text, Sim.Data/content/corridors.json | governance records | Re-pin every golden (flows change from the first pushDeficit > 0 turn, t11; from turn 1 if the schema bumps). ADR-024 "Bounded famine flight and basin-aggregate gap cap" recording every §8.2 item, the CHOSEN constants with frames, the measurement arms with pre-registered bars, the era-boundary continuity pin, the source-side over-count disposition. Queue items per §8.3. The packet spec carries the S8 §4.1 items; the per-event corridor is defined by the Director BEFORE code. |

---

## 10. Final Status

GO — migration + destination interaction redesign required.

---

## Appendix

### A. Artifact paths (all under `/tmp/claude-0/-home-user-civDemo/aba79415-ffd0-5838-8e88-66b12c4a9a0f/scratchpad/`)

- Pre-registered predictions: `cascade/predictions.md` (mtime 09:29:12, before every patch and run).
- This report: `cascade/cascade-report.md`.
- Tool build (45046eb, pristine): `cli-forensic/` (Sim.Cli/bin/Release/net10.0/Sim.Cli.dll); pristine hash log `cascade/runs/pristine-hash.log`.
- Worktrees (all detached at 45046eb, uncommitted diagnostic edits only): `cascade/wt-base`, `wt-m1`, `wt-m2`, `wt-m2b`, `wt-m3`, `wt-m3b`, `wt-m4`, `wt-design` (clean), `wt-audit` (Step 4 cohort dump), `wt-audit-saves/` (t118.bin, hash118.log, step119_reconstruct.py, step119.json).
- Patches: `cascade/patches/{diag-telemetry, m1, m2, m2b, m3, m3b, m4-composed-m1-m2, audit-cohort-dump}.patch`.
- Runs: `cascade/runs/{base, M1, M2, M2b, M3, M3b, M4, audit2, windows}/` — each with metrics.json, table.csv, telemetry-*.jsonl (BASE/M1/M2/M2b/M4 run id 02ee1038911d8d53; M3 1b9eb8ca153f056d; M3b b18461a9b8645f2c), trace-*.csv, hash.log; `cascade/base30.log`, `cascade/base30-session/` (patch-lane 30-turn smoke).
- Scripts: `cascade/cascade_metrics.py` (shared definitions; usage `python3 cascade_metrics.py <telemetry.jsonl> --out metrics.json --csv table.csv [--base ...] [--base-telemetry ...]`), `cascade/base_reference_metrics.py`, `cascade/audit_response.py`, `wxaudit/stats.py`, `wxaudit/derive.py`, `design/check.py`, `challenger/{checks,surge,extreme}.py`.
- Real run: `playtest-run/` (session-*.json, orders-20260917-125101.bin, trace-*.csv, chronicle-*.txt, telemetry-*.jsonl); `libur-extract/settlement_10_Libur.json`.

### B. What the counterfactuals cannot tell

1. SINGLE SEED (CONFIRMED limitation): every number is seed 42, 161 turns, 12 settlements; the deficit ≥ 0.05 bins hold 1–3 settlement-turns per lane and six of seven cascade events are Libur's; F is formally unresolved; the multi-seed behaviour of any redesign is unknown; Libur's famine sensitivity given bad weather (50% vs 2%) is unexplained.
2. LINUX vs WINDOWS: bit-equality on population/food/migration is measured on this seed only; a different seed or longer run could cross an integer boundary.
3. NO ISOLATING ARMS (the decisive gap): M1 bundles four elements measured only jointly (the 0.25 ceiling binds at t118); the S1+S2-only figures (0.288 / 0.140) and the challenger's integral-form figures (0.088 / 0.095) rest on the reconstructed transfer function, not runs; T1 (basin cap) was never run; M4+m2b and M1+M3 were not run; no lane varied dt.
4. DIAGNOSTIC PARAMETER CHOICES: 0.25/bucket/turn, 20-y stress window, 50-y memory window, σ 0.1933/0.1468 are single points with no sensitivity sweep.
5. IN-MEMORY DIAGNOSTIC STATE: m1/m2/m2b EMAs live in the system instance, unserialized and unhashed; replay-vs-live, save/load and the session hash are meaningless for those builds.
6. WEATHER NOT BIT-IDENTICAL under any population-moving change (travel-cost kernel): D3's zero-tolerance identity is unattainable; hence the replacement criterion.
7. SUSTAINED CRISES: every famine episode in every lane has length 1 (D6 = 0); the persistent/extreme regimes are untested and the shipped model has no state variable to attach them to.
8. COHORT-LEVEL FLOWS were never dumped: G's saturation, the elders/adults split and the ClassMobility withholding (−7 pp) are settlement-level reconstructions (±1.2 persons), PROBABLE.
9. THE RESIDUAL EPISODE: whether a ~59% single-shock loss that is 49 pp deaths with a 2-turn refill is "within intended design" is a Director judgement the metrics cannot make.
10. STRANDING: unplacedDeparture was not examined in M1/M4 (the m1 readout is stress-gated and could alter colonization timing on other seeds).
11. Whether corrected-weather runs would still show the pathology at higher-deficit events cannot be known from seed 42 (one M3 famine > 0.01; A's kill missed by 0.028).
12. The world-level crash metric is blind (crashes20 = 0 everywhere); no existing corridor instrument can see this pathology.
13. ADMISSIBILITY IS NOT MEASURABLE: whether M2 is a mechanism or a modifier under law 2 / ADR-012:73-77 is a governance judgement.
14. The design's own numbers: the challenger showed the endpoint-stress hazard mis-shapes the shock/tail split by ~40% (0.145 vs 0.088 at t118); no arm has yet measured the integral form, H_max cannot bind on any available run, and the source-side fan-out over-count has no counterfactual.

### C. Cleanup note

Worktrees under `scratchpad/cascade/wt-*` (and `wt-audit-saves/`) are diagnostic and may be removed with `git -C /home/user/civDemo worktree remove --force <path>`; they hold uncommitted diagnostic edits only. Nothing was committed, nothing pushed, no branch of /home/user/civDemo was modified (main repo clean on claude/civdemo-work-b1z2y4). Per ADR-015 §6 no finding here is actionable before the Director's ruling; every lane STOPPED for Director review.
