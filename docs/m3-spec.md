# M3 — GOODS, PRODUCTION & MARKETS: SPEC & TASK PACKETS
### The economy arrives. Settlements stop being identical food-machines and become places that make different things, trade them, and price them. Written post-M2-exit against code-truth, per S8 §4.

---

## 1. DECISIONS CLOSED THIS DOCUMENT

- **D-030 — No money at M3. Prices are grain-equivalent exchange ratios.** Grain is the numéraire (price ≡ 1 by definition); every other good's price is expressed in grain-equivalents. Rationale: coined money is an *institution*, not a starting condition, and introducing a conserved money stock now would drag monetary architecture (minting, debasement, credit) into the milestone that can least afford it. Historical warrant: Mesopotamian barley/silver accounting worked exactly this way. Money as an institution is deferred to its own milestone; the price tables are already denominated so the later switch is a re-denomination, not a rewrite.
- **D-031 — Goods roster v1 (14 goods, data-driven, `goods.json`).** Food: **grain** (numéraire), **livestock**, **fish**. Raw: **timber**, **stone**, **clay**, **copper ore**, **tin ore**, **fiber**, **hides**. Processed: **bronze**, **tools**, **pottery**, **cloth**. Housing is NOT a good — it is a durable per-settlement stock built from timber/stone/clay (§3). Sits inside the Scale Charter's 12–15 staging.
- **D-032 — Production is price-driven, not player-micromanaged.** The player allocates labor across a small number of **sectors** (farming · herding/fishing · extraction · crafting · construction) per settlement. *Within* a sector, what actually gets produced is decided by the sim from relative prices and local endowment. Rationale: the authority/bandwidth doctrine (Spine S1) forbids a slider per good, and price-driven allocation is both more historical and less UI. The M2 farm/path slider generalizes into this sector allocation.
- **D-033 — The price solver, fixed by mandate (Spine, "market solver mandate").** No global equilibrium solve, ever. Per settlement, per good: `p += λ · p · (excessDemand / marketScale) · dt`, clamped to a TUNE band AND to a maximum per-turn relative change. Excess demand = consumption demand + input demand − production − stock release. Grain's price is pinned at 1. Damped, incremental, explainable, bounded — the same discipline that stabilized migration at T2.8, applied to prices before it can misbehave.
- **D-034 — Trade is arbitrage on the existing network, damped by the T2.8 pattern.** For each connected settlement pair and good, if the price gap exceeds transport cost (per-good bulk × edge cost, from the T1.3/T1.6 network — one object, three jobs), goods flow low→high price, **capped at a TUNE fraction of the gap-closing quantity**. Overshoot is structurally impossible at f < 1. Famine-flight's lesson applies inverted: flows respond to gaps, and a gap that closes stops the flow.
- **D-035 — Needs bindings extend from one to four.** Sustenance ← a food basket (grain/livestock/fish, class-weighted). **Shelter** ← housing stock per capita. **Comfort** ← pottery + cloth. Belonging/Faith, Safety, Health, Dignity, Prospects remain unbound and continue to contribute exactly nothing (the T2.6 zero-effect gate still binds). D-018's class-differentiated baskets become real here: peasants and artisans want measurably different things.
- **D-036 — One new class: Merchants** (emergence predicate on sustained trade volume, per the D-020 DSL). Laborers/Capitalists wait for wage employment and private firms respectively. One class per milestone stays the discipline.
- **D-017 CLOSED — Settlement growth.** A settlement carries a **size** stock grown from population and construction, which drives housing capacity, catchment radius bonus, and (render-side) footprint. Sprawl visuals are symbology — deferred per the queue.
- **Scaffolding demolition (mandated, per M2 §1):** the M2 artisan tool-yield multiplier and artisan construction-labor abstraction are DELETED. Tools become a real good that raises farm yield when stocked and consumed; construction consumes real materials and labor. If the demolition changes trajectories, that is the point — re-pin deliberately.

## 2. SCOPE FENCE

**In:** goods/recipes data model · sector production · local price solver · trade & arbitrage · class consumption baskets · four bound needs incl. housing · Merchants · settlement size/housing · worldgen refresh (queued fixes) · market UI + price graphs · price/trade calibration corridors incl. the comparative-advantage emergence test.
**Out (where it lives):** money, credit, banking (own milestone) · firms as agents and private capital (later; M3 production is settlement-level) · any second polity, conflict, or foreign trade (M4) · unrest *behavior* (M5 — grievance still accrues and acts on nothing) · wage labor markets (with Laborers, later) · vintaged capital · weather/harvest variance (moved to M4 — M3 already carries the milestone's risk budget in the solver) · symbology/sprawl visuals (queued art packet).

## 3. SYSTEM NOTES (kernel-contract shapes)

- **Goods stocks** are conserved `long` quantities per (settlement, good), registered as conservation quantities — the auditor covers all 14 by name. The M2 `FoodStore` migrates into grain. Remainder accumulators per row, T0.5 precedent.
- **Recipes** are data: inputs (goods + labor) → outputs, with a per-recipe era/knowledge gate expressed in the D-020 DSL (bronze requires the alloy recipe to be *available*, not a calendar date — law 4).
- **Housing** is a durable stock: built by the construction sector from timber/stone/clay, decays at a TUNE rate, and supplies the Shelter need per capita. Decay makes maintenance a real ongoing cost rather than a one-time build.
- **Prices** live in a per-(settlement, good) table as `double`; they are *not* conserved and never touch the Ledger. All goods movement — production, consumption, trade — goes through Ledger transfers.
- **Pipeline (M3 preset):** Catchment → Production → Consumption → Market (price update) → Trade → ClassMobility → Migration → Demographics → Needs/Grievance → Construction → Chronicle-detect. Prices update *after* consumption so they respond to realized scarcity, and *before* trade so arbitrage acts on current prices; state the one-turn-lag consequences in the header.
- **Extraction is endowment-bound:** ore, stone, clay, timber availability derive from the terrain fields already generated (T1.1/T1.2) plus a per-settlement deposit roll at founding. This is what makes settlements *different*, and it is the precondition for comparative advantage to emerge at all.

## 4. TASK PACKETS (adversarial-mandatory: T3.4, T3.6, T3.10)

- **T3.1 — Worldgen refresh (first, so goldens move once).** (a) Verify whether the water-access field credits river adjacency; if it doesn't, fix it — fertile interior river valleys are the missing settlement type. (b) Edge taper: guarantee an ocean margin so continents are not truncated at the world boundary. (c) Founding variation: seeded jitter in siting (accept some lower-scored inland/riverine sites) and in endowment (population/food/deposit composition) so settlements diverge from turn 0. *Accept:* twin-identical; river-adjacent inland sites demonstrably occur across 10 seeds; no land at the world edge; founding endowments differ measurably seed-to-seed; the T2.4 lockstep signature (all settlements crossing a class threshold within one decade) provably breaks.
- **T3.2 — Goods & recipes.** Registries, stocks table, conservation registration for 14 goods, `FoodStore`→grain migration, deposit endowment. *Accept:* conservation exact across all goods under the FsCheck suite; populated-table tests; recipe validation errors actionable; goldens re-pinned.
- **T3.3 — Production + scaffolding demolition.** Sector labor allocation (D-032), recipes executing dt-correctly, tools-as-a-good raising yield, M2 scaffolding deleted. *Accept:* output scales with labor and inputs, never from nothing; tool stocks measurably raise yield and deplete; zero references remain to the deleted multiplier (grep-level); dt-halving holds.
- **T3.4 — The price solver. [adversarial-mandatory — the project's #1 flagged risk]** D-033 exactly. *Accept:* 500-turn autoplay soak with no sustained oscillation (sign-flip detector, T2.8 family, with a vacuity companion); prices bounded within clamps always; a scarcity shock raises the right price and relaxes when it clears; grain pinned at 1; **explainability**: a price change decomposes into its contributing terms (the glass-box law made real for the first time). *Adversarial:* reviewer designs a destabilizing config; vacuity check on the oscillation detector; verify no hidden global solve crept in.
- **T3.5 — Consumption baskets + needs.** Class-weighted baskets, Sustenance/Shelter/Comfort bound, unbound needs still exactly inert. *Accept:* peasant and artisan baskets provably differ; housing shortage lowers Shelter satisfaction and raises grievance; the T2.6 zero-effect gate still passes at 10⁹ weight.
- **T3.6 — Trade & arbitrage. [adversarial-mandatory]** D-034. *Accept:* goods flow low→high price and stop when the gap closes below transport cost; conservation exact world-wide; unreachable pairs move nothing; no two-turn oscillation (detector + teeth); transport cost measurably shapes what travels (bulk goods stay local, dense goods travel).
- **T3.7 — Merchants + mobility.** Emergence predicate on trade volume with hysteresis; mobility flows; merchant consumption profile. *Accept:* merchants emerge in a trading world and not in an isolated one (the discriminating test); hysteresis teeth both directions.
- **T3.8 — Settlement size + housing.** Size stock, housing build/decay, catchment bonus. *Accept:* housing decays without maintenance; size responds to population and construction; conservation of materials exact.
- **T3.9 — UI: markets. [Visual Gate]** Per-settlement goods/price panel, sector allocation controls replacing the farm/path slider, price time-series graphs, trade flows listed (map symbology deferred). *Accept:* view-model tests; gate: the director reads a scarcity as a rising price line and rules two settlements into different production mixes.
- **T3.10 — Calibration extension. [adversarial-mandatory]** New corridors: price stability and bounds; trade share of production; production-mix divergence between settlements; **the comparative-advantage emergence test** — settlements with different endowments must specialize *and* the specialization must track relative (not absolute) advantage. Malthus must still bind with the goods economy in place. Two-sided teeth per corridor; degenerate worlds must fail. *Adversarial:* corridor vacuity — can a barter-free or trade-free world pass?
- **T3.11 — Harness + goldens.** All determinism suites on the M3 world; first-reign re-pinned with shape asserts intact; xproc; bench (the price/trade phases are new hot paths — report per-phase).
- **T3.12 — M3 exit artifact. [Exit Gate]** Version strings, README, pre-exit sweep incl. nightly battery, exit build. Director's exit session: rule settlements into different production mixes, cause a shortage, watch prices and trade respond, read the annals.

## 5. M3 EXIT CRITERIA

All packets accepted · calibration battery green across ≥20 seeds with proven teeth, including comparative advantage · price solver soak stable over 500 turns · director exit session played from the CI zip with its log replaying hash-identical · harness green · first-reign shape asserts standing · goldens, milestones.md, `m3-exit` Release with attached zip · merged branch sweep.

## 6. GOVERNANCE

Kernel remains frozen. M3 does not begin until the art-substrate branch is merged and this spec is on main with packets cut. The M2 scaffolding demolition is *planned* removal, not amendment. Weather/harvest variance is deliberately moved to M4 to keep the solver as this milestone's single large risk. The M4 spec (neighbors, conflict, foreign trade) is written only after M3 exit.
