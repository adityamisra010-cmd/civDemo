# T4.1f — landWeight DERIVED AGAINST THE 95.2 km CATCHMENT SCALE: MANIFEST

**Written BEFORE measurement (§7.13).** Cut from `main` `1d42b84`.
**FENCE:** `attractivenessLandWeight` is the only constant that moves. `minSpacingKm` stays 95.2 on
`t4.1b` and is **NOT duplicated here** — measurement uses a config override, never a file edit.
No band edge. No test relaxed.

## THE PRIOR OCCURRENCE, READ FIRST

`sim.json:112` `_docLandWeight` records T3.4b's derivation verbatim. It is the same failure by the
same path: when the catchment became a 50 km hinterland the land term fell ~15×, land stopped
moving attractiveness, and the migration teeth died. T3.4b did **not** rescale to reproduce old
behaviour — *"the old balance was a product of the wrong denomination and has no claim to
correctness"* — it re-derived from what the signal should do.

**T3.4b's METHOD, quoted:** *"The weights are therefore set so the two terms contribute COMPARABLY
to the differential at FED conditions — which makes hunger automatically dominate the moment food
actually varies."* Its measurement: dev world, weather-live, turn 120; food/capita 1526–1544
(1.2 % spread), arable/capita 0.162–4.698 (26×). Land differential
`(4.698 − 0.162) × 0.078125 = 0.354` against a food differential of `~0.35`.

## THE RELATION, FIXED NOW

```
landWeight = foodDifferential(fed) ÷ arablePerCapitaSpread(at the new catchment scale)
  where  foodDifferential   = (max − min) foodPerCapita × attractivenessFoodWeight
         arablePerCapitaSpread = (max − min) arableKm2 per capita
```

**Same conditions as T3.4b: dev preset, weather live, turn 120 — with `minSpacingKm = 95.2`
overridden in config, since that is the scale the constant must serve.**

Then **round to a clean binary fraction**, as T3.4b did (`0.078125 = 5/64` exactly), so the weight
stays exactly representable and the product exact.

**WHY THIS IS NOT A SWEEP:** the value is computed from two measured spreads and a stated equality.
`MagnitudeCorridor_FedPhaseDrift_WithTeeth` and `FamineAtOneOfTwelve` are **the property being
restored** and are NOT consulted in deriving it. Fitting a constant to them would prove nothing
(CR-003 §5.1).

## THE VERIFICATION PROBLEM, STATED HONESTLY

**The two teeth tests cannot fail on THIS branch**: `t4.1f` carries `minSpacingKm = 480`, at which
both already pass. The property is only falsified at 95.2 km, which lives on `t4.1b`. So:
1. **derive** here, from the 95.2-km-override measurement;
2. **verify the teeth** with the new weight AND the 95.2 override together (probe);
3. **verify no regression at the shipped 480** with the full suite on this branch.
A green suite here is therefore necessary and **not sufficient** — stated so the handback is not
read as proof the teeth revived.

## PRE-COMMITTED READINGS (director's, verbatim)

- **both teeth live again** (10× rate moves gross flow > 1.5×; famine ordering demonstrable) →
  report and hand back with `t4.1b` ready to merge alongside;
- **one lives, one does not** → report which, with the measurement. **Do not iterate.**
- **neither** → the coupling is not `landWeight`. **STOP.**
- **a seventh test moves** → **STOP.**
