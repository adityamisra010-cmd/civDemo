# T4.2 — B-2 STORE BOUNDING: FALLOUT ENUMERATED, CLASSIFIED AS A SET. STOPPING.

**Manifest at `172b2ea`, committed BEFORE the constants were applied.** Implementation follows it
exactly: `grainSpoilagePerYear = 0.08`, `granaryYearsOfDemand = 1.5`, neither adjusted.

## THE MECHANISM AS BUILT

Two sinks in `ConsumptionSystem.BoundStore`, applied **after** eating (people eat before the store
spoils or overflows; the reverse order would starve a settlement for an evaluation-order reason):

1. **SPOILAGE** — `ReasonIds.Spoilage (14)`, `Ledger.Flow` sink on the grain stock.
   **Integrated as a survival exponential `1 − exp(−rate·dt)`, not `rate·dt`** — at the canonical
   dt of 10 years the linear form would destroy 80 % of the store in one turn and 100 % at
   dt = 12.5. **The exponential form is dt-invariant.**
2. **GRANARY CAPACITY** — `ReasonIds.GranaryOverflow (15)`, sink on the excess above
   `1.5 × annual grain demand`. Denominated in years of the settlement's OWN demand, so it scales
   with population and adds no per-world constant.

**Two reasons, not one, deliberately:** "why is the store not growing?" has two different answers,
and merging a decay process with a capacity constraint would destroy the audit trail. Conservation
is exact — stocks stay `long`, both losses go through the Ledger.

**Stated, not hidden:** neither loss banks a sub-unit remainder, so a store whose annual spoilage is
under half a unit does not spoil. A remainder field would change the serialized row shape, which is
outside this packet's fence.

## FALLOUT — ENUMERATED BY RUNNING BEFORE REPAIRING

**`Sim.Tests` 435 passed / 15 failed / 6 skipped. `Sim.Ui.Tests` 151 passed / 0 failed.**

## THE CLASSIFICATION, MADE AS A SET BEFORE ANY RE-PIN

**Nothing has been re-pinned. T4.1b stopped three times because this was done one test at a time.**

### VALUE (5) — re-pinnable, cause per line

| test | measured |
| --- | --- |
| `SnapshotTests.FoundedGolden_Seed42Turn300` | `7f536da2…` → `2ac9e0a5…` |
| `DrivenGoldenTests.DrivenGolden_Seed42Turn300` | `f2d896ff…` → `7837c05d…` |
| `FirstReignTests.FirstReign_PostFix_…` | `a631877c…` → `8bf37a07…` (**shape asserts must be RE-RUN, not carried — T4.1e's discriminator**) |
| `PopulationExactnessTests.FarmingAndConsumption_SingleStep_HandComputedExact` | 17,050 → 442. **A HAND-COMPUTED expectation: it must be RE-DERIVED BY HAND against the new mechanism, not pasted from the run** |
| `CalibrationBatteryTests.Dev_MalthusCorridors(42)` quarantine envelope | 0.000659126 → 7.21744E-05 (**×0.109**) |

### PROPERTY (10) — NOT re-pinnable. Each needs a ruling.

| test | the property, and what it now says |
| --- | --- |
| `PopulationTests.Reconciliation_FromLedgerAlone_PersonAndFoodExact` | **`unexpected Food flow reason 14`** — the reconciliation enumerates every legitimate food flow reason. **Extending it to Spoilage/GranaryOverflow is legitimate and NOT a weakening**, but it is a change to what the test accepts and is listed here rather than done silently |
| `EquilibriumInvariantTests.Invariant_AtThePredictedDensity_HarvestEqualsConsumption_Exactly` | expected 1,000,000 → **442** |
| `EquilibriumInvariantTests.Invariant_IsIndependentOf_CatchmentSize_And_Population` ×3 (scale 1, 4, 37) | expected 100,000,000 → **1,770**. **The invariant asserts store behaviour is scale-free; a capacity denominated in years of demand is scale-free BY CONSTRUCTION, so this needs re-derivation rather than re-pinning** |
| `ClassSystemTests.Famine_DrainsArtisansBeforePeasantStarvationPeaks` | **`demote-first violated: drain at turn 3, starvation peaked at turn 3`** — an ORDERING property. Starvation now arrives fast enough to coincide with the class drain |
| `NeedsGrievanceTests.Famine_RaisesGrievance_InTheStarvationWindow` | expected 3 → **28** |
| `CalibrationBatteryTests.Canonical_FedCorridors(1)` and `(2)` | `canonical.migrationGrossPerDecade 0.000204465 / 0.000253514 outside [0.001, 0.01]` — **a BAND breach. The fence forbids moving a band edge** |
| `CalibrationBatteryTests.Dev_MalthusCorridors(7)` | **`76 starvation deaths — the dev world is no longer pre-Malthusian. If a crash cycle is back, CR-003 is resolved: delete this quarantine and restore the five AssertInBand calls.`** |

## B1 — DOES ANYTHING STARVE? **YES, AND THE GUARD THAT SAYS SO IS THE ONE CR-003 LEFT BEHIND**

**The pre-committed reading fired on its first branch.** `Dev_MalthusCorridors(seed 7)` reports
**76 starvation deaths** where twenty full-length M3 runs produced zero. **The test failing IS the
blocker clearing** — its own message states the exit condition, written by T3.4c long before this
packet: *"If a crash cycle is back, CR-003 is resolved."*

**Not yet measured, and owed before any claim about the RATE:** deaths per decade as a fraction of
population, across seeds, and whether this is the "plausible rate" branch or the "mass die-off"
branch. **76 deaths in a dev world is not yet either — it is a nonzero number that refutes "nothing
starves" and nothing more.** The B1 branch cannot be called until the rate is measured.

## WHAT IS NOT DONE — the packet stops here

Not done: the six readings measured, the reserve-depth deliverable, the 1.0×/1.5×/2.0× sensitivity,
the five lenses, any re-pin. **Stopping because ten of fifteen failures are PROPERTIES**, including
a band breach the fence forbids repairing and an ordering property that may be telling us the
mechanism is too abrupt rather than that the test is stale.
