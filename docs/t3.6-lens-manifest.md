# T3.6 — required-lens manifest

Written **before** implementation (§7.3). Reconciled **BY NAME at handback** (§7.11); a name with
no result is BLOCKING; review infrastructure stands until the list is clean. Six lenses — the §7.6
floor is five and the T3.3 precedent names test-power separately.

| # | lens | what it attacks | required |
| --- | --- | --- | --- |
| 1 | `mandate-fidelity` | Is this D-034 and nothing else? Pairwise on the EXISTING network, one computation pass per pair per good, no global solve, no iteration to convergence, no parallel trade-route structure, f < 1 validated at load. Attack the desired-then-apply shape specifically: is the apply sweep a disguised second solve, or a T2.8-family cap with no feedback? If any pass reads what another pass wrote and re-decides, that is the banned shape. | YES |
| 2 | `conservation-and-drawdown` | Law 1 exact, world-wide, FsCheck over generated pair/good/quantity sequences — AND the drawdown, not only the balance (the Crafting timber-stranding precedent: moving nothing conserves perfectly). Wherever a flow is EXPECTED, is the QUANTITY pinned? Attack the integer-remainder allocation for a lost or minted unit. | YES |
| 3 | `stability-and-coupling` | The trade→stocks→prices→trade loop. D-021 paired-feedback: which damper actually BINDS under stress (f, the price rail, the transport-cost deadband)? The oscillation detector must be proven to FIRE on a known-oscillating series and NOT on constant / monotone / one-flip series, BEFORE it watches anything. Attack for a detector that cannot fire and for a stability claim measured only in calm worlds. | YES |
| 4 | `derivation-honesty` | The bulk values. They PRE-EXIST in goods.json (T3.2, declared D-034 bulk, TUNE) — were they REVIEWED against the reference class and corrected BEFORE measurement, shown from commit order and the pre-rebase-timestamps lesson? Attack transport-shapes-what-travels for circularity: does the test derive its expectation from the same constants it measures? And R1/R2/R3: were the readings applied without deliberation, with a discriminating observable per reading (the T3.5b lesson — a composite observable admits many readings)? | YES |
| 5 | `test-power` | Mutation sweep, bounds stated first (§7.1), golden-only kills counted separately (§7.2). Named mutants at minimum: f→1 (cap gone), transport cost→0 (deadband gone), direction reversed (high→low), conservation off by one unit, unreachable-pair guard deleted, ordering shuffled (must be invisible per decision (c), except pinned remainders), detector deleted. Every guard proven RED (§7.4). | YES |
| 6 | `scope-and-fence` | No store bounding in any disguise — especially transit loss as a backdoor granary drain (decision (a) says NO loss; verify none exists). No money/credit, no merchants, no foreign trade, no migration-weight change, no band edge moved, nothing tuned to close a discovered gap. Goldens re-pinned once, itemized; the T3.11 blocking-gap question answered with a measurement (do the goldens NOW exercise the goods economy?). The three queue items dispositioned, not silently absorbed. | YES |

## Mutation bounds (§7.1)
Clean filtered-subset baseline measured immediately before the sweep and recorded; per-mutant 5×
that baseline, hard timeout; a run that never built is not a hang (§7.9). One worktree per agent
(§6).

## Verification discipline
No finding actionable before its verdict returns (§6). §7.10 on every finding (name and QUOTE the
ratified item). §7.12 on every claim written. §7.13: the three readings are already pre-committed
in the spec with a discriminating observable each — reconcile which fired against the spec's text,
not against memory.

## Handback checklist
- [ ] Six lenses reconciled BY NAME (§7.11); a missing lens is BLOCKING
- [ ] Decisions (a) transit loss, (b) instantaneity, (c) ordering — stated with reasoning
- [ ] All five acceptance criteria with their named traps discharged
- [ ] R1/R2/R3 quoted, which fired, applied without deliberation
- [ ] Bulk values reviewed/derived BEFORE measurement; commit order + true timestamps preserved
- [ ] Mutation kill-record, bounds first, golden-only counted separately
- [ ] Per-phase `sim bench` including the new trade phase; the O(S²·G²) residue re-measured
- [ ] Goldens re-pinned once, itemized; T3.11 gap question answered with a measurement
- [ ] Three queue items dispositioned by name (extent-of-market, recipe-chain ordering, solver complexity)
- [ ] Coupling map answered: trade→stocks→prices→satisfaction→grievance; trade→production mix;
      trade→edge usage→PathBuild; transport cost→travel time
