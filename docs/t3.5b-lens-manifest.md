# T3.5b — required-lens manifest

Written **before** implementation (§7.3 standing practice). At handback each row is reconciled
**BY NAME, from this file, against a returned result** (§7.11). A name with no result is BLOCKING.
Review infrastructure stands until this list is clean.

Four lenses, per the directing prompt. Not adversarial-mandatory by m3-spec, but T3.6, T3.7 and
T3.10 are all calibrated against what this packet derives, so the two derivations get real attack.

| # | lens | what it attacks | required |
| --- | --- | --- | --- |
| 1 | `derivation-honesty` | Are items 1 and 2 DERIVED or fitted? Were the numbers fixed before the corridors were looked at, and can that be shown **from commit order** rather than asserted? Attack for a reference class selected because it produced a convenient number, and for any post-hoc adjustment of the mix or the standard after a measurement existed. | YES |
| 2 | `pre-commitment-integrity` | Were items 1a and 1b's readings written down BEFORE measurement, and applied WITHOUT deliberation? Was s\* recomputed from SHIPPED values rather than copied from the prompt's arithmetic? Did anything move to clear the threshold or to manufacture a food-economy response? | YES |
| 3 | `test-power` | Mutation sweep with bounds stated first (§7.1). Every new guard proven RED (§7.4). Specifically: is the exact-saturation branch genuinely LIVE in shipped config, or dead again? Is the anti-tautology bar real — does satisfaction respond to a perturbed standard — or does the variety normalisation reconcile by construction? | YES |
| 4 | `scope-and-regression` | Item 5 is a DECLARED widening; verify nothing ELSE widened. Store bounding absent in every disguise. The T2.13 extinction-grievance fix intact and pinned alongside the new empty-class fix. No band edge moved; no `Sim.Data` constant touched beyond the new derived values and the variety standard. | YES |

## Mutation bounds (§7.1)

- Clean filtered-subset baseline measured immediately before the sweep and recorded.
- Per-mutant bound **5× that baseline**, hard timeout, killed at the bound.
- A run that never built is not a hang — read the log before filing non-termination (§7.9).
- One worktree per verifying agent, never shared (§6).

## Verification discipline

- No finding actionable before its verdict returns (§6).
- §7.10 on every finding: name and QUOTE the ratified item requiring the property claimed broken.
- §7.12 on every claim written: a property asserted about this packet's own artifact is a
  measurement owed. The M10 lesson is one packet old.

## Handback checklist

- [ ] All four lenses reconciled BY NAME against returned results (§7.11)
- [ ] Item 1 mix derived with reference class recorded; commit order shows numbers-before-corridors
- [ ] Item 1a: s\* recomputed from shipped values, computation shown, pre-committed reading applied
- [ ] Item 1b: pre-committed reading applied to the measured food-economy response
- [ ] Item 2: standard derived; grain-only < fish-importing measured; exact-saturation branch
      proven LIVE; anti-tautology perturbation test in
- [ ] Item 3: empty-class accrual zero, extinction zeroing intact, both pinned
- [ ] Item 4: both guards fail loudly at load, each proven RED by guard removal
- [ ] Item 5: §7.14 written; the audit run ONCE, last, with a margins table
- [ ] Goldens re-pinned once, itemized, provenance chains preserved
- [ ] Coupling checklist answered: sector mix → production mix → goods stocks → prices → basket
      satisfaction → grievance; food-per-capita → migration attractiveness; equilibrium-density
      invariant; Malthus corridors
