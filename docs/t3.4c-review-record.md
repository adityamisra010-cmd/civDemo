# T3.4c — six-lens review record

**Pin: `0c49c0a6f0e7eec4ed6937c660ab8f88bbe995a1`.** Docs-only commits landed after the pin
(§7.10 filing + revert, `.gitignore`, handoff doc, governance correction); none touches code,
data, or any measured document. Baseline and bounds stated before the sweep: subset 46 tests,
stated bound 290 s, hard timeout 360 s; the test-power lens re-measured its own baseline in-tree
(80 s subset / 329 s full suite, bound 900 s declared before the full-suite pass). One worktree
per agent throughout (§6); every worktree verified clean at exit.

## Reconciliation BY NAME against `docs/t3.4c-lens-manifest.md` (§7.11)

| # | lens | result |
| --- | --- | --- |
| 1 | `variance-correctness` | REPORTED — fix confirmed correct; 1 finding |
| 2 | `re-measurement-honesty` | REPORTED — 7 findings; 3 sent to adversarial verification |
| 3 | `test-power` | REPORTED — kill record complete; 2 BLOCKING + 5 findings |
| 4 | `corridor-and-band` | REPORTED — nothing fitted; 1 HIGH + 3 findings |
| 5 | `quarantine-and-guards` | REPORTED — teeth verified; 6 findings |
| 6 | `scope-discipline` | REPORTED — no store bounding in any disguise; 3 findings |

All six point at returned results. Three verifier agents additionally returned verdicts on lens 2's
sharpest findings. **No finding has been applied** (§6 — nothing is actionable before the
director's certification, and several are unverified).

## What the review CONFIRMS about the packet

- **The variance fix is mathematically correct** (lens 1, independent derivation before reading the
  packet's algebra): `varInn` equals the independent derivation term for term; `varInn ∈ [1,2]` by
  construction, no blow-up reachable at any k or geometry; k=0 and k=1 remain exact. The pre-fix
  k-signature (exact at endpoints, maximal at k≈0.6) is gone — the post-fix curve is flat within
  noise. `E[mult]` converges to 0.9995 ± 0.0007 at 2.0M samples; `sdLog/σ ∈ [0.991, 1.004]` at every
  geometry; CV 0.297–0.302, centre of the reference band. Canonical n=12: E[mult] 1.011 ± 0.006,
  ratio 0.996.
- **RNG consumption is bit-identical** (lens 1): `RngStreams` byte-for-byte identical with only the
  normalisation removed, while world hashes differ — the comparison is non-vacuous and the packet's
  claim holds. Not a different packet.
- **Nothing was fitted anywhere** (lenses 4, 6, 2): `gapClosingFraction` 0.25 at every commit in
  history; every band byte-identical; `sim.json` not in the packet diff; the T3.4b epistemic caveat
  verbatim; zero assertions removed or weakened; goldens re-pinned in exactly one commit.
- **Both discriminator arms reproduce digit-for-digit** (lenses 4, 5): canonical 5/5 in band
  (+15.0% to +63.6%); dev 19/20 below floor, worst −64.1% (seed 6), best +7.5% (seed 11). Row 2 of
  the pre-committed table was correctly applied. The falsified prediction is real: the lens reverted
  the fix and reproduced the −13.5%→−30.5% move on 20 seeds, every seed falling.
- **The three named quarantine teeth all fire with the seed named** (lens 5, live mutations):
  band-immovability, drift-down, resolution-up. Corridor-scoped, literal envelope, dead constant
  removed.
- **No store bounding in any disguise** (lens 6): no spoilage/cap/clamp/new flow; no numeric
  constant moved anywhere in `Sim.Data`; era pacing and migration weights untouched under their
  rulings. Store depth measured directly: reserves −4.55% (the arithmetic shadow of removing the
  ~3.2% yield lift), still ~3,080 years, zero starvation in 20 runs — B-2 exactly as blocking as
  before.
- **Mutation results: semantic kills 0 → 9, golden-only 9 → 5, M15 killed semantically, all six new
  tests load-bearing** (each is a sole killer of at least one mutant; eight of nine semantic kills
  come from the new `HarvestWeatherTests`).

## Findings, most severe first (NONE applied)

### BLOCKING

**B1 — M10 SURVIVES ALL 355 TESTS; the acceptance criterion "M10 must be killed semantically" is
NOT met, and the packet's claim that it was is false** (lens 3; §7.12). `Weather_ScalesOutput_
NeverTheLandCap` asserts only that weather changes output; M10 (weather on the land side) satisfies
it trivially because **no tested world is ever land-capped** — the mutant world is bit-exact
identical to clean, `min(land·w, labor)·w == min(land, labor)·w` everywhere tested. The rig the
docstring describes was never built. CR-003 §5.2(b) is the ratified item the mutant violates, and
it remains untestable.

**B2 — M9 (weather before the Leontief min) also survives all 355, same root cause** (lens 3):
with production labor-capped everywhere tested, the placement is a numerical no-op in both
directions. Ordering relative to the min is unpinned.

### HIGH

**H1 — the refuted §7.7 worked example survives uncorrected in TWO ratified records** (lens 4;
explicit manifest item "Check §7.7's worked example was corrected"). `corridors.json:72` still says
the corridor "is set by LAND HETEROGENEITY and the cap", and ADR-015 §7.7 itself still carries the
same claim. The lens's factorial at corrected variance: weather span 3.70× across σ 0→0.5; land's
entire main effect 2.0%; deleting the land term moves the corridor −0.8%. The fix made land
*deader* (~150× on main effects, vs T3.4b's ~30×).

**H2 — the artisan-valve figures in `docs/t3.4b-quarantine-verdicts.md` do not reproduce at HEAD
and the headline inverts** (lens 5; §7.12 recurring). Doc: min surplus 1.114, 90 sub-2.0 turns,
recede missed by 1.2%, "one bad draw from un-quarantining itself". Measured at HEAD: 1.456
(settlement 0) / 0.536 (all-settlement min), 42 turns, missed by 32% or crossed depending on
reading. T3.4b-pin numbers published present-tense in a post-fix commit — the packet corrected one
unmeasured claim by writing another. Corollary: the three item-6 claims lens 5 could NOT re-measure
(resurrection σ→0 control, migration-teeth table, `anyFamine`) must be treated as **unverified,
not cleared**.

**H3 — the quarantine's drift tooth has a large silent bypass** (lens 5): the envelope permits
−62%/−68% for the two call-site seeds; a 300× reduction of `baseRatePerYear` plus famine-flight
disablement still lands inside the silent window `[0.00030, 0.001)`. The guard cannot see a
near-total disablement of the mechanism it watches.

### MEDIUM

**M1 — §6's peak row mixes denominators** (verifier verdict: lens 2's F1 number REFUTED, its
suspicion CONFIRMED). 5.66% IS reproducible — it is the pre-step-population denominator; the
"before" column's 6.08% is post-step. Like-for-like the corrected peak is 6.0811% vs 6.08% —
unchanged to 3 s.f. — so "peak falls 7.75% → 5.66%, consistent with removing an inflation" is
unsupported for the σ=0.18 arm, and 7.75% has no antecedent anywhere in the tree.

**M2 — §7's before column is an undisclosed re-derivation** (verifier: lens 2's F2 CONFIRMED and
strengthened): 0.952% is 39×1/2¹² back-computed; 32.5%/1.68× is a **12-settlement** statistic in a
row labelled "≥8 of 9"; 0.249 has no antecedent (the record's statistic is 0.45–0.51 at minimum
spacing). The verifier's own 12-settlement measurement shows the displayed deltas are **geometry
effects, not fix effects**. §7's after column reproduces only on an arbitrary 90-sample window
found by exhaustive scan (±2.4 pp SE quoted to 3 figures); lens 2's "no configuration reproduces
all three" was refuted, so the finding is CUT DOWN: the defect is missing rig specification and
undisclosed provenance, not fabrication.

**M3 — the canonical before/after is missing from the record and changes the story's shape**
(lens 4): canonical fell 19.9–23.5% too — the prediction was falsified on BOTH worlds; the packet's
prose reads as if canonical behaved differently. Strengthens §7.13; belongs in the record.

**M4 — the `sumSq == 0` fallback branch is unreachable dead code and three artifacts claim it is
exercised** (lens 1 and lens 3 independently; NaN-poisoning proof — bit-identical worlds). With
`w_ii = 1.0` unconditional, `S_i ≥ 1` always. The manifest's own checklist item "proven … and the
fallback branch" is undischargeable. M17 (fallback→1.0) survives trivially.

**M5 — M6 golden-only: the √(1−ρ²) factor's entire stated purpose (dt-invariant stationary
variance) is unpinned** (lens 3): at shipped dt=10 the factor is 0.99935 — inert; no test measures
variance at two dt.

**M6 — the spatial-correlation test does not test its own name** (lens 3): it asserts one global
mean in (0.05, 0.95) and never compares near vs far; M5 (k dead) and M8 (kernel dead) both pass it —
two shipped tuning parameters have no semantic test.

**M7 — risk direction inverted from ceiling to floor** (lens 4): headroom to ceiling improved ~22%,
but at σ=0 the shipped canonical world reads 33% BELOW its own floor — the corridor now lives near
the bottom of a band whose stated derivation is about land. T3.4b's "84% of ceiling" is
unreproducible in this tree (~4–5× above anything canonical produces) — reported as unreproducible,
not refuted.

### LOW / cut down by verification

- **Ceiling assert dead-as-failing** (lens 2 F6 + lens 5 F1, verifier: CUT DOWN to LOW): the three
  *required* teeth exist and are proven red; the ceiling is a fourth, unrequested assert whose
  failure set is empty (`value > 0.00115 && value < 0.001`), surviving its own §7.4 test. The
  docstring's "READ — unlike the T3.4b constant" implication is false for the ceiling. Deletion is
  the only coherent fix; reordering just moves the dead tooth.
- **`sim.json` `_docLandWeight` still asserts the design point met** (lens 6 F1 confirmed-narrowed +
  lens 2 F4): a tuning doc-string stating the opposite of the packet's own §5 measurement;
  annotation-only fix, no constant moves. The spec's explicitly-directed "mean EXACTLY 1" `_doc`
  correction was also not made.
- **Two false source comments the verdicts doc adjudicates are untouched in the .cs files**
  (lens 5 F4): `ClassSystemTests` "surplus stays above 2.0", `MigrationTests` "harvest variance
  opened real attractiveness gaps" — no cross-reference to the filing.
- **σ-sensitivity table in the evidence doc never re-taken** (F1 verifier, better-aimed residual);
  `t3.4b-migration-evidence.md` itself is REFUTED as a violation — it is a verbatim director-session
  record, correctly immutable.
- **Median convention** (lens 5 F5): −30.5% is the 11th-of-20 order statistic; the midpoint is
  −31.6%. Same convention as the baseline, so internally consistent.
- **Emergence anchor moved again** (lens 5 F6): 84 at HEAD vs 76 recorded at the T3.4b pin; still
  inside [30, 95].
- **Realization-pinned tests absorb mutation signal** (lens 3 F7): three tests fail under a
  distribution-preserving cos→sin swap — teeth, wrong aim (§7.2); they count as golden-class.
- **n=12 is unmeasurable on the dev preset** (lens 1): siting throws for all 40 seeds at 256 px;
  the "n = 1, 4, 12" acceptance row is satisfiable only on canonical (measured there: ratio 0.996).
  Another B-1b data point.
- **M7 (delete √Σw² norm) is pinned only at n=9** (lens 3 F6): single-geometry suites would miss it.
- Stale intra-doc scaffolding in the re-measurement §4 (lens 6 F3).

## Where this leaves the acceptance criteria

Met: fix in with proven-red tests; E[mult]/sdLog/CV at every measurable geometry; goldens re-pinned
once; quarantine re-scoped with three proven teeth; corridor measured on both worlds and read
against the pre-committed table; store bounding untouched; StateEquals + populated-table coverage;
M15 killed semantically; golden-only count fell materially (9 → 5).

**Not met: "M10 … must be killed semantically" — M10 survives, and the packet claimed otherwise.**
Also engaged: "no inflated-world measurement survives anywhere in the tree" (H2, M2, and the
annotation residue), and the manifest's fallback-branch checklist item is undischargeable as
written (M4).

Verification hygiene note: of lens 2's three sharpest findings, adversarial verification refuted
one number outright, cut one down, and confirmed one narrowed — §7.10's ratio holding for the
third packet in a row.
