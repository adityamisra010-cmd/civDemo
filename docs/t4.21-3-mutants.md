# T4.21-3 — mutant kill-record (measured)

**Status:** measured record, T4.21-3 continuation lane, 2026-09-17. Spec §4 row T4.21-3 and §6.4 name
three mutants for this packet — **M-DEM-ADAPT**, **M-DEM-CAP**, **M-DEM-BANKCAPPED** — and CLAUDE.md
requires a kill-record with at least one SEMANTIC test per mutant (ADR-015 §6: a claim in the tree
must have been measured by the agent who wrote it; §7.1: every run bounded, a hang is a finding;
§7.2: teeth AND aim). The step-2 commit (`630bfbe`) stated which tests were DESIGNED to kill each
mutant; this file is the measurement, written by the agent who ran it. The first lane's runs were
cut off by a container restart before any summary (its partial logs are not used).

## 1. Set-up

| | |
| --- | --- |
| tree | `t4.21-3-demographics` @ `038bcc6` (the packet's final commit before this record), one private detached worktree (`scratchpad/t421/p3-mut`), never shared — the packet worktree `p3` was being mutated concurrently by the first lane's orphaned runner scripts, so nothing measured there is used |
| build / run | `dotnet build Sim.Tests -c Release`; `dotnet test Sim.Tests -c Release --no-build` — the FULL suite (847 tests), no filter, per mutant; the mutant reverted with `git checkout -- Sim.Core/Systems/Demographics/DemographicsSystem.cs` after each run (`0 dirty paths` logged after every revert) |
| bound | 3 × the CLEAN full-suite baseline measured first in the same worktree under the same load: 1471 s ⇒ **4413 s**, enforced with `timeout`; a hit would be recorded as non-termination |
| load | the T4.21-2 lane's Release suite and the first lane's three orphaned full-suite runs overlapped this run on the 4-CPU machine (load average 21–33 throughout); the clean baseline therefore took 1471 s against the 506 s the packet's step 4 measured on a quieter machine, and each mutant took 686–1306 s — all inside the bound with margin ≥ 3.4× |
| clean baseline | 840 passed / **1 failed** / 6 skipped: the one red is `CalibrationBatteryTests.Dev_MalthusCorridors_AllInBand(seed: 7)`, red on this tree for the PRE-EXISTING migration-quarantine cause the step-4 correction (`038bcc6`) records (T4.21-4's re-pin, spec §4 N7); the seed-42 twin of the integration branch's CR-003 pair is GREEN on this tree. A mutant's failing set below is counted BEYOND this one red |
| mutants | exactly the spec's three; the diffs (applied by a script that asserts a unique anchor, so each is the one-site change it claims) are in §3 |

## 2. Results

| mutant | verdict | failed / passed (incl. the 1 pre-existing red) | elapsed | failing tests (parameterised cases collapsed; the pre-existing red excluded) |
|---|---|---|---|---|
| CLEAN (baseline) | — | 1 / 840 | 1471 s | Dev_MalthusCorridors_AllInBand(seed: 7) — pre-existing; bound := 3 × 1471 = 4413 s |
| M-DEM-ADAPT | KILLED | 16 / 825 | 1306 s | D_PostFamine_RecoversTowardLimit, D_Starvation_ReadsEffectiveDeficit, Demographics_SingleStep_EveryFlowAndCohort_ReplicaExact, Dev_MalthusCorridors_AllInBand, DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne, DrivenGolden_Seed42Turn300_MatchesPinnedConstant, DtInvariance_StarvationFlow_ExactAcrossDts, Famine_RaisesGrievance_InTheStarvationWindow, FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone, FoundedGolden_Seed42Turn300_MatchesPinnedConstant, Founded_Seed42_300Turns_EveryQuantityReconcilesEveryTurn, Stress_DoesNotSpike_NoBirthDeficit_NothingBanked, SurvivalFractions_NeverOverdraw_RemaindersNeverBankShortfalls |
| M-DEM-CAP | KILLED | 23 / 818 | 867 s | ArtisanPresence_CanPrecedeLocalLatch_ViaMigration_IsNotEmergence, Canonical_FedCorridors_AllInBand, ChainFill_IsTheSystemsFill_OnTheStarvedWorld, D_Asymptote_DefaultMix_LivestockSurplusIsNotFood, D_Asymptote_NoOvershootUnderConstantConditions, D_Cap_DtComposes_LandBound, D_Cap_NoGrowthOnAGranary, D_PostFamine_RecoversTowardLimit, D_Rebound_StrictRelease_NeverInvents, DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne, DrivenGolden_Seed42Turn300_MatchesPinnedConstant, Driven_Seed42_300Turns_EveryQuantityReconcilesEveryTurn, FirstReignTurn40_MovedForTheDisasterLayoutAlone, FirstReignTurn40_MovedForTheM4SchemaAlone, FirstReign_PostFix_HarvestDiesWithThePeople_NoFoodMountain, FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone, FoundedGolden_Seed42Turn300_MatchesPinnedConstant, Founded_Seed42_300Turns_EveryQuantityReconcilesEveryTurn, Order_SetsAllocationRowOnItsTurn_YieldShiftsDtCorrectlyNextTurn_BankAccrues, Starved_PrimaryIsSustenance_AndChainShowsHarvestBelowEaten |
| M-DEM-BANKCAPPED | KILLED | 16 / 825 | 686 s | ArtisanPresence_CanPrecedeLocalLatch_ViaMigration_IsNotEmergence, D_Cap_NoGrowthOnAGranary, D_PostFamine_RecoversTowardLimit, D_Rebound_StrictRelease_NeverInvents, DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne, DrivenGolden_Seed42Turn300_MatchesPinnedConstant, Driven_Seed42_300Turns_EveryQuantityReconcilesEveryTurn, FirstReignTurn40_MovedForTheDisasterLayoutAlone, FirstReignTurn40_MovedForTheM4SchemaAlone, FirstReign_PostFix_HarvestDiesWithThePeople_NoFoodMountain, FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone, FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone, FoundedGolden_Seed42Turn300_MatchesPinnedConstant, Stress_DoesNotSpike_NoBirthDeficit_NothingBanked |

Every run terminated inside its bound (the slowest, M-DEM-ADAPT at 1306 s, used 30 % of it); no
non-termination was observed. All three mutants are killed by at least one semantic test — 8, 8
and 5 semantic kills respectively (the goldens, the attribution ladder and the measured canonical
pins also fail under every mutant, which is teeth, not aim, and is listed but not counted).

Two measured facts worth stating beyond the table:

- **M-DEM-ADAPT** re-creates the CR-003 tooth on BOTH dev seeds (`Dev_MalthusCorridors(seed: 42)`
  fails with "3 starvation deaths", the integration branch's exact message) and puts 19 starvation
  turns back into the founded 300-turn world (`Founded_Seed42_300Turns` asserts exactly zero): the
  weather-driven STRESS starvation that CR-015 calls not-a-famine is precisely what the nominal
  deficit reintroduces. The SEVERE arm (`Famine_MortalitySpike_*`) and the abandonment twin do NOT
  fail under it — at d = 0.456 and in FAMINE the two arguments are close or equal — which is why
  the re-anchored STRESS phase (G2) is the semantic tooth there, as ruled.
- **M-DEM-BANKCAPPED** is killed by `Stress_DoesNotSpike_NoBirthDeficit_NothingBanked` (reservoir
  12.146 instead of exactly 0.0) as well as by the two cap rigs: the T2.7 stress rig has at least
  one binding step (`m < 1` — the non-zero reservoir under this mutant is the measurement of that;
  which turn binds was not traced here), and there the capped-pair bank invents a reservoir where
  nothing was suppressed — the exact property
  ADR-026 §2.2(ii) states ("headroom-withheld births are never banked"). Under `m = 1` the mutant
  is `frac × (unsuppressed − ((base + release) − release))`, which differs from the kernel's
  `frac × (unsuppressed − base)` only by floating-point association — enough to move every golden,
  which is teeth; the reservoir assertions are the aim.

## 3. Per-mutant record: diff, failing tests, the property each asserts

### M-DEM-ADAPT

**Mutation.** `starveRate[c] = StarvationRate(d, c, deficit)` — starvation mortality reads the NOMINAL deficit; the effective-deficit adaptation (CR-015, ADR-026 §2.2(i)) is bypassed for the mortality channel (fertility still reads `dEff`) — spec §6.4 M-DEM-ADAPT

```diff
diff --git a/Sim.Core/Systems/Demographics/DemographicsSystem.cs b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
index bafb019..7ed9ff0 100644
--- a/Sim.Core/Systems/Demographics/DemographicsSystem.cs
+++ b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
@@ -249,7 +249,7 @@ public sealed class DemographicsSystem : ISimSystem<DemographicsTables>
             double suppression = Math.Max(0.0, 1.0 - d.FamineFertilitySuppressionSlope * dEff);
             for (int c = 0; c < Cohorts.Count; c++)
             {
-                starveRate[c] = StarvationRate(d, c, dEff);
+                starveRate[c] = StarvationRate(d, c, deficit); // MUTANT M-DEM-ADAPT: nominal deficit
                 totalRate[c] = d.MortalityPerYear[c] + starveRate[c];
             }
```

**Result: KILLED — 15 failing test(s) beyond the clean tree's one pre-existing red, 1306 s against the 4413 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `DrivenGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical driven world hash at turn 300 — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical founded world hash at turn 300 — reproducibility, not semantics
- `Founded_Seed42_300Turns_EveryQuantityReconcilesEveryTurn` — [SEMANTIC (measured-turn pins)] founded seed 42, 300 turns: every quantity reconciles every turn AND the measured facts hold — starvation exactly zero on every turn (the weather-driven STRESS starvation of the pre-T4.21 tree is gone), first trade 21, 12 settlements
- `Dev_MalthusCorridors_AllInBand` — [TEETH (CR-003 quarantine)] dev-world pre-Malthusian tooth (starvation exactly 0) and the recorded-trajectory envelope; seed 7 is red on the clean tree for the pre-existing migration-quarantine cause
- `D_PostFamine_RecoversTowardLimit` — [SEMANTIC] abandonment famine (harvest 0, FAMINE read, the whole deficit starves) then restore: population rises monotonically toward N_lim after the last d > 0 turn and never overshoots the §28 bound — the banked rebound releases only into headroom
- `D_Starvation_ReadsEffectiveDeficit` — [SEMANTIC] STRESS d = 0.15 ≤ a starves NOBODY (hazard identically 0); SEVERE d = 0.4 starves == replica at dEff 0.25 and != replica at 0.4; FAMINE/Abandonment at the same d = 0.4 starves the WHOLE deficit == replica at 0.4 — adaptation absorbs an ordinary shortfall, a famine gets none
- `DtInvariance_StarvationFlow_ExactAcrossDts` — [SEMANTIC] d = 0.5 SEVERE (dEff 0.375): cumulative starvation over 20 years agrees at dt 10 / 5 / 2.5 within ±1 and matches N·e^(−s·20) — the effective deficit is a per-turn scalar that composes through e^(−s·h) (law 3)
- `Stress_DoesNotSpike_NoBirthDeficit_NothingBanked` — [SEMANTIC] G2 re-anchored phase 1: a famine-window harvest at ≈ 78 % of demand (worst d 0.171 ≤ a, every famine-read turn STRESS) starves EXACTLY nobody, per-capita mortality within 1.1× the fed baseline, births ≥ 0.9× baseline, reservoir exactly 0.0 — a survivable shortfall is not a famine
- `Famine_RaisesGrievance_InTheStarvationWindow` — [SEMANTIC] on the founded world a granary-truncation shortfall is STRESS and starves nobody (control starvation exactly 0); the famine arm starves (152) and raises grievance above the control
- `Demographics_SingleStep_EveryFlowAndCohort_ReplicaExact` — [SEMANTIC] one step at dt 2.5, seeded d = 0.25 (SEVERE, no cause row ⇒ dEff 0.0625): every ledger flow total, every cohort count, the reservoir and the remainders match the ADR-011 replica bit-exactly through the integer reconciliation
- `SurvivalFractions_NeverOverdraw_RemaindersNeverBankShortfalls` — [SEMANTIC] phase 2 at d = 0.25 (SEVERE ⇒ dEff 0.0625) and the d = 1 extreme (SEVERE ⇒ dEff exactly 1.0): the starved amount equals the replica within flooring, an exponential survival fraction never overdraws, remainders stay sub-unit, no phantom debt executes after the famine

Semantic kills: 8 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DEM-CAP

**Mutation.** `m = 1.0` — the headroom growth cap never binds (`bornMax` computed and discarded); births are the uncapped candidate whatever `H_rem` — spec §6.4 M-DEM-CAP

```diff
diff --git a/Sim.Core/Systems/Demographics/DemographicsSystem.cs b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
index bafb019..37f73b4 100644
--- a/Sim.Core/Systems/Demographics/DemographicsSystem.cs
+++ b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
@@ -349,7 +349,7 @@ public sealed class DemographicsSystem : ISimSystem<DemographicsTables>
                     double allowed = (1.0 - Math.Exp(-k * h)) * hRem;
                     double newbornNet = W(totalRate[0] * h) * Math.Exp(-totalRate[0] * h) * _cohortWeights[0];
                     double bornMax = (allowed + deathsPre - agingDrift) / newbornNet;
-                    m = sumCand > 0.0 ? Math.Min(1.0, Math.Max(0.0, bornMax) / sumCand) : 1.0;
+                    m = 1.0; // MUTANT M-DEM-CAP: the cap never binds
                 }
 
                 // PASS B: commit per group — bank, (scaled) release, births,
```

**Result: KILLED — 22 failing test(s) beyond the clean tree's one pre-existing red, 867 s against the 4413 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `DrivenGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical driven world hash at turn 300 — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FirstReignTurn40_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FirstReignTurn40_MovedForTheM4SchemaAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical founded world hash at turn 300 — reproducibility, not semantics
- `Starved_PrimaryIsSustenance_AndChainShowsHarvestBelowEaten` — [TEETH (measured pin)] deficit 0.7736879601044883 at turn 3 of the founded world (the turn-2 hold shrinks the demanding population)
- `ChainFill_IsTheSystemsFill_OnTheStarvedWorld` — [TEETH (measured pin)] grain demanded 3790 at the same founded turn — the turn-2 hold
- `Driven_Seed42_300Turns_EveryQuantityReconcilesEveryTurn` — [SEMANTIC (measured-turn pins)] driven seed 42, 300 turns: every quantity reconciles every turn AND the measured firsts hold — first starvation turn 8, first decay 82, trade 7, pottery 5
- `Founded_Seed42_300Turns_EveryQuantityReconcilesEveryTurn` — [SEMANTIC (measured-turn pins)] founded seed 42, 300 turns: every quantity reconciles every turn AND the measured facts hold — starvation exactly zero on every turn (the weather-driven STRESS starvation of the pre-T4.21 tree is gone), first trade 21, 12 settlements
- `Canonical_FedCorridors_AllInBand` — [TEETH (corridor pin)] canonical fed corridors on seeds 1–3 in band — a behavioural pin, not a cap semantics test
- `ArtisanPresence_CanPrecedeLocalLatch_ViaMigration_IsNotEmergence` — [TEETH (measured pin)] the artisan latch turn 43 on the founded world — moved at step 2 by the turn-2 headroom hold; the mutant restores the pre-cap 39
- `D_Asymptote_DefaultMix_LivestockSurplusIsNotFood` — [SEMANTIC] Default mix on a rich livestock deposit converges to the GRAIN-feedable limit (substitution's fixed point), never toward Σ food — headroom counts only what substitution can turn into nutrition
- `D_Asymptote_NoOvershootUnderConstantConditions` — [SEMANTIC] CR-015 §28: weather off, constant land-bound yield, 200 turns — N_nutr converges toward N_lim without drawdown, never overshoots INTO deficit (d ≤ the flooring band), N_nutr ≤ N_lim + Σ max(0, A_step − D_pre); the all-weights-1.0 twin has drift exactly 0
- `D_Cap_DtComposes_LandBound` — [SEMANTIC] binding regime on a land-bound rig: one dt-10 turn == two dt-5 turns within flooring (±2 per row, ±3 ae) — H_rem(end) = H_0·e^(−k·dt) composes across step cuts and turn boundaries
- `D_Cap_NoGrowthOnAGranary` — [SEMANTIC] a stockpile is not an influx: abandoned rig, d = 0, S = 0 ⇒ N_lim = 0 ⇒ H_0 = 0 ⇒ births replace deaths in adult-equivalents per step (replica-exact births and reservoir every turn) while the uncapped twin grows
- `D_PostFamine_RecoversTowardLimit` — [SEMANTIC] abandonment famine (harvest 0, FAMINE read, the whole deficit starves) then restore: population rises monotonically toward N_lim after the last d > 0 turn and never overshoots the §28 bound — the banked rebound releases only into headroom
- `D_Rebound_StrictRelease_NeverInvents` — [SEMANTIC] under a sustained ceiling (N_lim = 0, d = 0) the reservoir may only FALL, by exactly the realised (m-scaled) release; extra births over the rebound-off twin ≤ the bank — deferred, not invented; the §28 bound holds every step
- `FirstReign_PostFix_HarvestDiesWithThePeople_NoFoodMountain` — [TEETH (golden + shape)] FirstReign turn-40 hash plus its shape asserts
- `Order_SetsAllocationRowOnItsTurn_YieldShiftsDtCorrectlyNextTurn_BankAccrues` — [TEETH (measured pin)] the housing draw at turn-3 state (484 people; the turn-2 hold) — the mutant restores the pre-cap 500

Semantic kills: 8 (teeth-only kills excluded). At least one semantic test per mutant: YES.


### M-DEM-BANKCAPPED

**Mutation.** the bank is taken from the CAPPED pair — `reservoir += frac × (unsuppressed − (born·m − release·m))` — so births withheld by headroom are banked as if they were famine-suppressed conceptions (the R1 form ADR-026 refuses) — spec §6.4 M-DEM-BANKCAPPED

```diff
diff --git a/Sim.Core/Systems/Demographics/DemographicsSystem.cs b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
index bafb019..6d4fa41 100644
--- a/Sim.Core/Systems/Demographics/DemographicsSystem.cs
+++ b/Sim.Core/Systems/Demographics/DemographicsSystem.cs
@@ -361,17 +361,14 @@ public sealed class DemographicsSystem : ISimSystem<DemographicsTables>
 
                     double born = candA[i];
                     ref BucketRow reservoirRow = ref buckets.Ref(i);
-                    reservoirRow.ReboundReservoir += bankA[i];
+                    double release = releaseA[i];
+                    if (m < 1.0) { born *= m; release *= m; }
+                    // MUTANT M-DEM-BANKCAPPED: the bank taken from the CAPPED pair
+                    reservoirRow.ReboundReservoir += d.ReboundRecoverableFraction * (unsuppressedA[i] - (born - release));
                     if (deficit == 0.0 && unsuppressedA[i] > 0.0)
                     {
-                        double release = releaseA[i];
-                        if (m < 1.0) { born *= m; release *= m; }
                         reservoirRow.ReboundReservoir -= release;
                     }
-                    else if (m < 1.0)
-                    {
-                        born *= m;
-                    }
 
                     // Live births recorded; in-step infant deaths at the
                     // newborn cohort's rate; survivors join cohort 0.
```

**Result: KILLED — 15 failing test(s) beyond the clean tree's one pre-existing red, 686 s against the 4413 s bound.** Property each failing test asserts (aim, ADR-015 §7.2):

- `DrivenGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical driven world hash at turn 300 — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `DrivenGoldenSeed42Turn300_SeparatesTheSchemaMoveFromTheBehaviouralOne` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FirstReignTurn40_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FirstReignTurn40_MovedForTheM4SchemaAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheDisasterLayoutAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGoldenSeed42Turn300_MovedForTheM4SchemaAlone` — [TEETH (attribution ladder)] the strip control reproduces a recorded hash — reproducibility, not semantics
- `FoundedGolden_Seed42Turn300_MatchesPinnedConstant` — [TEETH (golden)] canonical founded world hash at turn 300 — reproducibility, not semantics
- `Driven_Seed42_300Turns_EveryQuantityReconcilesEveryTurn` — [SEMANTIC (measured-turn pins)] driven seed 42, 300 turns: every quantity reconciles every turn AND the measured firsts hold — first starvation turn 8, first decay 82, trade 7, pottery 5
- `ArtisanPresence_CanPrecedeLocalLatch_ViaMigration_IsNotEmergence` — [TEETH (measured pin)] the artisan latch turn 43 on the founded world — moved at step 2 by the turn-2 headroom hold; the mutant restores the pre-cap 39
- `D_Cap_NoGrowthOnAGranary` — [SEMANTIC] a stockpile is not an influx: abandoned rig, d = 0, S = 0 ⇒ N_lim = 0 ⇒ H_0 = 0 ⇒ births replace deaths in adult-equivalents per step (replica-exact births and reservoir every turn) while the uncapped twin grows
- `D_PostFamine_RecoversTowardLimit` — [SEMANTIC] abandonment famine (harvest 0, FAMINE read, the whole deficit starves) then restore: population rises monotonically toward N_lim after the last d > 0 turn and never overshoots the §28 bound — the banked rebound releases only into headroom
- `D_Rebound_StrictRelease_NeverInvents` — [SEMANTIC] under a sustained ceiling (N_lim = 0, d = 0) the reservoir may only FALL, by exactly the realised (m-scaled) release; extra births over the rebound-off twin ≤ the bank — deferred, not invented; the §28 bound holds every step
- `Stress_DoesNotSpike_NoBirthDeficit_NothingBanked` — [SEMANTIC] G2 re-anchored phase 1: a famine-window harvest at ≈ 78 % of demand (worst d 0.171 ≤ a, every famine-read turn STRESS) starves EXACTLY nobody, per-capita mortality within 1.1× the fed baseline, births ≥ 0.9× baseline, reservoir exactly 0.0 — a survivable shortfall is not a famine
- `FirstReign_PostFix_HarvestDiesWithThePeople_NoFoodMountain` — [TEETH (golden + shape)] FirstReign turn-40 hash plus its shape asserts

Semantic kills: 5 (teeth-only kills excluded). At least one semantic test per mutant: YES.


## 4. What this record changes

Nothing in the kernel or the tests: all three mutants die on the tests the step-2 commit said would
kill them, plus the goldens. It closes the packet's step 3 (spec §4 row T4.21-3, "mutants
M-DEM-CAP, M-DEM-ADAPT, M-DEM-BANKCAPPED"). The bound and the load are recorded so the verifier
can compare its own re-run (the spec's verifier line: "rerun every mutant with the stated time
bound (3× clean suite)") against a stated baseline rather than a number.
